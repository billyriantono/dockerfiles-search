MetaArgs: null
Stages:
- BaseName: ubuntu:eoan
  Commands:
  - Name: workdir
    Path: /tmp
  - CmdLine:
    - apt-get update -y && apt-get install -y --no-install-recommends wget openssl
      net-tools libpython2.7 python-setuptools python-minimal python-pkg-resources
      python-m2crypto python-apsw python-lxml python-libxslt1 && mkdir -p /opt/acestream
      && wget -o - http://acestream.org/downloads/linux/acestream_3.1.49_ubuntu_18.04_x86_64.tar.gz
      && tar -zxvf acestream_3.1.49_ubuntu_18.04_x86_64.tar.gz -C /opt/acestream &&
      cp -Rnv /usr/lib/x86_64-linux-gnu/* /opt/acestream/lib/ && apt-get clean &&
      apt autoremove -y && rm -rf /tmp/* /opt/acestream/data/plugins/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - root/
    - /
  - CmdLine:
    - chmod +x /opt/start.sh
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /
  From:
    Image: ubuntu:eoan
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:eoan
