MetaArgs: null
Stages:
- BaseName: busybox
  Commands:
  - Name: workdir
    Path: /downsource
  - Env:
    - Key: DOWNSOURCEURL
      Value: '"https://dl.k8s.io/v1.13.5/kubernetes-server-linux-amd64.tar.gz"'
    Name: env
  - CmdLine:
    - wget ${DOWNSOURCEURL}
    Name: run
    PrependShell: true
  - CmdLine:
    - echo ''' cp -rf /downsource/* /down/; if [ $? -ne 0 ]; then     echo 'False';
      else      echo '$DOWNSOURCEURL Done'; fi; ''' > /downsource/down.sh && chmod
      +x /downsource/down.sh
    Name: run
    PrependShell: true
  - CmdLine:
    - /downsource/down.sh
    Name: cmd
    PrependShell: true
  From:
    Image: busybox
  Name: ""
  Platform: ""
  SourceCode: FROM busybox
