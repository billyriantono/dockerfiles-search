MetaArgs: null
Stages:
- BaseName: ubuntu:16.04
  Commands:
  - Maintainer: FUJIMOTO Satoshi
    Name: maintainer
  - Name: user
    User: root
  - Name: workdir
    Path: /root
  - CmdLine:
    - apt-get update && apt-get upgrade -y  && apt-get install wget gcc make libjson-c-dev
      libreadline6 libreadline6-dev texinfo telnet -y  && wget https://github.com/FRRouting/frr/releases/download/frr-2.0/frr-2.0.tar.gz  &&
      tar xvf frr-2.0.tar.gz  && cd frr-2.0  && ./configure && make && make install  &&
      echo include /usr/local/lib >> /etc/ld.so.conf  && ldconfig  && mv /usr/local/etc/bgpd.conf.sample
      /usr/local/etc/bgpd.conf
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:16.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:16.04
