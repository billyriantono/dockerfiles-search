MetaArgs: null
Stages:
- BaseName: adoptopenjdk/openjdk8:jdk8u202-b08-alpine-slim
  Commands:
  - Maintainer: dockerfiles@nearpod.com
    Name: maintainer
  - Name: workdir
    Path: /usr/local
  - Env:
    - Key: LANG
      Value: '"C.UTF-8"'
    Name: env
  - Env:
    - Key: PATH
      Value: $PATH:$JAVA_HOME/bin
    Name: env
  - CmdLine:
    - set -ex;     WORKDIR=$PWD;     apk --no-cache add ca-certificates curl bash;     update-ca-certificates;     rm
      -rf /var/lib/{apt,dpkg,cache,log}/ /tmp/* /var/tmp/*;     java -version
    Name: run
    PrependShell: true
  From:
    Image: adoptopenjdk/openjdk8:jdk8u202-b08-alpine-slim
  Name: ""
  Platform: ""
  SourceCode: FROM adoptopenjdk/openjdk8:jdk8u202-b08-alpine-slim
