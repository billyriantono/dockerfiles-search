MetaArgs:
- DefaultValue: 1.13.1
  Key: TF_VERSION
  ProvidedValue: null
  Value: 1.13.1
Stages:
- BaseName: tensorflow/tensorflow:${TF_VERSION}
  Commands:
  - Env:
    - Key: LOG_DIR
      Value: '"/tmp"'
    Name: env
  - Env:
    - Key: PORT
      Value: '"8080"'
    Name: env
  - CmdLine:
    - /bin/bash
    - -c
    - /usr/local/bin/tensorboard --logdir ${LOG_DIR} --host 0.0.0.0 --port ${PORT}
    Name: cmd
    PrependShell: false
  From:
    Image: tensorflow/tensorflow:${TF_VERSION}
  Name: ""
  Platform: ""
  SourceCode: FROM tensorflow/tensorflow:${TF_VERSION}
