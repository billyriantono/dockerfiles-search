MetaArgs: null
Stages:
- BaseName: python:3.6
  Commands:
  - Name: workdir
    Path: /usr/src/app
  - CmdLine:
    - mkdir -p /etc/homeassistant
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get -y install --no-install-recommends   build-essential
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get -y install --no-install-recommends   nmap net-tools
      libcurl3-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get -y install --no-install-recommends   bluetooth libglib2.0-dev
      libbluetooth-dev
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - requirements.txt
    - .
  - CmdLine:
    - pip install --no-cache-dir --upgrade pip   && pip install --no-cache-dir -r
      requirements.txt   && pip install --no-cache-dir mysqlclient psycopg2 uvloop
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -SL https://github.com/home-assistant/home-assistant/archive/0.37.1.tar.gz   |
      tar -xz --strip-components 1 home-assistant-0.37.1/homeassistant
    Name: run
    PrependShell: true
  - CmdLine:
    - python
    - -m
    - homeassistant
    - --config
    - /etc/homeassistant
    Name: cmd
    PrependShell: false
  From:
    Image: python:3.6
  Name: ""
  Platform: ""
  SourceCode: FROM python:3.6
