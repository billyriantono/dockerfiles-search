MetaArgs: null
Stages:
- BaseName: alpine:3.3
  Commands:
  - CmdLine:
    - apk --update-cache add curl   && rm -rf /var/cache/apk/*
    Name: run
    PrependShell: true
  - Env:
    - Key: CONFD_VERSION
      Value: '"0.11.0"'
    Name: env
  - CmdLine:
    - curl -fsSL "https://github.com/kelseyhightower/confd/releases/download/v$CONFD_VERSION/confd-$CONFD_VERSION-linux-amd64"     -o
      /usr/bin/confd   && chmod u+x /usr/bin/confd
    Name: run
    PrependShell: true
  From:
    Image: alpine:3.3
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.3
