# Node LTS, Carbon, node version 8 on debian
# Node LTS, Dubnium, nove version 10 on debian
FROM node:dubnium

# install Firebase and Vue CLI
RUN npm install -g firebase-tools @vue/cli @vue/cli-service-global
