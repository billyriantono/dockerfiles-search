MetaArgs: null
Stages:
- BaseName: php:7.3.1-fpm-stretch
  Commands:
  - CmdLine:
    - "php -r \"copy('https://getcomposer.org/installer', 'composer-setup.php');\"
      \t&& php -r \"if (hash_file('sha384', 'composer-setup.php') === '48e3236262b34d30969dca3c37281b3b4bbe3221bda826ac6a9a62d6444cdb0dcd0615698a5cbe587c3f0fe57a54d8f5')
      { echo 'Installer verified'; } else { echo 'Installer corrupt'; unlink('composer-setup.php');
      } echo PHP_EOL;\" \t&& php composer-setup.php --install-dir=/usr/local/bin --filename=composer
      --version=1.8.3 \t&& php -r \"unlink('composer-setup.php');\""
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y git
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get install -y libzip-dev && docker-php-ext-install zip
    Name: run
    PrependShell: true
  - CmdLine:
    - docker-php-ext-install pdo_mysql
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get install -y libssl-dev && pecl install mongodb && docker-php-ext-enable
      mongodb
    Name: run
    PrependShell: true
  - CmdLine:
    - "curl -s https://phar.phpunit.de/phpunit-7.5.4.phar -O -L \t&& chmod +x phpunit-7.5.4.phar
      \t&& mv phpunit-7.5.4.phar /usr/local/bin/phpunit"
    Name: run
    PrependShell: true
  From:
    Image: php:7.3.1-fpm-stretch
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.3.1-fpm-stretch
