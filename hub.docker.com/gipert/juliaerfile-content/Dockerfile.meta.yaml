MetaArgs: null
Stages:
- BaseName: giodegas/opensim-bin
  Commands:
  - Maintainer: Giovanni De Gasperis  @giodegas
    Name: maintainer
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - after_install.sh
    - /sbin/after_install
  - CmdLine:
    - apt-get update && apt-get -y install unzip ssh-client &&     cd /opt && rm -fr
      opensim &&     wget http://download.craft-world.org/0.8.2-craft-dev.zip && unzip
      0.8.2-craft-dev.zip && mv 0.8.2-craft-dev2 opensim &&     chmod +x /sbin/after_install
      &&     apt-get remove --purge -y $BUILD_PACKAGES && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - 9000/tcp
  - Name: expose
    Ports:
    - 9000/udp
  From:
    Image: giodegas/opensim-bin
  Name: ""
  Platform: ""
  SourceCode: FROM giodegas/opensim-bin
