MetaArgs: null
Stages:
- BaseName: jenkins:2.46.1
  Commands:
  - Env:
    - Key: PATH
      Value: $PATH:$JENKINS_HOME/workspace/scripts-ci
    Name: env
  - Name: user
    User: root
  - CmdLine:
    - apt-get update && apt-get install -y apt-transport-https ca-certificates curl
      software-properties-common
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -fsSL https://download.docker.com/linux/debian/gpg | apt-key add -
    Name: run
    PrependShell: true
  - CmdLine:
    - add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/debian     $(lsb_release
      -cs)     stable"
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y docker-ce jq gettext xmlstarlet dos2unix
    Name: run
    PrependShell: true
  From:
    Image: jenkins:2.46.1
  Name: ""
  Platform: ""
  SourceCode: FROM jenkins:2.46.1
