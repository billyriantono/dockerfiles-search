MetaArgs: null
Stages:
- BaseName: alpine
  Commands:
  - Key: LEDGER_VERSION
    Name: arg
    Value: next
  - CmdLine:
    - "apk add --no-cache --virtual .build-deps \t\tboost boost-iostreams boost-unit_test_framework
      gmp mpfr3 \t\tboost-dev gmp-dev mpfr-dev cmake make g++ wget \t&& wget -qO-
      https://github.com/ledger/ledger/archive/$LEDGER_VERSION.tar.gz | tar xz -C
      /tmp \t&& cd /tmp/ledger-$LEDGER_VERSION \t&& ./acprep update --boost-suffix=-mt
      --prefix=/usr/local -j2 install \t&& rm -rf /tmp/ledger-$LEDGER_VERSION \t&&
      runDeps=\"$( \t\tscanelf --needed --nobanner --recursive /usr/local \t\t\t|
      awk '{ gsub(/,/, \"\\nso:\", $2); print \"so:\" $2 }' \t\t\t| sort -u \t\t\t|
      xargs -r apk info --installed \t\t\t| sort -u \t)\" \t&& apk add --no-cache
      --virtual .php-rundeps $runDeps \t&& apk del .build-deps"
    Name: run
    PrependShell: true
  - CmdLine:
    - ledger
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - bal
    Name: cmd
    PrependShell: false
  From:
    Image: alpine
  Name: ""
  Platform: ""
  SourceCode: FROM alpine
