MetaArgs: null
Stages:
- BaseName: konstruktoid/alpine
  Commands:
  - CmdLine:
    - apk update &&     apk upgrade &&     apk add --update acf-openssh
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - files/sshd_config
    - /etc/ssh/sshd_config
  - CmdLine:
    - ssh-keygen -f /etc/ssh/ssh_host_rsa_key -N '' -t rsa &&     ssh-keygen -f /etc/ssh/ssh_host_ecdsa_key
      -N '' -t ecdsa
    Name: run
    PrependShell: true
  - CmdLine:
    - /usr/sbin/sshd
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - -D
    Name: cmd
    PrependShell: false
  From:
    Image: konstruktoid/alpine
  Name: ""
  Platform: ""
  SourceCode: FROM konstruktoid/alpine
