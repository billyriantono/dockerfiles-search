MetaArgs:
- DefaultValue: stable
  Key: DOCKER_VERSION
  ProvidedValue: null
  Value: stable
Stages:
- BaseName: docker:${DOCKER_VERSION}
  Commands:
  - Key: DOCKER_VERSION
    Name: arg
    Value: null
  - CmdLine:
    - apk add --no-cache --virtual .app-deps        bash         git         py-pip         gettext         python-dev         libffi-dev         openssl-dev         gcc         libc-dev         make
      gettext    && pip install docker-compose
    Name: run
    PrependShell: true
  From:
    Image: docker:${DOCKER_VERSION}
  Name: ""
  Platform: ""
  SourceCode: FROM docker:${DOCKER_VERSION}
