MetaArgs: null
Stages:
- BaseName: alpine:3.6
  Commands:
  - CmdLine:
    - "set -x \t&& apk add --update sniproxy \t&& rm -rf /var/cache/apk/*"
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /etc/sniproxy
  - Name: expose
    Ports:
    - "443"
  - CmdLine:
    - /usr/sbin/sniproxy
    - -c
    - /etc/sniproxy/sniproxy.conf
    - -f
    Name: cmd
    PrependShell: false
  From:
    Image: alpine:3.6
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.6
