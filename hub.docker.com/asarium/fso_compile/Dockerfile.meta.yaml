MetaArgs: null
Stages:
- BaseName: buildpack-deps:stretch
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - llvm.list
    - /etc/apt/sources.list.d/
  - CmdLine:
    - wget -O - https://apt.llvm.org/llvm-snapshot.gpg.key | apt-key add -
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get -yq install clang-7 lld-7 libc++-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - curl https://cmake.org/files/v3.8/cmake-3.8.2-Linux-x86_64.tar.gz | tar -xzf
      - -C /usr/local --strip-components=1
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get -yq install ca-certificates ninja-build libopenal-dev
      libsdl2-dev libfreetype6-dev valgrind
    Name: run
    PrependShell: true
  From:
    Image: buildpack-deps:stretch
  Name: ""
  Platform: ""
  SourceCode: FROM buildpack-deps:stretch
