MetaArgs: null
Stages:
- BaseName: python:2.7-slim
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y         gcc         gettext         git         postgresql-client
      libpq-dev         libfreetype6-dev libjpeg-dev         libxslt1-dev libxml2-dev         make     --no-install-recommends
      && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - pip install --no-cache-dir tox
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /usr/src/app
  From:
    Image: python:2.7-slim
  Name: ""
  Platform: ""
  SourceCode: FROM python:2.7-slim
