MetaArgs: null
Stages:
- BaseName: php:7.2-fpm-alpine
  Commands:
  - Name: expose
    Ports:
    - "8000"
  - CmdLine:
    - apk update && apk upgrade     && apk add gcc libxml2 libxslt libcurl libc-dev
      libxml2-dev libxslt-dev make curl icu-dev zlib-dev git redis autoconf    &&
      docker-php-ext-install intl     && pecl install redis     && pecl install apcu     &&
      pecl install xdebug     && docker-php-ext-install mbstring     && docker-php-ext-install
      zip     && docker-php-ext-enable apcu     && docker-php-ext-enable redis     &&
      docker-php-ext-enable xdebug     && apk del gcc libxml2-dev libxslt-dev make     &&
      rm -rf /var/cache/apk/*
    Name: run
    PrependShell: true
  - Env:
    - Key: COMPOSER_ALLOW_SUPERUSER
      Value: "1"
    Name: env
  - Env:
    - Key: COMPOSER_HOME
      Value: /composer
    Name: env
  - Env:
    - Key: COMPOSER_VERSION
      Value: 1.8.0
    Name: env
  - Env:
    - Key: COMPOSER_INSTALLER_SIG
      Value: 93b54496392c062774670ac18b134c3b3a95e5a5e5c8f1a9f115f203b75bf9a129d5daa8ba6a13e2cc8a1da0806388a8
    Name: env
  - CmdLine:
    - php -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"     &&
      php -r "if (hash_file('sha384', 'composer-setup.php') === '${COMPOSER_INSTALLER_SIG}')
      { echo 'Installer verified'; } else { echo 'Installer corrupt'; unlink('composer-setup.php');
      } echo PHP_EOL;"     && php composer-setup.php --install-dir=/usr/bin --filename=composer
      --version=${COMPOSER_VERSION}     && php -r "unlink('composer-setup.php');"     &&
      composer --ansi --version --no-interaction
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - 60-user.ini
    - /usr/local/etc/php/conf.d/
  From:
    Image: php:7.2-fpm-alpine
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.2-fpm-alpine
