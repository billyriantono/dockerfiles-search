MetaArgs: null
Stages:
- BaseName: alpine
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"ospatil@gmail.com"'
    Name: label
  - CmdLine:
    - apk update && apk upgrade && apk add bash build-base gdb valgrind git cmake
      nasm &&   mkdir -p workspace/github && cd workspace/github && git clone https://github.com/libuv/libuv.git
      &&   cd libuv && mkdir -p out/cmake && cd out/cmake &&   cmake -DBUILD_TESTING=ON
      ../.. && make && make install
    Name: run
    PrependShell: true
  From:
    Image: alpine
  Name: ""
  Platform: ""
  SourceCode: FROM alpine
