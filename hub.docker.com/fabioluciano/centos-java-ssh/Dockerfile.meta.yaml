MetaArgs: null
Stages:
- BaseName: fabioluciano/centos-base-java
  Commands:
  - Maintainer: Fábio Luciano <fabio@naoimporta.com>
    Name: maintainer
  - Labels:
    - Key: Description
      Value: '"CentOS Java SSH"'
    Name: label
  - Name: workdir
    Path: /opt
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - custom/supervisor.d/*
    - /etc/supervisor.d/
  - CmdLine:
    - yum -y update && yum install -y openssh-server   && mkdir /opt/app   && printf
      "password\npassword" | adduser app   && echo "app:password" | chpasswd   &&
      ssh-keygen -q -t rsa -f /etc/ssh/ssh_host_rsa_key -P ""   && ssh-keygen -q -t
      dsa -f /etc/ssh/ssh_host_dsa_key -P ""   && ssh-keygen -q -t ecdsa -f /etc/ssh/ssh_host_ecdsa_key
      -P ""  && ssh-keygen -q -t ed25519 -f /etc/ssh/ssh_host_ed25519_key -P ""   &&
      echo "AllowUsers app" >> /etc/ssh/sshd_config   && yum clean all && rm -rf /tmp/*
      && rm -rf /var/cache/yum
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /opt/app
  - Name: expose
    Ports:
    - 22/tcp
    - 8080/tcp
    - 8443/tcp
  From:
    Image: fabioluciano/centos-base-java
  Name: ""
  Platform: ""
  SourceCode: FROM fabioluciano/centos-base-java
