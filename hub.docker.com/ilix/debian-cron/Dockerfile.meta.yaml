MetaArgs: null
Stages:
- BaseName: debian:jessie
  Commands:
  - CmdLine:
    - apt-get update
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get -y install cron
    Name: run
    PrependShell: true
  - CmdLine:
    - printenv > /etc/environment && crontab /etc/cron.d/crontab && chmod -R 0644
      /etc/cron.d && cron && sh -c "touch /var/log/cron.log && tail -f /var/log/cron.log"
    Name: cmd
    PrependShell: true
  From:
    Image: debian:jessie
  Name: ""
  Platform: ""
  SourceCode: FROM debian:jessie
