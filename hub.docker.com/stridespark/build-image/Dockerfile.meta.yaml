MetaArgs: null
Stages:
- BaseName: circleci/openjdk:8-jdk-stretch-node
  Commands:
  - CmdLine:
    - wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc |     sudo
      apt-key add -
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo sh -c 'echo "     deb http://apt.postgresql.org/pub/repos/apt/ stretch-pgdg
      main" >> /etc/apt/sources.list.d/postgresql.list'
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo apt-get -y -qq update &&     sudo apt-get -y -qq install     python-pip     python-dev     build-essential     postgresql-client-10     mysql-client     apt-transport-https     software-properties-common     jq     rsync     gnupg2     &&
      sudo rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -fsSL https://download.docker.com/linux/debian/gpg |  sudo apt-key add
      - &&     sudo add-apt-repository     "deb [arch=amd64] https://download.docker.com/linux/debian     $(lsb_release
      -cs)     stable" &&     sudo apt-get update &&     sudo apt-get -y -qq install
      docker-ce &&     sudo rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo pip install pipsi==0.9
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo pipsi --home=/ --bin-dir=/bin install awscli==1.16.120
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo pipsi --home=/ --bin-dir=/bin install credstash==1.15.0
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo wget --progress=dot:mega https://storage.googleapis.com/kubernetes-release/release/v1.9.11/bin/linux/amd64/kubectl
      && sudo chmod +x kubectl && sudo mv kubectl /usr/local/bin
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo npm install -g yarn@1.13.0
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - circle.npmrc
    - /home/circleci/.npmrc
  - Chown: ""
    Name: add
    SourcesAndDest:
    - circle.npmrc
    - /usr/local/share/.npmrc
  - Env:
    - Key: AWS_DEFAULT_REGION
      Value: '"us-west-2"'
    Name: env
  - Env:
    - Key: PGHOST
      Value: '"localhost"'
    Name: env
  - Env:
    - Key: PGUSER
      Value: '"postgres"'
    Name: env
  - Env:
    - Key: BASH_ENV
      Value: '"/app/bash.env"'
    Name: env
  - Env:
    - Key: USE_STAGING
      Value: '"true"'
    Name: env
  - Env:
    - Key: NODE_ENV
      Value: '"test"'
    Name: env
  - CmdLine:
    - sudo mkdir /app && sudo chown circleci:circleci app
    Name: run
    PrependShell: true
  From:
    Image: circleci/openjdk:8-jdk-stretch-node
  Name: ""
  Platform: ""
  SourceCode: FROM circleci/openjdk:8-jdk-stretch-node
