MetaArgs: null
Stages:
- BaseName: alpine:3.7
  Commands:
  - Maintainer: from wiggins
    Name: maintainer
  - Env:
    - Key: TERM
      Value: linux
    Name: env
  - CmdLine:
    - set -x && apk upgrade --update --no-cache && apk add --update --no-cache bash
      curl ca-certificates openssl curl tar iproute2 supervisor && rm -rf /var/cache/apk/*
    Name: run
    PrependShell: true
  From:
    Image: alpine:3.7
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.7
