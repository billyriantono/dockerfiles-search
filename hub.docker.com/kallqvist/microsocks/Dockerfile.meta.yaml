MetaArgs: null
Stages:
- BaseName: debian:stretch-slim
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y git gcc make dumb-init && apt-get clean
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir /build && cd /build && git clone https://github.com/rofl0r/microsocks.git
      && cd microsocks && make && make install && rm -R /build
    Name: run
    PrependShell: true
  - Name: user
    User: nobody
  - Name: workdir
    Path: /
  - CmdLine:
    - dumb-init
    - --
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - microsocks
    Name: cmd
    PrependShell: false
  From:
    Image: debian:stretch-slim
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch-slim
