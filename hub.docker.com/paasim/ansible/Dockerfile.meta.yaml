MetaArgs: null
Stages:
- BaseName: debian
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - ansible.list
    - /ansible.list
  - CmdLine:
    - apt-get update   && apt-get install -y gnupg2   && mv /ansible.list /etc/apt/sources.list.d/   &&
      apt-key adv --keyserver keyserver.ubuntu.com --recv-keys 93C4A3FD7BB9C367   &&
      apt-get purge -y --auto-remove gnupg2   && apt-get update   && apt-get install
      -y ansible   && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /data
  - CmdLine:
    - ansible
    Name: entrypoint
    PrependShell: false
  From:
    Image: debian
  Name: ""
  Platform: ""
  SourceCode: FROM debian
