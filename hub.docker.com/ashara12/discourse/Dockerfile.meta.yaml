MetaArgs: null
Stages:
- BaseName: samsaffron/discourse_base:0.2.3
  Commands:
  - Maintainer: Sam Saffron "https://twitter.com/samsaffron"
    Name: maintainer
  - CmdLine:
    - useradd discourse -s /bin/bash -m -U &&    mkdir /var/www && cd /var/www &&     git
      clone --depth 1 https://github.com/discourse/discourse.git &&     chown -R discourse:discourse
      /var/www/discourse &&    cd /var/www/discourse &&     sudo -u discourse RAILS4=1
      bundle install --deployment          --without test --without development &&    cd
      /var/www/discourse/vendor/bundle &&       find . -name tmp -type d | xargs rm
      -rf && cd /
    Name: run
    PrependShell: true
  From:
    Image: samsaffron/discourse_base:0.2.3
  Name: ""
  Platform: ""
  SourceCode: FROM samsaffron/discourse_base:0.2.3
