MetaArgs: null
Stages:
- BaseName: python:3.7-alpine3.8
  Commands:
  - Env:
    - Key: workdir
      Value: /data
    Name: env
  - Name: workdir
    Path: ${workdir}
  - CmdLine:
    - mkdir -p ${workdir} && adduser python --disabled-password
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - .
    - ${workdir}
  - Name: workdir
    Path: ${workdir}
  - CmdLine:
    - pip install --upgrade google-api-python-client oauth2client progressbar2 &&     python
      setup.py install
    Name: run
    PrependShell: true
  - Name: user
    User: python
  - CmdLine:
    - youtube-upload
    Name: entrypoint
    PrependShell: false
  From:
    Image: python:3.7-alpine3.8
  Name: ""
  Platform: ""
  SourceCode: FROM python:3.7-alpine3.8
