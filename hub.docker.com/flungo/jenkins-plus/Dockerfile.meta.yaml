MetaArgs: null
Stages:
- BaseName: jenkins
  Commands:
  - Maintainer: Fabrizio Lungo <docker@flungo.me>
    Name: maintainer
  - Key: user
    Name: arg
    Value: jenkins
  - Name: user
    User: root
  - CmdLine:
    - apt-get update && apt-get install -y cloc graphviz && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Name: user
    User: ${user}
  From:
    Image: jenkins
  Name: ""
  Platform: ""
  SourceCode: FROM jenkins
