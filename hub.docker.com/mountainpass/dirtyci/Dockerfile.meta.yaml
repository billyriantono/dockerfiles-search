MetaArgs:
- DefaultValue: 10.15.3
  Key: NODE_VERSION
  ProvidedValue: null
  Value: 10.15.3
Stages:
- BaseName: ubuntu:bionic
  Commands:
  - Env:
    - Key: NODE_ENV
      Value: production
    Name: env
  - Env:
    - Key: TZ
      Value: Australia/Sydney
    Name: env
  - Env:
    - Key: PKG
      Value: docker-ce-cli_18.09.3~3-0~ubuntu-bionic_amd64.deb
    Name: env
  - Env:
    - Key: URL
      Value: https://download.docker.com/linux/ubuntu/dists/bionic/pool/stable/amd64/${PKG}
    Name: env
  - CmdLine:
    - apt-get update && apt-get install --assume-yes --no-install-recommends curl
      xz-utils ca-certificates && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -OL ${URL} && dpkg -i ${PKG} && rm ${PKG}
    Name: run
    PrependShell: true
  - CmdLine:
    - docker --version
    Name: run
    PrependShell: true
  - Env:
    - Key: NODE_VERSION
      Value: 10.15.3
    Name: env
  - CmdLine:
    - curl -s -N https://nodejs.org/dist/v${NODE_VERSION}/node-v${NODE_VERSION}-linux-x64.tar.xz
      | tar -xJ --directory /usr/local --strip-components 1
    Name: run
    PrependShell: true
  - CmdLine:
    - node -v
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -L https://github.com/Yelp/dumb-init/releases/download/v1.2.2/dumb-init_1.2.2_amd64
      -o /usr/local/bin/dumb-init
    Name: run
    PrependShell: true
  - CmdLine:
    - chmod +x /usr/local/bin/dumb-init
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - .
    - .
  - CmdLine:
    - ln -sf /dev/stdout /root/stdout.log && ln -sf /dev/stderr /root/stderr.log
    Name: run
    PrependShell: true
  - CmdLine:
    - dumb-init node index.js
    Name: entrypoint
    PrependShell: true
  - Name: expose
    Ports:
    - "3000"
  From:
    Image: ubuntu:bionic
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:bionic
