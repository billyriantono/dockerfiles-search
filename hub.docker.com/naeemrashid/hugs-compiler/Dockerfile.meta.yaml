MetaArgs: null
Stages:
- BaseName: debian:jessie-slim
  Commands:
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - CmdLine:
    - "apt-get update && apt-get install -y     libhugs-base-bundled     libhugs-haskell98-bundled
      \    hugs     haskell-doc  --no-install-recommends \t&& rm -rf /var/lib/apt/lists/*"
    Name: run
    PrependShell: true
  - Env:
    - Key: http_proxy
      Value: '""'
    Name: env
  - Env:
    - Key: https_proxy
      Value: '""'
    Name: env
  - CmdLine:
    - hugs
    Name: entrypoint
    PrependShell: false
  From:
    Image: debian:jessie-slim
  Name: ""
  Platform: ""
  SourceCode: FROM debian:jessie-slim
