FROM debian:jessie

MAINTAINER "Jeremy CURNY" <jeremy.curny.pro@gmail.com>

RUN apt-get update && apt-get install -y \
    autoconf automake build-essential debconf-utils gcc libtool make \
    curl fail2ban htop iftop iotop telnet wget \
    dos2unix nano tree zip \
    git git-flow \
    mysql-client
