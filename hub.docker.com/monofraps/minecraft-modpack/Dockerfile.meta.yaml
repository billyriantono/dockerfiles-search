MetaArgs: null
Stages:
- BaseName: openjdk:8-jre
  Commands:
  - CmdLine:
    - mkdir /minecraft
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /minecraft
  - CmdLine:
    - mkdir /var/lib/minecraft
    Name: run
    PrependShell: true
  - CmdLine:
    - ln -s /var/lib/minecraft world
    Name: run
    PrependShell: true
  - CmdLine:
    - wget https://media.forgecdn.net/files/2559/308/Modern+Skyblock-3.4+Server+File.zip
      -O server.zip && unzip server.zip && rm server.zip
    Name: run
    PrependShell: true
  - CmdLine:
    - sh
    - -c
    - java -Xmx${JVM_MAX_HEAP:-6g} -Xms${JVM_MIN_HEAP:-2g} -Dfml.queryResult=confirm
      -jar forge*universal.jar nogui
    Name: cmd
    PrependShell: false
  From:
    Image: openjdk:8-jre
  Name: ""
  Platform: ""
  SourceCode: FROM openjdk:8-jre
