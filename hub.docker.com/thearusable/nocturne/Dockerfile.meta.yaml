MetaArgs: null
Stages:
- BaseName: ubuntu:18.04
  Commands:
  - CmdLine:
    - apt-get -y update     && apt-get upgrade -y     && apt-get install wget git
      cppcheck -y     && apt-get autoremove -y
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get install build-essential gcc-8 g++-8 -y     && update-alternatives --install
      /usr/bin/gcc gcc /usr/bin/gcc-7 700 --slave /usr/bin/g++ g++ /usr/bin/g++-7     &&
      update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-8 800 --slave /usr/bin/g++
      g++ /usr/bin/g++-8     && apt-get autoremove -y
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get install freeglut3 freeglut3-dev libglew1.5 libglew1.5-dev libglu1-mesa
      libglu1-mesa-dev libgl1-mesa-glx     libgl1-mesa-dev -y && apt-get autoremove
      -y
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get install libsdl2-dev -y && apt-get autoremove -y
    Name: run
    PrependShell: true
  - CmdLine:
    - wget https://github.com/Kitware/CMake/releases/download/v3.14.0/cmake-3.14.0.tar.gz     &&
      tar -xzf cmake-3.14.0.tar.gz     && cd cmake-3.14.0     && ./configure && make
      && make install && cd .. && rm -rf cmake-3.14.0
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:18.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:18.04
