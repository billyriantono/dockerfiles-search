MetaArgs: null
Stages:
- BaseName: alpine
  Commands:
  - CmdLine:
    - adduser -s /bin/sh -D user -G wheel
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /home/user/filesToPersist
  - CmdLine:
    - apk update && apk add --no-cache wget tar ca-certificates ncurses-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - wget -O /tmp/brogue.tbz2 https://sites.google.com/site/broguegame/brogue-1.7.4-linux-amd64.tbz2
      && mkdir /home/user/brogue && tar -C /home/user/brogue -xvf /tmp/brogue.tbz2
      --strip-components 1 && rm /tmp/brogue.tbz2
    Name: run
    PrependShell: true
  - CmdLine:
    - apk add --no-cache build-base gcc abuild binutils bash && make --directory=/home/user/brogue
      clean && make --directory=/home/user/brogue curses && apk del build-base gcc
      abuild binutils ca-certificates bash
    Name: run
    PrependShell: true
  - Name: user
    User: user
  - Name: workdir
    Path: /home/user/filesToPersist
  - CmdLine:
    - sleep 0.2 ; /home/user/brogue/bin/brogue
    Name: entrypoint
    PrependShell: true
  From:
    Image: alpine
  Name: ""
  Platform: ""
  SourceCode: FROM alpine
