MetaArgs: null
Stages:
- BaseName: php:7.0-cli
  Commands:
  - Maintainer: Jonas Renggli <jonas.renggli@visol.ch>
    Name: maintainer
  - CmdLine:
    - apt-get update     && apt-get install -y         git         wget         unzip         rsync         openssh-client     &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - buildRequirements="libxml2-dev"     && apt-get update && apt-get install -y
      ${buildRequirements}     && docker-php-ext-install soap     && apt-get purge
      -y ${buildRequirements}     && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - buildRequirements="libpng-dev libjpeg-dev libfreetype6-dev"     && apt-get update
      && apt-get install -y ${buildRequirements}     && docker-php-ext-configure gd
      --with-freetype-dir=/usr/include/ --with-jpeg-dir=/usr/lib     && docker-php-ext-install
      gd     && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - buildRequirements="libicu-dev g++"     && apt-get update && apt-get install
      -y ${buildRequirements}     && docker-php-ext-install intl     && apt-get purge
      -y ${buildRequirements}     && runtimeRequirements="libicu57"     && apt-get
      install -y --auto-remove ${runtimeRequirements}     && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - buildRequirements="zlib1g-dev"     && apt-get update && apt-get install -y ${buildRequirements}     &&
      docker-php-ext-install zip     && apt-get purge -y ${buildRequirements}     &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - wget https://raw.githubusercontent.com/composer/getcomposer.org/76a7060ccb93902cd7576b67264ad91c8a2700e2/web/installer
      -O - -q | php -- --filename=composer --install-dir=/usr/bin
    Name: run
    PrependShell: true
  - CmdLine:
    - echo 'export PATH=~/.composer/vendor/bin:$PATH' >> ~/.bashrc
    Name: run
    PrependShell: true
  - CmdLine:
    - composer global require "squizlabs/php_codesniffer=*"
    Name: run
    PrependShell: true
  - CmdLine:
    - composer global require "hirak/prestissimo"
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update     && apt-get install -y         gnupg     && rm -rf /var/lib/apt/lists/*     &&
      curl -sL https://deb.nodesource.com/setup_10.x | bash -     && apt-get update     &&
      apt-get install -y nodejs     && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | apt-key add -     && echo
      "deb https://dl.yarnpkg.com/debian/ stable main" | tee /etc/apt/sources.list.d/yarn.list     &&
      apt-get update     && apt-get install -y yarn     && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - npm install -g grunt-cli bower
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update     && apt-get install -y         ruby         ruby-dev     &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update     && apt-get install -y         automake         libtool     &&
      rm -rf /var/lib/apt/lists/*     && gem install compass
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update     && apt-get install -y         build-essential         g++     &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  From:
    Image: php:7.0-cli
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.0-cli
