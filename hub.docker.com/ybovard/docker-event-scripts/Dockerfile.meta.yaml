MetaArgs: null
Stages:
- BaseName: alpine:latest
  Commands:
  - CmdLine:
    - apk update && apk upgrade &&     apk add python3 && pip3 install --upgrade pip
      &&     pip3 install docker docker-event-scripts &&     mkdir -p /etc/docker/events.d
    Name: run
    PrependShell: true
  - CmdLine:
    - /usr/bin/des
    Name: entrypoint
    PrependShell: true
  From:
    Image: alpine:latest
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:latest
