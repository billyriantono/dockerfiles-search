MetaArgs: null
Stages:
- BaseName: golang
  Commands:
  - CmdLine:
    - apt-get update &&   apt-get install -y awscli zip &&   rm -rf /var/lib/apt/lists/*
      &&   rm -rf /tmp/*
    Name: run
    PrependShell: true
  From:
    Image: golang
  Name: ""
  Platform: ""
  SourceCode: FROM golang
