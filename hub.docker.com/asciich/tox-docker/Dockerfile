From asciich/tox

MAINTAINER Reto Hasler <reto.hasler@asciich.ch>

RUN apk add --no-cache \
        docker \
    && \
    pip install --upgrade \
        docker \
    && \
    rm -rf /var/cache/* && \
    rm -rf /root/.cache/pip/

