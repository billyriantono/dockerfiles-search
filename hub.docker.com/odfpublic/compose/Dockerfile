FROM alpine:3.7
RUN apk add --no-cache git curl openssl docker py-pip
RUN curl -L 'https://github.com/docker/compose/releases/download/1.23.2/docker-compose-Linux-x86_64' > /usr/bin/docker-compose && chmod +x /usr/bin/docker-compose

