MetaArgs: null
Stages:
- BaseName: debian:stretch
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - conf/texlive.conf
    - /texlive.profile
  - CmdLine:
    - "PACKAGES=\"wget libswitch-perl\" \t&& apt-get update \t&& apt-get -y upgrade
      \t&& apt-get install -qq $PACKAGES --no-install-recommends \t&& wget -qO- http://mirror.ctan.org/systems/texlive/tlnet/install-tl-unx.tar.gz
      |tar xz \t&& cd install-tl-* \t&& ./install-tl -profile /texlive.profile \t&&
      rm -rf install-tl-* \t&& apt-get remove --purge -qq $PACKAGES \t&& apt-get autoremove
      --purge -qq \t&& rm -rf /var/lib/apt/lists/* \t&& mkdir /var/tex"
    Name: run
    PrependShell: true
  From:
    Image: debian:stretch
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch
