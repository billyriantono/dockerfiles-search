MetaArgs: null
Stages:
- BaseName: fedora:rawhide
  Commands:
  - Maintainer: Curtis L. Knight
    Name: maintainer
  - CmdLine:
    - yum -y update && yum clean all
    Name: run
    PrependShell: true
  - CmdLine:
    - yum -y install git yum-utils make rpmdevtools rpmlint cpp pylint
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - freeipa-devel-rpm-build
    - /usr/sbin/freeipa-devel-rpm-build
  - CmdLine:
    - chmod -v +x /usr/sbin/freeipa-devel-rpm-build
    Name: run
    PrependShell: true
  - CmdLine:
    - master
    Name: cmd
    PrependShell: false
  - CmdLine:
    - /usr/sbin/freeipa-devel-rpm-build
    Name: entrypoint
    PrependShell: false
  From:
    Image: fedora:rawhide
  Name: ""
  Platform: ""
  SourceCode: FROM fedora:rawhide
