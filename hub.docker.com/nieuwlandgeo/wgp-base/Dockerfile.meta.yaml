MetaArgs: null
Stages:
- BaseName: php:7.2-fpm-buster
  Commands:
  - CmdLine:
    - apt-get update   && apt-get install -y gnupg
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -s https://www.postgresql.org/media/keys/ACCC4CF8.asc | apt-key add -   &&
      echo "deb http://apt.postgresql.org/pub/repos/apt/ stretch-pgdg main" > /etc/apt/sources.list.d/pgdg.list
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sL https://deb.nodesource.com/setup_10.x | bash -   && apt-get install
      -y nodejs
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update   && apt-get install -y postgresql-client-9.6 libjpeg62-turbo-dev
      libpng-dev    libbz2-dev libfreetype6-dev libpq-dev libicu-dev unzip git wget
      cron gdal-bin libgeos-dev   libmagickwand-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - docker-php-ext-configure gd --with-freetype-dir=/usr/include --with-jpeg-dir=/usr/include   &&
      docker-php-ext-install -j$(nproc)   gd   pdo_pgsql   zip   bcmath   bz2   gettext   intl   sockets   &&
      pecl install apcu   && docker-php-ext-enable apcu   && pecl install imagick   &&
      docker-php-ext-enable imagick   && pecl install redis-4.0.2   && docker-php-ext-enable
      redis
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /opt
  - CmdLine:
    - git clone https://git.osgeo.org/gitea/geos/php-geos.git
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /opt/php-geos
  - CmdLine:
    - ./autogen.sh   && phpize   && ./configure   && make -j "$(nproc)"   && make
      install   && docker-php-ext-enable geos
    Name: run
    PrependShell: true
  - CmdLine:
    - php -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"   &&
      php -r "copy('https://composer.github.io/installer.sig', 'composer-setup.sig');"   &&
      php -r "if (hash_file('SHA384', 'composer-setup.php') === trim(file_get_contents('composer-setup.sig')))
      { echo 'Installer verified'; } else { echo 'Installer corrupt'; unlink('composer-setup.php');
      } echo PHP_EOL;"   && php composer-setup.php   && php -r "unlink('composer-setup.php');"   &&
      mv composer.phar /usr/local/bin/composer
    Name: run
    PrependShell: true
  From:
    Image: php:7.2-fpm-buster
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.2-fpm-buster
