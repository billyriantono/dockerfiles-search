MetaArgs: null
Stages:
- BaseName: debian:stable-slim
  Commands:
  - Maintainer: Yipikaye
    Name: maintainer
  - CmdLine:
    - apt-get update     --quiet   && apt-get install          --yes          --no-install-recommends          --no-install-suggests        boinc-client   &&
      apt-get clean   && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Expression: VOLUME /data
    Name: onbuild
  - Name: workdir
    Path: /data
  - CmdLine:
    - /usr/bin/boinc
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - --attach_project
    - www.worldcommunitygrid.org
    - 74bb7c9b222da499033a307a7e5003c8
    Name: cmd
    PrependShell: false
  From:
    Image: debian:stable-slim
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stable-slim
