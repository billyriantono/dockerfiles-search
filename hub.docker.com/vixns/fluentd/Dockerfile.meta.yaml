MetaArgs: null
Stages:
- BaseName: fluent/fluentd:v1.0
  Commands:
  - Name: workdir
    Path: /home/fluent
  - Env:
    - Key: PATH
      Value: /home/fluent/.gem/ruby/2.3.0/bin:$PATH
    Name: env
  - CmdLine:
    - apk --no-cache --update add build-base ruby-dev geoip geoip-dev libmaxminddb
      libmaxminddb-dev snappy-dev ruby-bundler     && gem install     fluent-plugin-prometheus     zookeeper     snappy     fluent-plugin-kafka     fluent-plugin-s3     fluent-plugin-sentry     fluent-plugin-elasticsearch     strptime     geoip2_c     geoip2_compat     fluent-plugin-multi-format-parser     fluent-plugin-genhashvalue     fluent-plugin-geoip     &&
      rm -rf /home/fluent/.gem/ruby/2.3.0/cache/*.gem && gem sources -c     && apk
      del build-base ruby-dev geoip-dev libmaxminddb-dev ruby-bundler && rm -rf /var/cache/apk/*
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "24224"
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - fluent.conf
    - /fluentd/etc/
  - CmdLine:
    - exec fluentd -c /fluentd/etc/$FLUENTD_CONF -p /fluentd/plugins $FLUENTD_OPT
    Name: cmd
    PrependShell: true
  From:
    Image: fluent/fluentd:v1.0
  Name: ""
  Platform: ""
  SourceCode: FROM fluent/fluentd:v1.0
