MetaArgs: null
Stages:
- BaseName: alpine:3.10
  Commands:
  - CmdLine:
    - apk add nodejs npm python jq
    Name: run
    PrependShell: true
  - CmdLine:
    - npm i -g --unsafe-perm=true --allow-root truffle
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - .
    - /app
  - Name: workdir
    Path: /app
  - CmdLine:
    - npm init -y && npm i --save ethers
    Name: run
    PrependShell: true
  - CmdLine:
    - truffle compile && truffle test --show-events --compile-all
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "BYTECODE:" &&     echo "==========" &&     cat /app/build/contracts/QARK.json
      | jq '.bytecode' &&     echo "==========" &&     echo "ABI:" &&     echo "=========="
      &&     cat /app/build/contracts/QARK.json | jq '.abi' &&     echo "=========="
    Name: run
    PrependShell: true
  - CmdLine:
    - truffle
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - test
    - --show-events
    - --compile-all
    Name: cmd
    PrependShell: false
  From:
    Image: alpine:3.10
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.10
