MetaArgs: null
Stages:
- BaseName: stox/nodejs
  Commands:
  - CmdLine:
    - sudo apt-get install -y curl git libfreetype6 libfontconfig
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sSL https://rvm.io/mpapis.asc | gpg --import -
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -L https://get.rvm.io | bash -s stable
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash -l -c 'export PATH=$PATH:/usr/local/rvm/bin >> .profile'
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash -l -c 'echo "source /usr/local/rvm/scripts/rvm" >> .profile'
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash -l -c "rvm requirements"
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash -l -c "rvm install 2.1.2"
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash -l -c "rvm use 2.1.2 --default"
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash -l -c "gem install compass"
    Name: run
    PrependShell: true
  From:
    Image: stox/nodejs
  Name: ""
  Platform: ""
  SourceCode: FROM stox/nodejs
