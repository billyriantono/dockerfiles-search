MetaArgs: null
Stages:
- BaseName: openjdk:8-jdk-slim
  Commands:
  - CmdLine:
    - mkdir -p /usr/share/man/man1mkdir -p /usr/share/man/man1
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update   && apt-get upgrade -y   && apt-get install maven -y   && apt-get
      clean   && rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - .
    - /usr/src/myapp
  - Name: workdir
    Path: /usr/src/myapp
  - CmdLine:
    - mvn -pl rdfunit-validate -am clean install -DskipTests=true
    Name: run
    PrependShell: true
  - CmdLine:
    - /usr/src/myapp/bin/rdfunit
    Name: entrypoint
    PrependShell: false
  From:
    Image: openjdk:8-jdk-slim
  Name: ""
  Platform: ""
  SourceCode: FROM openjdk:8-jdk-slim
