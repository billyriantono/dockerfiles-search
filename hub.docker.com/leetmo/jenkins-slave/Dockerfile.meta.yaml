MetaArgs: null
Stages:
- BaseName: jenkinsci/slave
  Commands:
  - Name: user
    User: root
  - CmdLine:
    - apt-get update     && apt-get install -q -y       git build-essential subversion
      libncurses5-dev zlib1g-dev unzip ccache       gawk wget libssl-dev libxml-parser-perl
      file rsync groff-base
    Name: run
    PrependShell: true
  - Name: user
    User: jenkins
  From:
    Image: jenkinsci/slave
  Name: ""
  Platform: ""
  SourceCode: FROM jenkinsci/slave
