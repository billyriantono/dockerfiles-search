MetaArgs: null
Stages:
- BaseName: ruby:alpine
  Commands:
  - Env:
    - Key: HTTP_PORT
      Value: "1080"
    Name: env
  - Env:
    - Key: SMTP_PORT
      Value: "1025"
    Name: env
  - Name: expose
    Ports:
    - $HTTP_PORT
    - $SMTP_PORT
  - CmdLine:
    - apk add --no-cache g++ make musl-dev sqlite-libs sqlite-dev libstdc++ &&     gem
      install --no-document mailcatcher &&     apk del --purge g++ make musl-dev sqlite-dev
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - run.sh
    - /run.sh
  - CmdLine:
    - chmod +x /run.sh
    Name: run
    PrependShell: true
  - CmdLine:
    - /run.sh
    Name: cmd
    PrependShell: false
  From:
    Image: ruby:alpine
  Name: ""
  Platform: ""
  SourceCode: FROM ruby:alpine
