MetaArgs: null
Stages:
- BaseName: circleci/node:8
  Commands:
  - CmdLine:
    - sudo apt-get update && sudo apt-get install -y apt-transport-https && sudo apt-get
      clean && sudo rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | sudo apt-key add -
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "deb https://dl.yarnpkg.com/debian/ stable main" | sudo tee /etc/apt/sources.list.d/yarn.list
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo apt-get update && sudo apt-get install -y yarn && sudo apt-get clean &&
      sudo rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo rm /usr/local/bin/yarn
    Name: run
    PrependShell: true
  From:
    Image: circleci/node:8
  Name: ""
  Platform: ""
  SourceCode: FROM circleci/node:8
