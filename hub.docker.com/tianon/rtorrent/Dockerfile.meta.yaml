MetaArgs: null
Stages:
- BaseName: debian:wheezy
  Commands:
  - CmdLine:
    - useradd --create-home user
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -yq rtorrent
    Name: run
    PrependShell: true
  - CmdLine:
    - "mkdir -p /torrents/session \t&& touch /torrents/.rtorrent.rc \t&& chown -R
      user:user /torrents"
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /torrents
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - rtorrent.rc
    - /home/user/.rtorrent.rc
  - CmdLine:
    - chown -R user:user /home/user
    Name: run
    PrependShell: true
  - Name: user
    User: user
  - CmdLine:
    - rtorrent
    Name: cmd
    PrependShell: false
  From:
    Image: debian:wheezy
  Name: ""
  Platform: ""
  SourceCode: FROM debian:wheezy
