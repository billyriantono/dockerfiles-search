MetaArgs: null
Stages:
- BaseName: php:7.0.16-fpm
  Commands:
  - Maintainer: alex-saf
    Name: maintainer
  - Env:
    - Key: LANG
      Value: en_US.UTF-8
    - Key: LANGUAGE
      Value: en_US:en
    - Key: LC_ALL
      Value: en_US.UTF-8
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - CmdLine:
    - apt-get update     && apt-get -y install             libicu52             libicu-dev             git             zlib1g-dev         --no-install-recommends     &&
      docker-php-ext-install -j$(nproc) intl     && docker-php-ext-install -j$(nproc)
      opcache     && docker-php-ext-install bcmath     && docker-php-ext-install mysqli
      && docker-php-ext-install pdo_mysql     && pecl install apcu-5.1.8 && docker-php-ext-enable
      apcu     && docker-php-ext-install -j$(nproc) zip     && apt-get purge -y         icu-devtools         libicu-dev         zlib1g-dev     &&
      apt-get autoremove -y     && rm -r /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - install-composer
    - /install-composer
  - CmdLine:
    - /install-composer && rm /install-composer && useradd -u 1000 -s /bin/bash -d
      /home/icons8 icons8
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - framework.tar.gz
    - /home/icons8
  - Chown: ""
    Name: add
    SourcesAndDest:
    - requirements.tar.gz
    - /home/icons8
  - Name: workdir
    Path: /home/icons8
  From:
    Image: php:7.0.16-fpm
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.0.16-fpm
