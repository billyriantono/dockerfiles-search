MetaArgs: null
Stages:
- BaseName: debian:jessie
  Commands:
  - Maintainer: '"Gary Smith" <docker@kc.gs>'
    Name: maintainer
  - CmdLine:
    - "apt-get update && apt-get install -y \t\tcurl \t\tgit         wget\t\t\tsqlite3\t
      \       file         unzip         dos2unix         tree         vim         htop
      \        tmux         libsqlite3-dev         libnotify-bin \t&& mkdir -p /app_temp/app
      \t&& mkdir -p /app_temp/logs \t&& chown -R www-data /app_temp \t&& chmod -R
      1777 /app_temp"
    Name: run
    PrependShell: true
  From:
    Image: debian:jessie
  Name: ""
  Platform: ""
  SourceCode: FROM debian:jessie
