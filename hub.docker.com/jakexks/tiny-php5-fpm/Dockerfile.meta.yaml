MetaArgs: null
Stages:
- BaseName: scratch
  Commands:
  - Maintainer: Jake Sanders <jake@livewyer.com>
    Name: maintainer
  - Chown: ""
    Name: add
    SourcesAndDest:
    - rootfs.tar.gz
    - /
  - Chown: ""
    Name: add
    SourcesAndDest:
    - php-fpm.conf
    - /etc/php-fpm.conf
  - CmdLine:
    - addgroup nobody && mkdir /srv && mkdir /srv/website && echo "<h1>It works!</h1>"
      > /srv/website/index.html && echo "<?php phpinfo();" > /srv/website/index.php
      && chmod -R 0666 /srv/website
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sS https://getcomposer.org/installer | php -- --install-dir=/bin --filename=composer
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "9000"
  - CmdLine:
    - php-fpm
    - -F
    - -R
    Name: cmd
    PrependShell: false
  From:
    Scratch: true
  Name: ""
  Platform: ""
  SourceCode: FROM scratch
