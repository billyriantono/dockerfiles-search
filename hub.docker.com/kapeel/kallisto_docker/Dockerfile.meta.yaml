MetaArgs: null
Stages:
- BaseName: ubuntu:14.04.3
  Commands:
  - Maintainer: Kapeel Chougule
    Name: maintainer
  - Labels:
    - Key: Description
      Value: '"This image is used for running Kallisto RNA seq qauntification tool
        "'
    Name: label
  - CmdLine:
    - apt-get update && apt-get install -y build-essential cmake zlib1g-dev libhdf5-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - git clone https://github.com/pachterlab/kallisto.git && cd kallisto && mkdir
      build && cd build && cmake .. && make && make install
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:14.04.3
  Name: ""
  Platform: ""
  SourceCode: FROM      ubuntu:14.04.3
