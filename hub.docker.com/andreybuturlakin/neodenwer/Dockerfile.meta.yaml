MetaArgs: null
Stages:
- BaseName: archlinux/base
  Commands:
  - CmdLine:
    - 'pacman --noconfirm -Syu && mkdir -p /var/cache/pacman/pkg && pacman --noconfirm
      -Syu screen git wget yajl redis postgresql nginx htop freetype2 harfbuzz neovim
      composer php-fpm php php-gd php-imap php-intl php-pgsql php-snmp php-sqlite
      php-xsl php-geoip && /bin/mkdir -p /srv/webserver && chown -R http:http /srv/webserver
      && /bin/mkdir -p /srv/adminer && chown -R http:http /srv/adminer && /bin/mkdir
      -p /srv/redisweb && chown -R http:http /srv/redisweb && /bin/mkdir -p /srv/welcome
      && chown -R http:http /srv/welcome && mkdir -p /run/postgresql && mkdir -p /var/lib/postgres/data
      && mkdir -p /var/log/postgres && chown -R postgres:postgres /run/postgresql
      && chown -R postgres:postgres /var/lib/postgres && chown -R postgres:postgres
      /var/log/postgres && su - postgres -c "initdb --locale en_US.UTF-8 -E UTF8 -D
      ''/var/lib/postgres/data'' --auth-local=trust --auth-host=trust" && chown -R
      postgres:postgres /run/postgresql && chown -R postgres:postgres /var/lib/postgres
      && chown -R postgres:postgres /var/log/postgres && mkdir -p /opt/arangodb &&
      groupadd arangodb && useradd -g arangodb arangodb && chown -R arangodb:arangodb
      /opt/arangodb && echo "dev ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers && groupadd
      dev && useradd -b /home/dev -d /home/dev -m -g dev -G dev dev && echo "dev:1234567890"
      | chpasswd && echo "root:1234567890" | chpasswd'
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - src/arangodb
    - /opt/arangodb
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - src/welcome
    - /srv/welcome
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - src/redis/redis.conf
    - /etc
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - src/redis/web
    - /srv/redisweb
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - src/adminer
    - /srv/adminer
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - src/nginx/nginx.conf
    - /etc/nginx
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - src/php/php.ini
    - /etc/php
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - src/php/www.conf
    - /etc/php/php-fpm.d
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - src/start
    - /usr/sbin
  - CmdLine:
    - /usr/sbin/start
    Name: cmd
    PrependShell: true
  From:
    Image: archlinux/base
  Name: ""
  Platform: ""
  SourceCode: FROM archlinux/base
