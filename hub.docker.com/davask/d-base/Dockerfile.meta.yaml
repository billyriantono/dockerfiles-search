MetaArgs: null
Stages:
- BaseName: davask/d-debian:8.x
  Commands:
  - Maintainer: davask <docker@davaskweblimited.com>
    Name: maintainer
  - Name: user
    User: root
  - Labels:
    - Key: dwl.server.base
      Value: '"base d8.x"'
    Name: label
  - CmdLine:
    - apt-get update && apt-get install -y acl apt-utils binutils build-essential
      cron curl expect git perl htop unzip
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get upgrade -y && apt-get autoremove -y && apt-get clean && rm -rf /var/lib/apt/lists/*
      /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - ./build/dwl/init.sh
    - /dwl/
  - CmdLine:
    - chmod +x /dwl/init.sh && chown root:sudo -R /dwl
    Name: run
    PrependShell: true
  - Name: user
    User: admin
  From:
    Image: davask/d-debian:8.x
  Name: ""
  Platform: ""
  SourceCode: FROM davask/d-debian:8.x
