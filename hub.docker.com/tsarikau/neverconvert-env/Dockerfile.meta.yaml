MetaArgs: null
Stages:
- BaseName: golang:1.4-cross
  Commands:
  - CmdLine:
    - apt-get update
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get -y install libmagic-dev ghostscript imagemagick pkg-config yasm
    Name: run
    PrependShell: true
  - CmdLine:
    - wget https://github.com/FFmpeg/FFmpeg/archive/n2.6.3.tar.gz -O - | tar -xzv
      -C /root
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /root/FFmpeg-n2.6.3     && ./configure  --enable-shared     && make     &&
      make install
    Name: run
    PrependShell: true
  - CmdLine:
    - ldconfig
    Name: run
    PrependShell: true
  - CmdLine:
    - go get -v github.com/jteeuwen/go-bindata github.com/tools/godep
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - :8080
  From:
    Image: golang:1.4-cross
  Name: ""
  Platform: ""
  SourceCode: FROM golang:1.4-cross
