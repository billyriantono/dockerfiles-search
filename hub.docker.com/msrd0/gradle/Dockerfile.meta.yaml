MetaArgs: null
Stages:
- BaseName: gradle:jdk8
  Commands:
  - Name: user
    User: root
  - CmdLine:
    - 'apt-get -y update  && apt-get -y install --no-install-recommends sudo  && apt-get
      -y clean  && rm -rf /var/lib/apt/lists/*  && echo ''gradle ALL=(ALL) NOPASSWD:
      ALL'' >/etc/sudoers.d/gradle'
    Name: run
    PrependShell: true
  - Name: user
    User: gradle
  From:
    Image: gradle:jdk8
  Name: ""
  Platform: ""
  SourceCode: FROM gradle:jdk8
