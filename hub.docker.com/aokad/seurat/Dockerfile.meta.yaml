MetaArgs: null
Stages:
- BaseName: r-base:3.5.2
  Commands:
  - Maintainer: aokada <aokada@ncc.go.jp>
    Name: maintainer
  - CmdLine:
    - apt-get -y update &&     apt-get install -y libssl-dev libcurl4-openssl-dev
      libhdf5-dev &&        Rscript -e "install.packages('devtools')" &&     Rscript
      -e "devtools::install_github(repo = 'satijalab/seurat', ref = 'release/3.0')"
      &&     Rscript -e "install.packages('dplyr')"
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash
    Name: cmd
    PrependShell: false
  From:
    Image: r-base:3.5.2
  Name: ""
  Platform: ""
  SourceCode: FROM r-base:3.5.2
