MetaArgs: null
Stages:
- BaseName: ubuntu
  Commands:
  - Maintainer: liye04@baidu.com
    Name: maintainer
  - CmdLine:
    - apt-get update && apt-get install -y --no-install-recommends     ca-certificates     curl     wget     php5     php5-cli     subversion     git     &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sL https://deb.nodesource.com/setup_0.12 | sudo bash -
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y --no-install-recommends     nodejs     &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - npm i -g     fis     yo     generator-surf     generator-mis     grunt     grunt-cli     bower     &&
      npm cache clear
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "alias bower='bower --allow-root'" >> /root/.bashrc
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu
