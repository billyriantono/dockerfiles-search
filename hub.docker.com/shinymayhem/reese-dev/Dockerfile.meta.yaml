MetaArgs: null
Stages:
- BaseName: shinymayhem/dev
  Commands:
  - Chown: ""
    Name: add
    SourcesAndDest:
    - run.sh
    - /opt/deploy/run.sh
  - Chown: ""
    Name: add
    SourcesAndDest:
    - init-home.sh
    - /opt/deploy/init-home.sh
  - CmdLine:
    - "apt-get update && \tapt-get install -y screen && \trm -rf /var/lib/apt/lists/*
      && \tchmod +x /opt/deploy/*.sh && \tuseradd -m reese -s /bin/bash && \tusermod
      -a -G admin reese"
    Name: run
    PrependShell: true
  - Name: user
    User: reese
  - CmdLine:
    - "cd /home/reese && \tgit clone http://github.com/shinymayhem/home && \tcp -R
      home/. . && \techo 'so ~/.syncedvimrc' > .vimrc && \tvim -E -u NONE -S ~/.vimrc
      +qall"
    Name: run
    PrependShell: true
  - Name: user
    User: root
  From:
    Image: shinymayhem/dev
  Name: ""
  Platform: ""
  SourceCode: FROM shinymayhem/dev
