MetaArgs: null
Stages:
- BaseName: debian:stretch-slim
  Commands:
  - CmdLine:
    - apt-get update        && apt-get install -y cron  ca-certificates unzip curl
      man-db mysql-client libatomic1 libglib2.0-dev  --no-install-recommends        &&
      apt-get clean        && rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -LO https://github.com/maxbube/mydumper/releases/download/v0.9.5/mydumper_0.9.5-2.stretch_amd64.deb
      && dpkg -i mydumper_0.9.5-2.stretch_amd64.deb && rm mydumper_0.9.5-2.stretch_amd64.deb
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - docker/crons/
    - /etc/cron.d/
  - CmdLine:
    - chmod -Rf 0644 /etc/cron.d/
    Name: run
    PrependShell: true
  - CmdLine:
    - touch /var/log/cron.log
    Name: run
    PrependShell: true
  - CmdLine:
    - docker/scripts/tools.sh
    Name: entrypoint
    PrependShell: true
  From:
    Image: debian:stretch-slim
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch-slim
