MetaArgs: null
Stages:
- BaseName: openjdk:8u171-jre-slim-stretch
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - bin/
    - /usr/bin/
  - CmdLine:
    - /usr/bin/entrypoint
    Name: entrypoint
    PrependShell: false
  - Health:
      Interval: 10000000000
      Test:
      - CMD
      - /usr/bin/healthcheck
      Timeout: 1000000000
    Name: healthcheck
  - Name: shell
    Shell:
    - /bin/bash
    - -euxc
  - Name: workdir
    Path: /srv
  - CmdLine:
    - apt-install ca-certificates curl gettext-base jq  && rm -r /usr/bin/*gettext*
      /usr/share/java/libintl.jar  && adduser --disabled-login --disabled-password
      --group --no-create-home --quiet --system srv  && mkdir -p /usr/local/etc  &&
      chown srv /srv /usr/local/etc  && /usr/bin/install-tini  && (find / -perm /6000
      -type f -exec chmod a-s {} \; 2>/dev/null || :)
    Name: run
    PrependShell: true
  From:
    Image: openjdk:8u171-jre-slim-stretch
  Name: ""
  Platform: ""
  SourceCode: FROM openjdk:8u171-jre-slim-stretch
