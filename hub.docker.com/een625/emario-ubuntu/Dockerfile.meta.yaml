MetaArgs: null
Stages:
- BaseName: ubuntu
  Commands:
  - Maintainer: Edward Mario (edward.mario@gmail.com)
    Name: maintainer
  - CmdLine:
    - apt-get -y update && apt-get install -y openssh-server && apt-get install -y
      iputils-ping && apt-get install -y sudo && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - useradd -m emario -s /bin/bash && usermod -aG sudo emario
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir -p /home/emario/.ssh
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - authorized_keys
    - /home/emario/.ssh/authorized_keys
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - sshd_config
    - /etc/ssh/sshd_config
  - CmdLine:
    - chown emario:emario /home/emario/.ssh/authorized_keys && chmod 400 /home/emario/.ssh/authorized_keys
    Name: run
    PrependShell: true
  - CmdLine:
    - /etc/init.d/ssh start && tail -f /dev/null
    Name: entrypoint
    PrependShell: true
  From:
    Image: ubuntu
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu
