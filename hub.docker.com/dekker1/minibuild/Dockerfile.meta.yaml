MetaArgs: null
Stages:
- BaseName: centos/devtoolset-7-toolchain-centos7
  Commands:
  - Name: user
    User: "0"
  - CmdLine:
    - yum install -y --setopt=tsflags=nodocs epel-release && yum install -y  --setopt=tsflags=nodocs     bison     ccache     cmake3     flex     git     make     ninja-build     subversion     zlib-devel
      && yum clean all -y && rm -rf /var/cache/yum
    Name: run
    PrependShell: true
  - CmdLine:
    - ln -s /usr/bin/cmake3 /usr/bin/cmake
    Name: run
    PrependShell: true
  From:
    Image: centos/devtoolset-7-toolchain-centos7
  Name: ""
  Platform: ""
  SourceCode: FROM centos/devtoolset-7-toolchain-centos7
