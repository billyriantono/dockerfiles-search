MetaArgs: null
Stages:
- BaseName: debian
  Commands:
  - CmdLine:
    - apt-get update && apt-get install --no-install-recommends -y   git   libasound2-dev   curl   automake   make   nano   g++   ca-certificates    &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - git clone https://github.com/wb2osz/direwolf.git
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /direwolf
  - CmdLine:
    - make
    Name: run
    PrependShell: true
  - CmdLine:
    - make install
    Name: run
    PrependShell: true
  - CmdLine:
    - make install-conf
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /direwolf
  - Name: workdir
    Path: /direwolf
  - CmdLine:
    - direwolf
    Name: cmd
    PrependShell: true
  From:
    Image: debian
  Name: ""
  Platform: ""
  SourceCode: FROM debian
