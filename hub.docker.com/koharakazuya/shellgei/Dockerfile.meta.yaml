MetaArgs: null
Stages:
- BaseName: buildpack-deps:stretch-curl
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y --no-install-recommends     bc     git     less     locales     man-db     manpages     manpages-ja     nkf     openssh-client     rs     vim  &&
      locale-gen ja_JP.UTF-8  && localedef -f UTF-8 -i ja_JP ja_JP  && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Env:
    - Key: LANG
      Value: ja_JP.UTF-8
    Name: env
  - Env:
    - Key: LANGUAGE
      Value: ja_JP:jp
    Name: env
  - Env:
    - Key: LC_ALL
      Value: ja_JP.UTF-8
    Name: env
  - CmdLine:
    - /bin/bash
    Name: cmd
    PrependShell: false
  From:
    Image: buildpack-deps:stretch-curl
  Name: ""
  Platform: ""
  SourceCode: FROM buildpack-deps:stretch-curl
