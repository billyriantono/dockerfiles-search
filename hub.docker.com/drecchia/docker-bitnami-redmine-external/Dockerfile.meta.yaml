MetaArgs: null
Stages:
- BaseName: ubuntu
  Commands:
  - Maintainer: Danilo Trani Recchia <danilo@deltatecnologia.com>
    Name: maintainer
  - CmdLine:
    - apt-get update &&   apt-get clean && rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
      &&   useradd -m mysql &&   useradd -m subversion &&   mkdir -p /opt/redmine
      &&   ln -sf /opt/redmine /opt/redmine-2.4.2-0
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - files/runme.sh
    - /runme.sh
  - CmdLine:
    - chmod +x /runme.sh
    Name: run
    PrependShell: true
  - CmdLine:
    - /runme.sh
    Name: cmd
    PrependShell: true
  From:
    Image: ubuntu
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu
