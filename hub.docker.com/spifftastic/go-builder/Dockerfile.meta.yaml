MetaArgs: null
Stages:
- BaseName: golang:1.9.0-stretch
  Commands:
  - Maintainer: Noel Cower <ncower@gmail.com>
    Name: maintainer
  - CmdLine:
    - echo 'deb http://httpredir.debian.org/debian stretch-backports main' > /etc/apt/sources.list.d/stretch-backports.list
      &&         apt-get update && apt-get install -y --no-install-recommends -t stretch-backports
      libc6 git openssl ca-certificates openssh-client jq m4
    Name: run
    PrependShell: true
  - Expression: RUN RUN apt-get update && apt-get install -y --no-install-recommends
      -t stretch-backports libc6 git openssl ca-certificates openssh-client jq m4
    Name: onbuild
  From:
    Image: golang:1.9.0-stretch
  Name: ""
  Platform: ""
  SourceCode: FROM golang:1.9.0-stretch
