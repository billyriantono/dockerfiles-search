MetaArgs: null
Stages:
- BaseName: debian:jessie
  Commands:
  - Maintainer: Tim Robinson <tim@panubo.com>
    Name: maintainer
  - CmdLine:
    - useradd -s /bin/bash -U --uid 48 -c 'Toolbox User' user
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update &&   apt-get install -y git tar bzip2 unzip zip lftp curl wget
      mariadb-client sudo pv vim nano
    Name: run
    PrependShell: true
  - CmdLine:
    - "echo \"user\tALL=(ALL:ALL) NOPASSWD:ALL\" > /etc/sudoers.d/user &&   chmod
      600 /etc/sudoers.d/user"
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get -y update &&   apt-get -y install curl wget vim &&   curl -L  https://github.com/coreos/etcd/releases/download/v2.0.11/etcd-v2.0.11-linux-amd64.tar.gz
      -o etcd-v2.0.11-linux-amd64.tar.gz &&   tar xzvf etcd-v2.0.11-linux-amd64.tar.gz
      &&   cp -a etcd-v2.0.11-linux-amd64/etcd etcd-v2.0.11-linux-amd64/etcdctl /usr/local/bin
    Name: run
    PrependShell: true
  - Name: user
    User: user
  From:
    Image: debian:jessie
  Name: ""
  Platform: ""
  SourceCode: FROM debian:jessie
