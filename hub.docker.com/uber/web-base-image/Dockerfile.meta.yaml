MetaArgs: null
Stages:
- BaseName: node:12.13.0
  Commands:
  - CmdLine:
    - yarn global add yarn@1.19.1
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get -y install jq libxi-dev libgl1-mesa-dev xvfb
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - xvfb
    - /etc/init.d/xvfb
  - CmdLine:
    - chmod a+x /etc/init.d/xvfb
    Name: run
    PrependShell: true
  - Env:
    - Key: DISPLAY
      Value: :99
    Name: env
  - CmdLine:
    - apt-get update && apt-get install -y wget --no-install-recommends   && wget
      -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | apt-key add
      -   && sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/
      stable main" >> /etc/apt/sources.list.d/google.list'   && apt-get update   &&
      apt-get install -y google-chrome-unstable   --no-install-recommends   && rm
      -rf /var/lib/apt/lists/*   && rm -rf /src/*.deb   && npx node-gyp@4 install
    Name: run
    PrependShell: true
  From:
    Image: node:12.13.0
  Name: ""
  Platform: ""
  SourceCode: FROM node:12.13.0
