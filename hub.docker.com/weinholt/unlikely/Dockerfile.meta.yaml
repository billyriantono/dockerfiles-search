MetaArgs: null
Stages:
- As: build
  BaseName: debian:buster-slim
  Commands:
  - CmdLine:
    - apt-get update && apt-get -y --no-install-recommends install       build-essential
      ca-certificates git  && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /build/picrin
  - CmdLine:
    - git init  && git remote add origin https://github.com/marijnh/uscheme.git  &&
      git fetch origin 7fce0b6e7875337da9a6a2300d8074f01e4b68d2 --depth 1 --tags  &&
      git reset --hard FETCH_HEAD
    Name: run
    PrependShell: true
  - CmdLine:
    - make
    Name: run
    PrependShell: true
  - CmdLine:
    - make install
    Name: run
    PrependShell: true
  From:
    Image: debian:buster-slim
  Name: build
  Platform: ""
  SourceCode: FROM debian:buster-slim AS build
- BaseName: debian:buster-slim
  Commands:
  - Chown: ""
    From: build
    Name: copy
    SourcesAndDest:
    - /usr/local/
    - /usr/local/
  - CmdLine:
    - ln -s uscheme /usr/local/bin/scheme-banner
    Name: run
    PrependShell: true
  - CmdLine:
    - scheme-banner
    Name: cmd
    PrependShell: false
  From:
    Image: debian:buster-slim
  Name: ""
  Platform: ""
  SourceCode: FROM debian:buster-slim
