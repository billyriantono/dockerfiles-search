MetaArgs: null
Stages:
- BaseName: fedora:30
  Commands:
  - Labels:
    - Key: version
      Value: '"2.0"'
    Name: label
  - Labels:
    - Key: name
      Value: '"base"'
    Name: label
  - CmdLine:
    - dnf --setopt=install_weak_deps=False --best -y  install      http://rpms.remirepo.net/fedora/remi-release-30.rpm     dnf-plugins-core
      &&   dnf config-manager --set-enabled remi &&   rpm --import /etc/pki/rpm-gpg/RPM-GPG-KEY-remi-30
      &&   dnf --setopt=install_weak_deps=False --best -y update &&   dnf --setopt=install_weak_deps=False
      --best -y install      git     iputils     mcrypt     mhash     openssl     p7zip     patch     unzip     which     zip     bind-utils     mariadb     procps     rsync     lsof
      &&   dnf clean all
    Name: run
    PrependShell: true
  - Env:
    - Key: USER
      Value: webuser
    Name: env
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - start.sh
    - /usr/local/bin/start.sh
  - CmdLine:
    - chmod +x /usr/local/bin/start.sh
    Name: run
    PrependShell: true
  - CmdLine:
    - /usr/local/bin/start.sh
    Name: entrypoint
    PrependShell: false
  From:
    Image: fedora:30
  Name: ""
  Platform: ""
  SourceCode: FROM fedora:30
