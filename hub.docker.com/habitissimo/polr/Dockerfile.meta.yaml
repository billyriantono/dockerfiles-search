MetaArgs: null
Stages:
- BaseName: library/php:7.1.1-apache
  Commands:
  - Maintainer: Joan Font <jfont@habitissimo.com>
    Name: maintainer
  - CmdLine:
    - "apt-get update && \tapt-get install -y git zlib1g-dev && \tapt-get clean &&
      \    rm -rf /tmp/* &&     rm -rf /var/tmp/* &&     rm -rf /var/lib/apt/lists/*"
    Name: run
    PrependShell: true
  - CmdLine:
    - "docker-php-ext-install pdo \tpdo_mysql \ttokenizer \tzip"
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin
      --filename=composer
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - .
    - /var/www/html
  - CmdLine:
    - composer install --no-dev -o
    Name: run
    PrependShell: true
  - CmdLine:
    - chmod -R 755 . && chown -R www-data .
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - site.conf
    - /etc/apache2/sites-available/000-default.conf
  - CmdLine:
    - a2enmod rewrite
    Name: run
    PrependShell: true
  - CmdLine:
    - touch .env
    Name: run
    PrependShell: true
  From:
    Image: library/php:7.1.1-apache
  Name: ""
  Platform: ""
  SourceCode: FROM library/php:7.1.1-apache
