MetaArgs: null
Stages:
- BaseName: archlinux
  Commands:
  - CmdLine:
    - pacman -Sy --noconfirm openssh
    Name: run
    PrependShell: true
  - CmdLine:
    - passwd -l root
    Name: run
    PrependShell: true
  - CmdLine:
    - useradd -ms /bin/bash ssh-user
    Name: run
    PrependShell: true
  - CmdLine:
    - ssh-keygen -t dsa -f /etc/ssh/ssh_host_dsa_key
    Name: run
    PrependShell: true
  - CmdLine:
    - ssh-keygen -t rsa -f /etc/ssh/ssh_host_rsa_key
    Name: run
    PrependShell: true
  - CmdLine:
    - sed -i 's/#ListenAddress 0.0.0.0/ListenAddress 0.0.0.0/g' /etc/ssh/sshd_config
    Name: run
    PrependShell: true
  - CmdLine:
    - passwd -l ssh-user
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "22"
  - CmdLine:
    - /usr/bin/sshd
    - -D
    - -e
    - -h
    - /etc/ssh/ssh_host_rsa_key
    - -E
    - /var/log/sshd.log
    Name: cmd
    PrependShell: false
  From:
    Image: archlinux
  Name: ""
  Platform: ""
  SourceCode: FROM archlinux
