MetaArgs: null
Stages:
- BaseName: ubuntu:16.04
  Commands:
  - Maintainer: Marcela Rodriguez <marcela@cantina.co>
    Name: maintainer
  - CmdLine:
    - apt-key update && apt-get update && apt-get install -y curl build-essential
      libssl-dev nano man git wget python ntp libkrb5-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - curl https://raw.githubusercontent.com/creationix/nvm/v0.33.11/install.sh |
      bash
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash
    - -c
    - source /root/.nvm/nvm.sh && nvm install v8.12.0 && nvm alias default v8.12.0
      && nvm use default && npm install -g grunt-cli forever json
    Name: run
    PrependShell: false
  From:
    Image: ubuntu:16.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:16.04
