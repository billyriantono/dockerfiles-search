MetaArgs: null
Stages:
- BaseName: ubuntu:xenial
  Commands:
  - Maintainer: Mathias Åsberg mathias.asberg@gmail.com
    Name: maintainer
  - Key: DEBIAN_FRONTEND
    Name: arg
    Value: noninteractive
  - CmdLine:
    - apt-get clean all && apt-get update && apt-get -y upgrade && apt-get install
      -y     apache2     apache2-utils     curl     php7.0     libapache2-mod-php7.0     php7.0-cli     php7.0-common     php7.0-curl     php7.0-gd     php7.0-xml     php7.0-mysql     vim     unzip
      wget git && apt-get clean && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - service apache2 restart
    Name: run
    PrependShell: true
  - CmdLine:
    - rm -fr /etc/apache2/sites-available/apache_deb.conf &&     rm -fr /var/www/*;
      mkdir -p /var/www/html &&     rm -fr /tmp/install.log &&     chown -R www-data:www-data
      /var/www/html &&     ls -lah /var/www
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /var/www/html
  - CmdLine:
    - chown -R www-data:www-data /var/www
    Name: run
    PrependShell: true
  - Env:
    - Key: TERM
      Value: xterm
    Name: env
  - CmdLine:
    - /bin/bash -c "service apache2 stop && /usr/sbin/apache2ctl -D FOREGROUND"
    Name: cmd
    PrependShell: true
  From:
    Image: ubuntu:xenial
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:xenial
