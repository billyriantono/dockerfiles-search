MetaArgs: null
Stages:
- BaseName: wordpress
  Commands:
  - CmdLine:
    - docker-php-ext-install mbstring zip
    Name: run
    PrependShell: true
  - CmdLine:
    - "touch /usr/local/etc/php/conf.d/uploads.ini \t&& echo \"post_max_size = 120M;\"
      >> /usr/local/etc/php/conf.d/uploads.ini \t&& echo \"upload_max_filesize = 120M;\"
      >> /usr/local/etc/php/conf.d/uploads.ini"
    Name: run
    PrependShell: true
  From:
    Image: wordpress
  Name: ""
  Platform: ""
  SourceCode: FROM wordpress
