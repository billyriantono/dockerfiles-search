MetaArgs: null
Stages:
- BaseName: php:5.6-fpm
  Commands:
  - CmdLine:
    - set -ex;     apt-get update && apt-get install -y         git         unzip         curl         subversion
    Name: run
    PrependShell: true
  - CmdLine:
    - pecl install xdebug-2.5.5 && docker-php-ext-enable xdebug
    Name: run
    PrependShell: true
  - CmdLine:
    - docker-php-ext-install mysqli
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sSL https://phar.phpunit.de/phpunit-5.7.phar -o /usr/local/bin/phpunit;     chmod
      +x /usr/local/bin/phpunit
    Name: run
    PrependShell: true
  - CmdLine:
    - version=$(curl -s https://api.github.com/repos/wp-cli/wp-cli/tags | egrep -o
      "v[0-9].[0-9]{1,}.[0-9]" | sort | uniq -d | tail -n1 | sed 's/^v//g');     curl
      -sSL https://github.com/wp-cli/wp-cli/releases/download/v$version/wp-cli-$version.phar
      -o /usr/local/bin/wp;     chmod +x /usr/local/bin/wp
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sSL https://getcomposer.org/composer.phar -o /usr/local/bin/composer;     chmod
      +x /usr/local/bin/composer
    Name: run
    PrependShell: true
  From:
    Image: php:5.6-fpm
  Name: ""
  Platform: ""
  SourceCode: FROM php:5.6-fpm
