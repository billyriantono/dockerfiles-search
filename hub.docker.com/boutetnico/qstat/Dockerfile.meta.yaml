MetaArgs: null
Stages:
- BaseName: debian:stretch-slim
  Commands:
  - CmdLine:
    - set -x     && apt-get update     && apt-get install --no-install-recommends
      --no-install-suggests -y git ca-certificates automake gcc libc-dev make     &&
      git clone https://github.com/multiplay/qstat.git     && cd qstat     && ./autogen.sh     &&
      ./configure     && make     && make install     && apt-get remove --purge --auto-remove
      -y git ca-certificates automake gcc libc-dev make     && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - qstat
    Name: cmd
    PrependShell: false
  From:
    Image: debian:stretch-slim
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch-slim
