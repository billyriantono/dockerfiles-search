MetaArgs:
- DefaultValue: cpu
  Key: hardware
  ProvidedValue: null
  Value: cpu
Stages:
- BaseName: turkunlp/turku-neural-parser:latest-base-${hardware}
  Commands:
  - Key: models
    Name: arg
    Value: '"fi_tdt en_ewt sv_talbanken"'
  - Name: shell
    Shell:
    - /bin/bash
    - -c
  - Name: workdir
    Path: /app
  - CmdLine:
    - 'echo "MODELS: $models" && for m in ${models} ; do echo "DOWNLOADING $m" ; python3
      fetch_models.py $m ; done && find /app -name "models_*" -type d -exec chmod
      a+rwx -R {} \;'
    Name: run
    PrependShell: true
  From:
    Image: turkunlp/turku-neural-parser:latest-base-${hardware}
  Name: ""
  Platform: ""
  SourceCode: FROM turkunlp/turku-neural-parser:latest-base-${hardware}
