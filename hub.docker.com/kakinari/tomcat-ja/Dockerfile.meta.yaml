MetaArgs: null
Stages:
- BaseName: kakinari/openjdk-ja:jre-1.8.0
  Commands:
  - Env:
    - Key: container
      Value: docker
    Name: env
  - Env:
    - Key: CATALINA_VERSION
      Value: 8.5.39
    Name: env
  - Env:
    - Key: CATALINA_HOME
      Value: /opt/apache-tomcat-${CATALINA_VERSION}
    Name: env
  - Env:
    - Key: CATALINA_BASE
      Value: /opt/tomcat
    Name: env
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - log4j.properties
    - /opt/
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - tomcat.service
    - /etc/systemd/system/
  - CmdLine:
    - yum -y install unzip;yum -y upgrade;yum -y clean all; rm -rf /var/cache/yum/*;groupadd
      -g 1000 apache;useradd -u 1000 -g 1000 tomcat;(cd /opt ;curl -O http://archive.apache.org/dist/tomcat/tomcat-8/v${CATALINA_VERSION}/bin/apache-tomcat-${CATALINA_VERSION}.zip;unzip
      $CATALINA_HOME.zip);mkdir $CATALINA_BASE;(cd $CATALINA_BASE; mkdir bin conf
      logs webapps lib work temp;touch bin/setenv.sh;chmod +x bin/setenv.sh;cp $CATALINA_HOME/bin/tomcat-juli.jar
      bin;cp -r $CATALINA_HOME/conf/* conf;cp -r $CATALINA_HOME/lib/* lib);chown -R
      tomcat:apache $CATALINA_HOME;chown -R tomcat:apache $CATALINA_BASE;mv /opt/log4j.properties
      $CATALINA_HOME/lib/;rm -rf $CATALINA_HOME/webapps/*;chmod +x $CATALINA_HOME/bin/*.sh;rm
      -f $CATALINA_HOME.zip;rm -f $CATALINA_HOME/[A-Z]*;rm -f $CATALINA_HOME/bin/*.bat;rm
      -f $CATALINA_HOME/bin/*.tar.gz;ln -s /etc/systemd/system/tomcat.service /etc/systemd/system/multi-user.target.wants/tomcat.service;
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "8080"
  - Name: volume
    Volumes:
    - /sys/fs/cgroup
  - CmdLine:
    - /usr/sbin/init
    Name: cmd
    PrependShell: true
  From:
    Image: kakinari/openjdk-ja:jre-1.8.0
  Name: ""
  Platform: ""
  SourceCode: FROM kakinari/openjdk-ja:jre-1.8.0
