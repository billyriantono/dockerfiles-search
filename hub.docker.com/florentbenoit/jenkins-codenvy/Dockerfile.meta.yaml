MetaArgs: null
Stages:
- BaseName: codenvy/ubuntu_jdk8
  Commands:
  - CmdLine:
    - cd /tmp && git clone https://github.com/jenkinsci/jenkins && cd jenkins && mvn
      clean install -pl war -am -DskipTests && rm -rf $HOME/.m2/repository/org/jenkins-ci/main
      && rm -rf /tmp/jenkins
    Name: run
    PrependShell: true
  From:
    Image: codenvy/ubuntu_jdk8
  Name: ""
  Platform: ""
  SourceCode: FROM codenvy/ubuntu_jdk8
