MetaArgs: null
Stages:
- BaseName: ubuntu
  Commands:
  - CmdLine:
    - apt-get update
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get install -y git build-essential libudns0 libudns-dev libpcre3 libpcre3-dev
      libev4 libev-dev devscripts automake libtool autoconf autotools-dev cdbs debhelper
      dh-autoreconf dpkg-dev gettext  pkg-config fakeroot
    Name: run
    PrependShell: true
  - CmdLine:
    - git clone https://github.com/dlundquist/sniproxy.git
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /sniproxy
  - CmdLine:
    - ./autogen.sh && ./configure && make && make install
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - sniproxy.conf
    - /etc/sniproxy.conf
  - CmdLine:
    - sniproxy -f
    Name: cmd
    PrependShell: true
  From:
    Image: ubuntu
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu
