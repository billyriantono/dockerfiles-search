MetaArgs: null
Stages:
- BaseName: base/archlinux:latest
  Commands:
  - CmdLine:
    - "pacman -Sy &&   yes | pacman -S \t\tgit \t\tgcc     libzip     luarocks5.1
      &&   luarocks-5.1 install lua-libzip &&   luarocks-5.1 install love-release
      2.0.8-1 &&   mkdir ~/bin && \tcd ~/bin &&   curl -L -o butler.zip https://broth.itch.ovh/butler/linux-amd64/LATEST/archive/default
      && \tunzip -j butler.zip &&   chmod +x butler &&   ./butler upgrade && \t./butler
      -V && \tcurl -L https://github.com/tcnksm/ghr/releases/download/v0.12.0/ghr_v0.12.0_linux_386.tar.gz
      | tar -xz --strip-components 1 &&   chmod +x ghr"
    Name: run
    PrependShell: true
  - Env:
    - Key: PATH
      Value: '"$PATH:~/bin"'
    Name: env
  From:
    Image: base/archlinux:latest
  Name: ""
  Platform: ""
  SourceCode: FROM base/archlinux:latest
