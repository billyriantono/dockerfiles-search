MetaArgs: null
Stages:
- BaseName: berkus/docker-android-ci:latest
  Commands:
  - CmdLine:
    - apt-get update &&     apt-get install -y --no-install-recommends pkg-config
      &&     rm -rf /var/lib/apt/lists/* &&     mkdir -p /dist &&     wget -O /dist/rustup-init
      https://static.rust-lang.org/rustup/dist/x86_64-unknown-linux-gnu/rustup-init
      &&     chmod +x /dist/rustup-init &&     /dist/rustup-init -y --default-toolchain
      nightly &&     rm -rf /dist &&     ~/.cargo/bin/rustup self update &&     ~/.cargo/bin/rustup
      update &&     ~/.cargo/bin/rustup toolchain add stable &&     ~/.cargo/bin/rustup
      target add armv7-linux-androideabi aarch64-linux-android wasm32-unknown-unknown
      &&     ~/.cargo/bin/rustup component add clippy llvm-tools-preview rls rust-analysis
      rustfmt
    Name: run
    PrependShell: true
  From:
    Image: berkus/docker-android-ci:latest
  Name: ""
  Platform: ""
  SourceCode: FROM berkus/docker-android-ci:latest
