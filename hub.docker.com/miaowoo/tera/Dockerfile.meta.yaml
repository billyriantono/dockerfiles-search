MetaArgs: null
Stages:
- As: builder
  BaseName: node:10.15.3-alpine
  Commands:
  - CmdLine:
    - set -x &&   apk add --no-cache python tzdata make g++ &&   cp /usr/share/zoneinfo/Asia/Shanghai
      /etc/localtime &&   rm -rf /var/cache/apk/* &&   echo "Asia/Shanghai" > /etc/timezone
      &&   mkdir -p /app &&   wget -O- https://gitlab.com/terafoundation/tera2/-/archive/master/tera-master.tar.gz
      | tar -xzC /app --strip-components=1 &&   cd /app/Source && npm install
    Name: run
    PrependShell: true
  From:
    Image: node:10.15.3-alpine
  Name: builder
  Platform: ""
  SourceCode: FROM node:10.15.3-alpine AS builder
- BaseName: node:10.15.3-alpine
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"admin@yumc.pw(MiaoWoo)"'
    Name: label
  - CmdLine:
    - /sbin/tini
    - --
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - set -x &&   apk add --no-cache tini tzdata &&   cp /usr/share/zoneinfo/Asia/Shanghai
      /etc/localtime &&   rm -rf /var/cache/apk/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: builder
    Name: copy
    SourcesAndDest:
    - /app/Source
    - /app/Source
  - Name: workdir
    Path: /app/Source
  - Env:
    - Key: PORT
      Value: "80"
    Name: env
  - Env:
    - Key: PASSWD
      Value: "123456"
    Name: env
  - Name: expose
    Ports:
    - "80"
  - CmdLine:
    - node set httpport:${PORT} password:${PASSWD} && node run-node.js
    Name: cmd
    PrependShell: true
  From:
    Image: node:10.15.3-alpine
  Name: ""
  Platform: ""
  SourceCode: FROM node:10.15.3-alpine
