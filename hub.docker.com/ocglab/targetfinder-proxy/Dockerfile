# This is proxy implemented with nginx to route requests to appropriate docker hosts so we can have a development
# version of targetFinder running on same server as the main version.

FROM nginx:1.15
COPY nginx.conf /etc/nginx/nginx.conf
