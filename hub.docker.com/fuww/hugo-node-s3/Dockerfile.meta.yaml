MetaArgs: null
Stages:
- BaseName: node:8-alpine
  Commands:
  - CmdLine:
    - apk add --no-cache     hugo py-pip bash libpng-dev lcms2-dev alpine-sdk nasm
      autoconf automake git openssh &&   pip install --upgrade pip &&   pip install
      s3cmd &&   rm -rf ~/.cache
    Name: run
    PrependShell: true
  From:
    Image: node:8-alpine
  Name: ""
  Platform: ""
  SourceCode: FROM node:8-alpine
