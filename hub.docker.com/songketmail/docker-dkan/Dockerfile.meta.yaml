MetaArgs: null
Stages:
- BaseName: php:5.5-apache
  Commands:
  - Maintainer: Harisfazillah Jamel linuxmalaysia <linuxmalaysia@gmail.com>
    Name: maintainer
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - CmdLine:
    - a2enmod rewrite
    Name: run
    PrependShell: true
  - CmdLine:
    - "apt-get update && apt-get install -y libpng12-dev libjpeg-dev libpq-dev fail2ban
      ufw git curl \t&& rm -rf /var/lib/apt/lists/* \t&& docker-php-ext-configure
      gd --with-png-dir=/usr --with-jpeg-dir=/usr \t&& docker-php-ext-install gd mbstring
      pdo pdo_mysql pdo_pgsql"
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /var/www
  - Env:
    - Key: DRUPAL_VERSION
      Value: "7"
    Name: env
  - CmdLine:
    - "rm -rf /var/www/html/*         && rmdir html         && git config --global
      user.name \"Harisfazillah Jamel\"         && git config --global user.email
      \"linuxmalaysia@gmail.com\"         && git clone --branch master https://github.com/nuams/dkan-drops-7.git
      dkan         && mv dkan html         && cd /var/www/html \t&& chown -R www-data:www-data
      sites"
    Name: run
    PrependShell: true
  From:
    Image: php:5.5-apache
  Name: ""
  Platform: ""
  SourceCode: FROM php:5.5-apache
