MetaArgs: null
Stages:
- BaseName: liararepo/static-platform:base
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - default_liara_nginx.conf
    - /etc/nginx/conf.d/liara_nginx.conf
  - Expression: COPY --from=builder /app/dist /usr/share/nginx/html
    Name: onbuild
  - Expression: COPY --from=builder /app/liara_nginx.conf /app/liara_nginx.conf
    Name: onbuild
  - Expression: RUN if cmp /etc/nginx/conf.d/liara_nginx.conf /app/liara_nginx.conf;   then     echo
      'Applying default liara_nginx.conf...';   else     echo 'Applying custom liara_nginx.conf...';     mv
      /app/liara_nginx.conf /etc/nginx/conf.d/liara_nginx.conf; fi
    Name: onbuild
  From:
    Image: liararepo/static-platform:base
  Name: ""
  Platform: ""
  SourceCode: FROM liararepo/static-platform:base
