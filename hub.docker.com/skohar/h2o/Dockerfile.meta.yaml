MetaArgs: null
Stages:
- BaseName: buildpack-deps
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y     cmake     && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - git clone https://github.com/h2o/h2o     && cd h2o     && git submodule update
      --init --recursive     && cmake .     && make install h2o     && cd ..     &&
      rm -rf /h2o
    Name: run
    PrependShell: true
  From:
    Image: buildpack-deps
  Name: ""
  Platform: ""
  SourceCode: FROM buildpack-deps
