MetaArgs: null
Stages:
- BaseName: php:7.1-fpm
  Commands:
  - Key: user_id
    Name: arg
    Value: null
  - Key: group_id
    Name: arg
    Value: null
  - CmdLine:
    - apt-get update &&     apt-get install -y     git     zip     unzip     libmcrypt-dev     libmagickwand-dev     libicu-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - php -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"     &&
      php composer-setup.php --no-ansi --install-dir=/usr/local/bin --filename=composer     &&
      php -r "unlink('composer-setup.php');"
    Name: run
    PrependShell: true
  - Env:
    - Key: COMPOSER_ALLOW_SUPERUSER
      Value: "1"
    Name: env
  - CmdLine:
    - docker-php-ext-install pdo pdo_mysql mbstring mcrypt intl zip
    Name: run
    PrependShell: true
  - CmdLine:
    - id -u "$user_id" &>/dev/null || (useradd -u "$user_id" -g "$group_id" -m dev
      &&   mkdir /home/dev/.composer &&   chown dev /home/dev/.composer &&   mkdir
      -p /home/dev/.ssh &&   chown dev /home/dev/.ssh)
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - php.ini
    - /usr/local/etc/php/php.ini
  - CmdLine:
    - pecl install apcu   && docker-php-ext-enable apcu
    Name: run
    PrependShell: true
  - CmdLine:
    - pecl install imagick   && docker-php-ext-enable imagick
    Name: run
    PrependShell: true
  - CmdLine:
    - docker-php-ext-install opcache
    Name: run
    PrependShell: true
  From:
    Image: php:7.1-fpm
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.1-fpm
