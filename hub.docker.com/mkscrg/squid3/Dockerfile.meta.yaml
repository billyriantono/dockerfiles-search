MetaArgs: null
Stages:
- BaseName: alpine:3.5
  Commands:
  - CmdLine:
    - set -e;   apk add --update build-base perl;   rm -rf /var/cache/apk /etc/apk/cache
    Name: run
    PrependShell: true
  - CmdLine:
    - set -e;   cd ~;   wget http://www.squid-cache.org/Versions/v3/3.5/squid-3.5.25.tar.gz;   tar
      -vxf squid-3.5.25.tar.gz;   cd squid-3.5.25;   ./configure --prefix=/usr/local/squid;   make
      all;   make install;   cd ~;   rm -r squid-3.5.25*
    Name: run
    PrependShell: true
  - CmdLine:
    - adduser -D proxy
    Name: run
    PrependShell: true
  - Name: user
    User: proxy
  - CmdLine:
    - /usr/local/squid/sbin/squid -N -f /var/squid/squid.conf
    Name: cmd
    PrependShell: true
  From:
    Image: alpine:3.5
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.5
