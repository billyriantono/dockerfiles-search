MetaArgs: null
Stages:
- BaseName: debian:stable
  Commands:
  - Maintainer: Anil Madhavapeddy <anil@recoil.org>
    Name: maintainer
  - CmdLine:
    - apt-get -y update &&   DEBIAN_FRONTEND=noninteractive apt-get -y upgrade &&   echo
      'Acquire::Retries "5";' > /etc/apt/apt.conf.d/mirror-retry &&   apt-get -y update
      &&   DEBIAN_FRONTEND=noninteractive apt-get -y upgrade &&   DEBIAN_FRONTEND=noninteractive
      apt-get -y install sudo pkg-config git build-essential m4 software-properties-common
      aspcud unzip rsync curl dialog nano libx11-dev &&   apt-get -y update &&   DEBIAN_FRONTEND=noninteractive
      apt-get -y upgrade &&   DEBIAN_FRONTEND=noninteractive apt-get -y install ocaml
      ocaml-native-compilers camlp4-extra rsync
    Name: run
    PrependShell: true
  From:
    Image: debian:stable
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stable
