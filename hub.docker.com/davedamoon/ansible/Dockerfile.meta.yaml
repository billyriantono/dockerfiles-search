MetaArgs: null
Stages:
- BaseName: ubuntu:16.04
  Commands:
  - CmdLine:
    - "apt-get update && \tapt-get install -y software-properties-common && \tapt-add-repository
      ppa:ansible/ansible && \tapt-get update && \tapt-get install -y ansible=2.2.0.0-1ppa~xenial
      python-pip rsync && \trm -rf /var/lib/apt/lists/*"
    Name: run
    PrependShell: true
  - CmdLine:
    - pip install 'dopy==0.3.5'
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir /ansible
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /ansible
  - CmdLine:
    - ansible
    - --version
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:16.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:16.04
