MetaArgs: null
Stages:
- BaseName: fvigotti/env-fatubuntu
  Commands:
  - CmdLine:
    - curl http://packages.elasticsearch.org/GPG-KEY-elasticsearch | sudo apt-key
      add - &&     echo "deb http://packages.elastic.co/beats/apt stable main" |  sudo
      tee -a /etc/apt/sources.list.d/beats.list &&     sudo apt-get update && sudo
      apt-get -y install filebeat
    Name: run
    PrependShell: true
  - CmdLine:
    - /usr/bin/filebeat
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - -e
    - -c
    - /etc/filebeat/filebeat.yml
    Name: cmd
    PrependShell: false
  From:
    Image: fvigotti/env-fatubuntu
  Name: ""
  Platform: ""
  SourceCode: FROM fvigotti/env-fatubuntu
