MetaArgs: null
Stages:
- BaseName: haskell:8.6.5
  Commands:
  - CmdLine:
    - apt-get update     && apt-get install --yes xz-utils make unzip     && apt-get
      clean
    Name: run
    PrependShell: true
  - CmdLine:
    - stack --resolver lts-14.7 install base     protolude     text     aeson     yaml     unordered-containers     case-insensitive     regex-compat     wai     warp     diagrams     proto-lens     proto-lens-setup     proto-lens-runtime     proto-lens-protoc
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -L https://github.com/protocolbuffers/protobuf/releases/download/v3.10.0/protoc-3.10.0-linux-x86_64.zip
      > protoc.zip && unzip protoc.zip -d /usr/local && cp -rf /root/.local/bin/*
      /usr/local/bin/ && rm -rf protoc.zip
    Name: run
    PrependShell: true
  From:
    Image: haskell:8.6.5
  Name: ""
  Platform: ""
  SourceCode: FROM haskell:8.6.5
