MetaArgs: null
Stages:
- BaseName: debian:stretch-slim
  Commands:
  - Maintainer: Jorge Obiols <jorge.obiols@gmail.com>
    Name: maintainer
  - CmdLine:
    - set -x;         apt-get update         && apt-get install -y --no-install-recommends             dirmngr             unzip             gnupg
    Name: run
    PrependShell: true
  - CmdLine:
    - set -x;         echo 'deb http://apt.postgresql.org/pub/repos/apt/ stretch-pgdg
      main' > etc/apt/sources.list.d/pgdg.list         && export GNUPGHOME="$(mktemp
      -d)"         && repokey='B97B0AFCAA1A47F044F244A07FCC7D46ACCC4CF8'         &&
      gpg --batch --keyserver keyserver.ubuntu.com --recv-keys "${repokey}"         &&
      gpg --batch --armor --export "${repokey}" > /etc/apt/trusted.gpg.d/pgdg.gpg.asc         &&
      gpgconf --kill all         && rm -rf "$GNUPGHOME"         && apt-get update          &&
      apt-get install -y postgresql-client         && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - restore.sh
    - /usr/bin/restore.sh
  - Chown: ""
    Name: add
    SourcesAndDest:
    - extend.sh
    - /usr/bin/extend.sh
  - Chown: ""
    Name: add
    SourcesAndDest:
    - deactivate.sql
    - /
  - Chown: ""
    Name: add
    SourcesAndDest:
    - extend.sql
    - /
  - CmdLine:
    - '"/usr/bin/restore.sh"'
    Name: cmd
    PrependShell: true
  From:
    Image: debian:stretch-slim
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch-slim
