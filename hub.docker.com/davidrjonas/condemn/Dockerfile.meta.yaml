MetaArgs: null
Stages:
- As: build
  BaseName: rust:1-slim-stretch
  Commands:
  - CmdLine:
    - apt-get update  && apt-get install -y build-essential pkg-config libssl-dev
      curl  && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /build
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - Cargo.*
    - ./
  - CmdLine:
    - cargo fetch
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - src/
    - ./src/
  - CmdLine:
    - cargo build --release
    Name: run
    PrependShell: true
  From:
    Image: rust:1-slim-stretch
  Name: build
  Platform: ""
  SourceCode: FROM rust:1-slim-stretch as build
- BaseName: debian:stretch-slim
  Commands:
  - CmdLine:
    - apt-get update  && apt-get install -y openssl ca-certificates  && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: build
    Name: copy
    SourcesAndDest:
    - /build/target/release/condemn
    - /usr/local/bin/
  - CmdLine:
    - condemn
    Name: entrypoint
    PrependShell: false
  From:
    Image: debian:stretch-slim
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch-slim
