MetaArgs: null
Stages:
- BaseName: alpine:3.10.3
  Commands:
  - CmdLine:
    - apk add     curl     openssh-client     python     py-boto     py-dateutil     py-httplib2     py-pip     py-setuptools     py-crypto     py-cryptography     tar
      &&   pip install --upgrade pip jinja2 PyYAML jmespath docker &&   rm -rf /var/cache/apk/*
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /app
  - CmdLine:
    - mkdir /etc/ansible/ /ansible
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "[local]" >> /etc/ansible/hosts &&     echo "localhost" >> /etc/ansible/hosts
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -fsSL https://releases.ansible.com/ansible/ansible-2.9.1.tar.gz -o ansible.tar.gz
      &&   tar -xzf ansible.tar.gz -C ansible --strip-components 1 &&   rm -fr ansible.tar.gz
      /ansible/docs /ansible/examples /ansible/packaging
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir -p /ansible/playbooks
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /ansible/playbooks
  - Env:
    - Key: ANSIBLE_GATHERING
      Value: smart
    Name: env
  - Env:
    - Key: ANSIBLE_HOST_KEY_CHECKING
      Value: "false"
    Name: env
  - Env:
    - Key: ANSIBLE_RETRY_FILES_ENABLED
      Value: "false"
    Name: env
  - Env:
    - Key: ANSIBLE_ROLES_PATH
      Value: /ansible/playbooks/roles
    Name: env
  - Env:
    - Key: ANSIBLE_SSH_PIPELINING
      Value: "True"
    Name: env
  - Env:
    - Key: PATH
      Value: /ansible/bin:$PATH
    Name: env
  - Env:
    - Key: PYTHONPATH
      Value: /ansible/lib
    Name: env
  - CmdLine:
    - ansible-playbook
    Name: entrypoint
    PrependShell: false
  From:
    Image: alpine:3.10.3
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.10.3
