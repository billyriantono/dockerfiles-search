MetaArgs: null
Stages:
- BaseName: node:4.9
  Commands:
  - CmdLine:
    - echo "deb http://archive.ubuntu.com/ubuntu trusty main universe restricted multiverse"
      > /etc/apt/sources.list  && apt-get update  && apt-get install -y --force-yes
      libtool pkg-config build-essential autoconf automake libzmq-dev  && apt-get
      clean  && rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  - CmdLine:
    - npm install -g bitcore@4.1.1
    Name: run
    PrependShell: true
  - CmdLine:
    - ln -s /usr/local/lib/node_modules/bitcore/node_modules/bitcore-node/bin/bitcoind
      /usr/local/bin
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /bitcoin
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - bitcoin.conf
    - .
  - Name: expose
    Ports:
    - "3001"
  - CmdLine:
    - bitcoind
    - --conf=/bitcoin/bitcoin.conf
    - --datadir=/bitcoin
    - -printtoconsole
    Name: cmd
    PrependShell: false
  From:
    Image: node:4.9
  Name: ""
  Platform: ""
  SourceCode: FROM node:4.9
