MetaArgs: null
Stages:
- BaseName: debian:stretch-slim
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"Fabio Rodrigues Ribeiro <farribeiro@gmail.com>"'
    Name: label
  - Env:
    - Key: USER
      Value: ff
    Name: env
  - Env:
    - Key: GUID
      Value: "1000"
    Name: env
  - Env:
    - Key: LANG
      Value: pt_BR.UTF-8
    Name: env
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - startup.sh
    - /home/ff/
  - CmdLine:
    - "apt-get update && \tapt-get install -y --no-install-recommends \t\tlocales
      \t\tca-certificates \t\twget \t\tfirefox-esr \t\tfirefox-esr-l10n-pt-br \t\tlibc6
      \t\tlibdbus-1-3 \t\tlibgcc1 \t\tlibgdk-pixbuf2.0-0 \t\tlibglib2.0-0 \t\tlibgtk2.0-0
      \t\tlibnss3-tools \t\tlibpango-1.0-0 \t\tlibpython2.7-minimal \t\tlibpython2.7-stdlib
      \t\tlibstdc++6 \t\tlibx11-6 \t\tlibxi6 \t\topenssl \t\tprocps \t\tpython-gpg
      \t\tpython-openssl \t\tpython2.7 \t\tsed \t\tsudo \t\txauth \t\tzenity \t&&
      echo \"pt_BR.UTF-8 UTF-8\" > /etc/locale.gen \t&& locale-gen \t&& mkdir -p /src
      \t&& wget https://cloud.gastecnologia.com.br/gas/diagnostico/warsaw_setup_64.deb
      -O /src/GBPCEFwr64.deb \t&& mkdir -p /home/${USER} \t&& chmod 744 /home/ff/startup.sh
      \t&& groupadd -g ${GUID} -r ${USER} \t&& useradd -u ${GUID} -r -g ${USER} -G
      audio,video ${USER} -d /home/${USER} \t&& chown -R ${GUID}:${GUID} /home/${USER}
      \t&& echo 'ff ALL=(ALL) NOPASSWD: ALL' >> /etc/sudoers \t&& echo 'Defaults !requiretty'
      >> /etc/sudoers \t&& echo root:wscef | chpasswd \t&& apt remove --purge -y wget
      \t&& apt autoremove -y \t&& apt clean \t&& rm -rf /var/lib/apt/lists/* /var/cache/apt/*"
    Name: run
    PrependShell: true
  - Name: user
    User: ff
  - Name: volume
    Volumes:
    - '"/home/ff/Downloads"'
  - CmdLine:
    - /home/ff/startup.sh
    Name: cmd
    PrependShell: true
  From:
    Image: debian:stretch-slim
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch-slim
