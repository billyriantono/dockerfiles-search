MetaArgs: null
Stages:
- BaseName: ualibraries/ruby_plus2.3.3
  Commands:
  - CmdLine:
    - apt-get update -qq &&     apt-get install -y libpq-dev                        nodejs                        libreoffice-core                        libreoffice-base-core                        libreoffice-common                        imagemagick                        unzip
      &&     rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir /app
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /app
  - Chown: ""
    Name: add
    SourcesAndDest:
    - .
    - /app
  - CmdLine:
    - 'echo "gem ''tinymce-rails-imageupload'', github: ''PerfectlyNormal/tinymce-rails-imageupload''"
      >> /app/Gemfile'
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /app && bundle install
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /app && rake assets:precompile
    Name: run
    PrependShell: true
  - CmdLine:
    - rake db:migrate && rails server -b 0.0.0.0
    Name: cmd
    PrependShell: true
  - Name: expose
    Ports:
    - "3000"
  From:
    Image: ualibraries/ruby_plus2.3.3
  Name: ""
  Platform: ""
  SourceCode: FROM ualibraries/ruby_plus2.3.3
