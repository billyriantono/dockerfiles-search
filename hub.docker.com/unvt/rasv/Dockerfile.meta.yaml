MetaArgs: null
Stages:
- BaseName: debian:unstable
  Commands:
  - CmdLine:
    - apt-get update && apt-get -y upgrade &&  apt-get install -y curl ca-certificates
      &&  c_rehash &&  apt-get install -y git gnupg &&  curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg
      |    apt-key add - &&  echo "deb https://dl.yarnpkg.com/debian/ stable main"
      |    tee /etc/apt/sources.list.d/yarn.list &&  apt-get update &&  apt-get -y
      install     apt-transport-https     asciinema     automake     bash-completion     build-essential     clang     clang-tidy     cmake     cppcheck     gcc     gdal-bin     iwyu     libboost-program-options-dev     libbz2-dev     libexpat1-dev     libgles2-mesa-dev     libglfw3-dev     libsqlite3-dev     libtool     llvm     nano     nodejs     osmium-tool     pkg-config     ruby     sqlite3     tmux     vim     xvfb     yarn     zip     zlib1g-dev
      &&  curl https://www.npmjs.com/install.sh | sh &&  npm config set unsafe-perm
      true &&  npm install -g npm &&  yarn global add pm2 hjson browserify rollup     @mapbox/mapbox-gl-style-spec
      budo @pushcorn/hocon-parser &&  mkdir -p /tmp/workdir &&  cd /tmp/workdir &&  git
      clone https://github.com/mapbox/tippecanoe &&  cd tippecanoe &&  make &&  make
      install &&  cd .. && rm -rf /tmp/workdir/tippecanoe &&  cd /root &&  git clone
      https://github.com/maputnik/editor &&  cd editor &&  yarn &&  cd /root &&  git
      clone https://github.com/ibesora/vt-optimizer &&  cd vt-optimizer &&  yarn &&  cd
      /root &&  apt-get -y remove     apt-transport-https     automake     build-essential     clang     clang-tidy     cmake     cppcheck     gcc     libbz2-dev     libexpat1-dev     libgles2-mesa-dev     libglfw3-dev     libsqlite3-dev     llvm     zlib1g-dev
      &&  apt-get -y autoremove &&  rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /root
  From:
    Image: debian:unstable
  Name: ""
  Platform: ""
  SourceCode: FROM debian:unstable
