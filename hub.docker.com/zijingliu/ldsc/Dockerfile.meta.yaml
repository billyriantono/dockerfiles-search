MetaArgs: null
Stages:
- BaseName: continuumio/miniconda2:latest
  Commands:
  - CmdLine:
    - conda config --add channels bioconda &&     conda install -y bitarray=0.8 &&     conda
      install -y nose=1.3 &&     conda install -y numpy=1.12 &&     conda install
      -y pandas=0.20 &&     conda install -y pybedtools=0.7 &&     pip install scipy==0.18
    Name: run
    PrependShell: true
  - CmdLine:
    - git clone https://github.com/bulik/ldsc.git
    Name: run
    PrependShell: true
  From:
    Image: continuumio/miniconda2:latest
  Name: ""
  Platform: ""
  SourceCode: FROM continuumio/miniconda2:latest
