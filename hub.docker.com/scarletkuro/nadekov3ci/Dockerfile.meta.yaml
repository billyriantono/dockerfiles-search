MetaArgs: null
Stages:
- BaseName: ubuntu:trusty
  Commands:
  - CmdLine:
    - "apt-get update \t&& apt-get install -y --no-install-recommends \tlibc6 \tlibcurl3
      \tlibgcc1 \tlibgssapi-krb5-2 \tlibicu52 \tliblttng-ust0 \tlibssl1.0.0 \tlibstdc++6
      \tlibunwind8 \tlibuuid1 \tzlib1g \t&& rm -rf /var/lib/apt/lists/* && rm -f /var/cache/apt/*.bin"
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys 3FA7E0328081BFF6A14DA29AA6A19B38D3D831EF
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "deb http://download.mono-project.com/repo/debian wheezy/snapshots/5.0.0.100
      main" > /etc/apt/sources.list.d/mono-xamarin.list   && apt-get update   && apt-get
      install -y mono-devel ca-certificates-mono fsharp mono-vbnc nuget   && rm -rf
      /var/lib/apt/lists/* && apt-get -q clean -y && rm -rf /var/lib/apt/lists/* &&
      rm -f /var/cache/apt/*.bin
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y curl libicu-dev && apt-get -q clean -y
      && rm -rf /var/lib/apt/lists/* && rm -f /var/cache/apt/*.bin
    Name: run
    PrependShell: true
  - CmdLine:
    - which ssh-agent || ( apt-get update -y && apt-get install openssh-client git
      -y && rm -rf /var/lib/apt/lists/* && rm -f /var/cache/apt/*.bin )
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:trusty
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:trusty
