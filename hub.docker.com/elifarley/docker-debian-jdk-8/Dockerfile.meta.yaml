MetaArgs: null
Stages:
- BaseName: debian:jessie-slim
  Commands:
  - Maintainer: Elifarley Cruz <elifarley@gmail.com>
    Name: maintainer
  - Env:
    - Key: BASE_IMAGE
      Value: debian:jessie-slim
    Name: env
  - CmdLine:
    - apt-get update && apt-get install -y --no-install-recommends ca-certificates
      curl &&   curl -fsSL https://raw.githubusercontent.com/elifarley/cross-installer/master/install.sh
      | sh &&   xinstall save-image-info &&   xinstall add jdk-8-nodesktop &&   xinstall
      cleanup &&   xinstall meta remove
    Name: run
    PrependShell: true
  From:
    Image: debian:jessie-slim
  Name: ""
  Platform: ""
  SourceCode: FROM debian:jessie-slim
