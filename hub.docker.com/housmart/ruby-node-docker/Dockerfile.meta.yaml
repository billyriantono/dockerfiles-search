MetaArgs: null
Stages:
- BaseName: ruby:2.4.1
  Commands:
  - CmdLine:
    - apt-get update -qq && apt-get install -y build-essential libpq-dev libreoffice
      unzip
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y curl apt-transport-https wget &&   curl
      -sS https://dl.yarnpkg.com/debian/pubkey.gpg | apt-key add - &&   echo "deb
      https://dl.yarnpkg.com/debian/ stable main" | tee /etc/apt/sources.list.d/yarn.list
      &&   apt-get update && apt-get install -y yarn
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sL https://deb.nodesource.com/setup_8.x | bash - &&   apt-get install
      nodejs
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /tmp
  - CmdLine:
    - wget -O IPAexfont00301.zip https://oscdl.ipa.go.jp/IPAexfont/IPAexfont00301.zip
      &&   unzip IPAexfont00301.zip &&   mkdir /usr/share/fonts/ipa-ex &&   cp IPAexfont00301/*.ttf
      /usr/share/fonts/ipa-ex &&   fc-cache -fv
    Name: run
    PrependShell: true
  From:
    Image: ruby:2.4.1
  Name: ""
  Platform: ""
  SourceCode: FROM ruby:2.4.1
