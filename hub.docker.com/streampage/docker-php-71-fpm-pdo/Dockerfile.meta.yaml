MetaArgs: null
Stages:
- BaseName: php:7.2-fpm
  Commands:
  - Maintainer: tanner.doug@gmail.com
    Name: maintainer
  - CmdLine:
    - apt-get update && apt-get install -y zlib1g-dev     && docker-php-ext-install
      zip
    Name: run
    PrependShell: true
  - CmdLine:
    - docker-php-ext-install pdo pdo_mysql
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y         libmagickwand-dev --no-install-recommends
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get install -y libpng-dev libfreetype6-dev libjpeg62-turbo-dev && docker-php-ext-configure
      gd --with-freetype-dir --with-jpeg-dir --with-png-dir --with-zlib-dir && docker-php-ext-install
      gd
    Name: run
    PrependShell: true
  - CmdLine:
    - pecl install imagick && docker-php-ext-enable imagick
    Name: run
    PrependShell: true
  - CmdLine:
    - docker-php-ext-configure opcache --enable-opcache && docker-php-ext-install
      opcache
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "80"
  From:
    Image: php:7.2-fpm
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.2-fpm
