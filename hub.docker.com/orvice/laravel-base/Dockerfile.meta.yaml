MetaArgs: null
Stages:
- BaseName: orvice/apache-base:71
  Commands:
  - Maintainer: orvice<orvice@orx.me>
    Name: maintainer
  - Env:
    - Key: VERSION
      Value: "1.0"
    Name: env
  - Name: workdir
    Path: /var/www/html
  - CmdLine:
    - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin
      --filename=composer
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update &&   apt-get install -y supervisor git &&   rm -rf /var/lib/apt/lists/*
      &&   sed -i 's/^\(\[supervisord\]\)$/\1\nnodaemon=true/' /etc/supervisor/supervisord.conf
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /etc/supervisor/conf.d
  - CmdLine:
    - mkdir -p /var/log/supervisor && mkdir -p /etc/supervisor
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - supervisord.conf
    - /etc/supervisor/conf.d/supervisord.conf
  - Name: workdir
    Path: /var/www/html
  - Expression: COPY . /var/www/html
    Name: onbuild
  - Expression: RUN cd /var/www/html && composer install --no-scripts
    Name: onbuild
  - Expression: RUN chmod -R 777 /var/www/html/storage
    Name: onbuild
  - Name: expose
    Ports:
    - "80"
  - Chown: ""
    Name: add
    SourcesAndDest:
    - entrypoint.sh
    - /entrypoint.sh
  - CmdLine:
    - /entrypoint.sh
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - supervisord
    - -c
    - /etc/supervisor/supervisord.conf
    Name: cmd
    PrependShell: false
  From:
    Image: orvice/apache-base:71
  Name: ""
  Platform: ""
  SourceCode: FROM orvice/apache-base:71
