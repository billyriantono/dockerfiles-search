MetaArgs: null
Stages:
- BaseName: debian:stretch
  Commands:
  - CmdLine:
    - apt-get update     && apt-get install --no-install-recommends -y         openjdk-8-jdk         openjfx         maven         unzip     &&
      rm -f /var/lib/apt/lists/*_dists_*
    Name: run
    PrependShell: true
  From:
    Image: debian:stretch
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch
