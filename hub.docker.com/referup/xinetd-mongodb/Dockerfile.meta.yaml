MetaArgs: null
Stages:
- BaseName: pataquets/xinetd-mongodb
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - hosts.allow.oficina
    - hosts.allow.vmhosts
    - hosts.allow.google
    - /tmp/
  - CmdLine:
    - 'cat     /tmp/hosts.allow.oficina     /tmp/hosts.allow.vmhosts     /tmp/hosts.allow.google     |
      tee -a /etc/hosts.allow   &&   rm -v     /tmp/hosts.allow.oficina     /tmp/hosts.allow.vmhosts     /tmp/hosts.allow.google   &&   echo
      "ALL: ALL" | tee -a /etc/hosts.deny   &&   nl /etc/hosts.allow &&   nl /etc/hosts.deny'
    Name: run
    PrependShell: true
  From:
    Image: pataquets/xinetd-mongodb
  Name: ""
  Platform: ""
  SourceCode: FROM pataquets/xinetd-mongodb
