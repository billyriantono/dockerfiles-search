MetaArgs: null
Stages:
- BaseName: php:5.6-apache
  Commands:
  - CmdLine:
    - apt-get update     && apt-get install -y --fix-missing git libssl-dev zlib1g-dev
      libicu-dev g++ mcrypt php-pear php5-apcu    libfreetype6-dev     libxslt-dev     libjpeg62-turbo-dev     libmcrypt-dev     imagemagick    libpng12-dev     php5-xsl     libcurl4-openssl-dev     nano     &&
      pecl install mongo     && echo extension=mongo.so > /usr/local/etc/php/conf.d/mongo.ini     &&
      pecl install xdebug     && echo zend_extension=xdebug.so > /usr/local/etc/php/conf.d/xdebug.ini     &&
      docker-php-ext-install zip mbstring intl gd mcrypt pdo pdo_mysql mysqli
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sL https://deb.nodesource.com/setup_6.x | bash -
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get install -y nodejs
    Name: run
    PrependShell: true
  - Env:
    - Key: APACHE_RUN_USER
      Value: www-data
    Name: env
  - Env:
    - Key: APACHE_RUN_GROUP
      Value: staff
    Name: env
  - Env:
    - Key: APACHE_LOG_DIR
      Value: /var/log/apache2
    Name: env
  - Env:
    - Key: ENVIRONMENT
      Value: docker
    Name: env
  - CmdLine:
    - a2enmod ssl rewrite  && export TERM=xterm  && mkdir /root/.ssh/  && docker-php-ext-install
      xsl
    Name: run
    PrependShell: true
  - CmdLine:
    - usermod -u 1000 www-data
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sS https://getcomposer.org/installer | php     && mv composer.phar /usr/bin/composer
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - docker/vhost.conf
    - /etc/apache2/sites-enabled/000-default.conf
  - Chown: ""
    Name: add
    SourcesAndDest:
    - docker/php.ini
    - /usr/local/etc/php/php.ini
  - CmdLine:
    - mkdir -p /var/www/apps/
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /var/www/apps/
  - Name: volume
    Volumes:
    - /var/www/
    - /var/log/apache2/
  From:
    Image: php:5.6-apache
  Name: ""
  Platform: ""
  SourceCode: FROM php:5.6-apache
