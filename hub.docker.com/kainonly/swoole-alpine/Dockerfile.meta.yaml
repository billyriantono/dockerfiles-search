MetaArgs: null
Stages:
- BaseName: php:7.4.0-alpine
  Commands:
  - Name: workdir
    Path: /app
  - CmdLine:
    - apk add --no-cache     libstdc++     libbz2     bzip2     libzip     openssl
    Name: run
    PrependShell: true
  - CmdLine:
    - apk add --no-cache --virtual .build-deps $PHPIZE_DEPS         bzip2-dev     openssl-dev         &&
      docker-php-ext-install         bz2     iconv     sockets     mysqli     pdo_mysql     opcache     &&
      pecl install redis swoole     && docker-php-ext-enable redis swoole     && apk
      del .build-deps
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "swoole.use_shortname='Off'" >> /usr/local/etc/php/conf.d/docker-php-ext-swoole.ini
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /app
  - CmdLine:
    - php
    - -a
    Name: cmd
    PrependShell: false
  From:
    Image: php:7.4.0-alpine
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.4.0-alpine
