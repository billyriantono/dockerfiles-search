MetaArgs: null
Stages:
- BaseName: debian:8.7
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"j.zelger@techdivision.com"'
    Name: label
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - fs
    - /tmp/
  - CmdLine:
    - apt-get update &&     DEBIAN_FRONTEND=noninteractive apt-get install -y --force-yes
      --no-install-recommends         vim tar wget curl apt-transport-https ca-certificates
      net-tools htop python-pip pv &&     cp -r /tmp/etc/apt /etc &&     curl https://www.dotdeb.org/dotdeb.gpg
      | apt-key add - &&     curl -sL https://deb.nodesource.com/setup_6.x | bash
      - &&     apt-get update &&     DEBIAN_FRONTEND=noninteractive apt-get install
      -y --force-yes --no-install-recommends         ssh openssl rsync git graphicsmagick
      nodejs build-essential rubygems ruby-dev ruby-compass         php7.0 php7.0-cli
      php7.0-common php7.0-curl php7.0-gd php7.0-mcrypt php7.0-mysql php7.0-soap         php7.0-json
      php7.0-zip php7.0-intl php7.0-bcmath php7.0-xsl php7.0-xml php7.0-mbstring php7.0-xdebug         php7.0-mongodb
      php7.0-ldap php7.0-imagick php7.0-readline &&     gem update —system &&     gem
      install compass sass &&     npm install -g gulp-cli &&     curl -sS https://getcomposer.org/installer
      | php -- --install-dir=/usr/local/bin --filename=composer &&     composer global
      require hirak/prestissimo &&     mkdir /build &&     apt-get clean &&     rm
      -rf /tmp/*
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /build
  - Name: workdir
    Path: /build
  From:
    Image: debian:8.7
  Name: ""
  Platform: ""
  SourceCode: FROM debian:8.7
