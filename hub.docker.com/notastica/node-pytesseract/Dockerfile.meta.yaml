MetaArgs: null
Stages:
- BaseName: node
  Commands:
  - Maintainer: Rafael Roman "rafael@notastica.org"
    Name: maintainer
  - CmdLine:
    - apt-get update     && apt-get -y upgrade     && apt-get install -y python-dev
      tesseract-ocr tesseract-ocr-fra python-pip     && apt-get clean     && rm -rf
      /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - pip install -U pip &&     pip install pytesseract Pillow
    Name: run
    PrependShell: true
  From:
    Image: node
  Name: ""
  Platform: ""
  SourceCode: FROM node
