MetaArgs: null
Stages:
- BaseName: openjdk:7-jre
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - .
    - /hathi-client
  - CmdLine:
    - apt-get update     && DEBIAN_FRONTEND=noninteractive apt-get install -yq krb5-user     &&
      rm -rf /var/lib/apt/lists/*     && /hathi-client/bin/get.sh hadoop spark     &&
      echo 'eval $(/hathi-client/bin/env.sh)' > /etc/profile.d/10-hathi.sh
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash
    - -l
    Name: entrypoint
    PrependShell: false
  From:
    Image: openjdk:7-jre
  Name: ""
  Platform: ""
  SourceCode: FROM openjdk:7-jre
