MetaArgs: null
Stages:
- BaseName: gitlab/gitlab-runner:alpine
  Commands:
  - CmdLine:
    - 'apk --no-cache add --update sudo docker &&     echo "gitlab-runner ALL=(ALL)
      NOPASSWD: ALL" > /etc/sudoers.d/gitlab-runner'
    Name: run
    PrependShell: true
  From:
    Image: gitlab/gitlab-runner:alpine
  Name: ""
  Platform: ""
  SourceCode: FROM gitlab/gitlab-runner:alpine
