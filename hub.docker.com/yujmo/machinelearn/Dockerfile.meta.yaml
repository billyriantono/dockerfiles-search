MetaArgs: null
Stages:
- BaseName: yujmo/archbase
  Commands:
  - CmdLine:
    - "yaourt -S tk --noconfirm && yaourt -S python-pip --noconfirm && pip install
      numpy \t&& pip install scipy && pip install scikit-learn && pip install matplotlib"
    Name: run
    PrependShell: true
  From:
    Image: yujmo/archbase
  Name: ""
  Platform: ""
  SourceCode: FROM yujmo/archbase
