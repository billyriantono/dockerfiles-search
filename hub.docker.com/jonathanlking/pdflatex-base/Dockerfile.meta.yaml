MetaArgs: null
Stages:
- BaseName: debian:9
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"Jonathan King <jonathanlking@me.com>"'
    Name: label
  - CmdLine:
    - apt-get update     && apt-get install -y     texlive-latex-base     && echo
      "\nmain_memory = 12000000" >> /etc/texmf/texmf.d/00debian.cnf     && echo "\nextra_mem_bot
      = 12000000" >> /etc/texmf/texmf.d/00debian.cnf     && echo "\nfont_mem_size
      = 12000000" >> /etc/texmf/texmf.d/00debian.cnf     && echo "\npool_size = 12000000"
      >> /etc/texmf/texmf.d/00debian.cnf     && echo "\nbuf_size = 12000000" >> /etc/texmf/texmf.d/00debian.cnf     &&
      update-texmf     && texhash     && apt-get clean     && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  From:
    Image: debian:9
  Name: ""
  Platform: ""
  SourceCode: FROM debian:9
