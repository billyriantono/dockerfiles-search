MetaArgs: null
Stages:
- BaseName: pottava/tensorflow:1.8-note
  Commands:
  - Env:
    - Key: KERAS_VERSION
      Value: 2.1.6
    Name: env
  - CmdLine:
    - pip uninstall numpy -y
    Name: run
    PrependShell: true
  - CmdLine:
    - pip install --ignore-installed --upgrade "keras == ${KERAS_VERSION}"     &&
      pip install captcha matplotlib matplotlib pydot     && apk add --no-cache -u
      graphviz     && find / -depth -type d -name tests -exec rm -rf {} \;     &&
      find / -depth -type d -name \__pycache__ -exec rm -rf {} \;     && rm -rf /root/.cache
    Name: run
    PrependShell: true
  From:
    Image: pottava/tensorflow:1.8-note
  Name: ""
  Platform: ""
  SourceCode: FROM pottava/tensorflow:1.8-note
