MetaArgs: null
Stages:
- BaseName: debian:jessie
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y     apt-transport-https     build-essential     ca-certificates     curl     g++     gcc     git     make     sudo     wget     python-all     &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Name: shell
    Shell:
    - /bin/bash
    - -l
    - -c
  - Env:
    - Key: NVM_DIR
      Value: /usr/local/nvm
    Name: env
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - .
    - /src/
  - Name: workdir
    Path: /src
  - CmdLine:
    - curl https://raw.githubusercontent.com/creationix/nvm/master/install.sh | bash
    Name: run
    PrependShell: true
  - CmdLine:
    - "[ -f .nvmrc ] && \tnvm install && nvm use ||   nvm install stable && nvm use
      stable"
    Name: run
    PrependShell: true
  From:
    Image: debian:jessie
  Name: ""
  Platform: ""
  SourceCode: FROM debian:jessie
