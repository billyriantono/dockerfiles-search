MetaArgs: null
Stages:
- BaseName: php:7-fpm-alpine
  Commands:
  - Maintainer: Matt Hartstonge <matt@mykro.co.nz>
    Name: maintainer
  - CmdLine:
    - apk add --no-cache         freetype-dev         libjpeg-turbo-dev         libmcrypt-dev         libpng-dev         postgresql-dev         zip         unzip     &&
      docker-php-ext-configure gd         -with-freetype-dir=/usr/include/          -with-png-dir=/usr/png/         -with-jpeg-dir=/usr/jpeg/     &&
      docker-php-ext-configure pgsql         -with-pgsql=/usr/include/postgresql/     &&
      pecl install zip     && docker-php-ext-enable zip     && docker-php-ext-install         gd         mcrypt         mysqli         pdo         pdo_pgsql         pgsql
    Name: run
    PrependShell: true
  From:
    Image: php:7-fpm-alpine
  Name: ""
  Platform: ""
  SourceCode: FROM php:7-fpm-alpine
