MetaArgs: null
Stages:
- BaseName: node:carbon
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y sudo     && adduser node sudo     && echo
      '%sudo ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers     && apt-get clean && rm -rf
      /var/lib/apt/lists/
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y jq     && apt-get clean && rm -rf /var/lib/apt/lists/
    Name: run
    PrependShell: true
  - Name: user
    User: node
  - CmdLine:
    - mkdir /home/node/.npm-global
    Name: run
    PrependShell: true
  - Env:
    - Key: PATH
      Value: /home/node/.npm-global/bin:$PATH
    Name: env
  - Env:
    - Key: NPM_CONFIG_PREFIX
      Value: /home/node/.npm-global
    Name: env
  - CmdLine:
    - npm install -g firebase-tools
    Name: run
    PrependShell: true
  From:
    Image: node:carbon
  Name: ""
  Platform: ""
  SourceCode: FROM node:carbon
