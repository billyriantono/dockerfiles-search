MetaArgs: null
Stages:
- BaseName: python:3.7-slim
  Commands:
  - CmdLine:
    - apt-get update     &&     apt-get install -y         git         gcc     &&     apt-get
      clean     &&     rm -rf /var/lib/apt/lists/*     &&     python3 -m pip install
      --no-cache-dir         pre-commit
    Name: run
    PrependShell: true
  - CmdLine:
    - pre-commit run --all-files
    Name: cmd
    PrependShell: true
  From:
    Image: python:3.7-slim
  Name: ""
  Platform: ""
  SourceCode: FROM python:3.7-slim
