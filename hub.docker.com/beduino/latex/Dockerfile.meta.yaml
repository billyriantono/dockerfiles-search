MetaArgs: null
Stages:
- BaseName: debian:testing
  Commands:
  - CmdLine:
    - echo ttf-mscorefonts-installer msttcorefonts/accepted-mscorefonts-eula     select
      true | debconf-set-selections
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "deb http://deb.debian.org/debian testing contrib" >> /etc/apt/sources.list
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y   wget   git   make   texlive-full   ttf-mscorefonts-installer
      &&   apt-get --purge remove -y .\*-doc$ &&   apt-get clean -y
    Name: run
    PrependShell: true
  From:
    Image: debian:testing
  Name: ""
  Platform: ""
  SourceCode: FROM debian:testing
