MetaArgs: null
Stages:
- BaseName: suchenglong/ubuntu16.04
  Commands:
  - CmdLine:
    - pip install python_docx     && pip install web.py     && pip install jieba     &&
      pip install numpy     && pip install scipy     && pip install sklearn     &&
      pip install gensim     && pip install apscheduler
    Name: run
    PrependShell: true
  From:
    Image: suchenglong/ubuntu16.04
  Name: ""
  Platform: ""
  SourceCode: FROM suchenglong/ubuntu16.04
