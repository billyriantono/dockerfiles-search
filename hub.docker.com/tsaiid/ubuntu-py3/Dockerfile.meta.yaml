MetaArgs: null
Stages:
- BaseName: ubuntu:16.04
  Commands:
  - Maintainer: I-Ta Tsai <itsai@gmail.com>
    Name: maintainer
  - CmdLine:
    - "apt-get update && apt-get upgrade -y --no-install-recommends && \tapt-get install
      -y --no-install-recommends         tzdata         python3-minimal python3-pip
      python3-setuptools python3-dev &&     apt-get -y autoremove && apt-get clean
      &&     rm -rf /usr/share/doc && rm -rf /usr/share/man &&     rm -rf /var/lib/apt/lists/*"
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:16.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:16.04
