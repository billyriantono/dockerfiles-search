MetaArgs: null
Stages:
- BaseName: continuumio/anaconda3
  Commands:
  - CmdLine:
    - pip install pendulum
    Name: run
    PrependShell: true
  - CmdLine:
    - pip uninstall -y matplotlib &&   python -m pip install --upgrade pip &&   pip
      install matplotlib
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update &&     apt-get install -y --no-install-recommends     fonts-dejavu     tzdata     gfortran     gcc
      && apt-get clean &&     rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - conda install libiconv
    Name: run
    PrependShell: true
  - CmdLine:
    - conda install --quiet --yes -c conda-forge r-rmarkdown r-knitr r-tidyverse r-ggmap
      r-maps r-here
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install make
    Name: run
    PrependShell: true
  From:
    Image: continuumio/anaconda3
  Name: ""
  Platform: ""
  SourceCode: FROM continuumio/anaconda3
