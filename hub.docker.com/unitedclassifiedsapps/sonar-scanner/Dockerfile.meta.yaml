MetaArgs: null
Stages:
- BaseName: anapsix/alpine-java:8_jdk
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"United Classifieds <unitedclassifiedsapps@gmail.com>"'
    Name: label
  - Env:
    - Key: HOME
      Value: '"/root"'
    Name: env
  - Name: workdir
    Path: ${HOME}
  - Env:
    - Key: SONAR_SCANNER_VERSION
      Value: 3.0.3.778
    Name: env
  - Env:
    - Key: SONAR_LINT_VERSION
      Value: 2.1.0.566
    Name: env
  - Env:
    - Key: PATH
      Value: '"$PATH:${HOME}/sonar-scanner/bin:${HOME}/sonarlint-cli/bin:${HOME}/bin"'
    Name: env
  - CmdLine:
    - apk update && apk add --no-cache     bash     unzip     git     && rm -rf /tmp/*
      /var/tmp/*
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - https://sonarsource.bintray.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-${SONAR_SCANNER_VERSION}.zip
    - sonar-scanner.zip
  - CmdLine:
    - unzip sonar-scanner.zip && rm sonar-scanner.zip && mv sonar-scanner-${SONAR_SCANNER_VERSION}
      sonar-scanner
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - scan
    - bin/scan
  - CmdLine:
    - chmod u+x bin/scan
    Name: run
    PrependShell: true
  From:
    Image: anapsix/alpine-java:8_jdk
  Name: ""
  Platform: ""
  SourceCode: FROM anapsix/alpine-java:8_jdk
