MetaArgs: null
Stages:
- BaseName: archlinux/base
  Commands:
  - CmdLine:
    - "pacman -Syy --noconfirm pacman-contrib curl sed --needed && \tcurl -s \"https://www.archlinux.org/mirrorlist/?country=FR&country=GB&use_mirror_status=on\"
      | sed -e 's/^#Server/Server/' -e '/^#/d' | rankmirrors -n 6 - > /etc/pacman.d/mirrorlist
      && \tpacman -Syyu --noconfirm --needed \t\tpandoc \t\ttexlive-core \t\ttexlive-pictures
      \t\ttexlive-bibtexextra \t\ttexlive-pstricks \t\ttexlive-latexextra \t\tca-certificates
      \t\tbiber \t\tmake \t\tgit \t\tgit-lfs \t\tinkscape \t\tlibrsvg && \tpacman
      -Scc --noconfirm && \trm -rf /var/lib/pacman/sync /tmp/* /var/tmp/*"
    Name: run
    PrependShell: true
  - Env:
    - Key: PATH
      Value: '"/usr/bin/vendor_perl/:${PATH}"'
    Name: env
  - CmdLine:
    - /bin/sh
    Name: cmd
    PrependShell: true
  From:
    Image: archlinux/base
  Name: ""
  Platform: ""
  SourceCode: FROM archlinux/base
