MetaArgs: null
Stages:
- BaseName: giabar/gb-c7sysd
  Commands:
  - Maintainer: giabar
    Name: maintainer
  - CmdLine:
    - yum -y install epel-release deltarpm &&    yum -y install wget tar curl zip
      unzip git
    Name: run
    PrependShell: true
  - CmdLine:
    - yum -y install http://repos.mesosphere.io/el/7/noarch/RPMS/mesosphere-el-repo-7-1.noarch.rpm
      &&    yum -y install mesosphere-zookeeper mesos marathon chronos &&    echo
      'docker,mesos' > /etc/mesos-slave/containerizers
    Name: run
    PrependShell: true
  - Env:
    - Key: DOCKER_VERSION
      Value: 1.13.0
    Name: env
  - CmdLine:
    - wget -q https://get.docker.com/builds/Linux/x86_64/docker-${DOCKER_VERSION}.tgz  &&   tar
      xzf docker-${DOCKER_VERSION}.tgz &&   mv docker/* /usr/bin/ &&   chmod +x /usr/bin/docker
      &&   rm -r docker*
    Name: run
    PrependShell: true
  - CmdLine:
    - systemctl enable zookeeper &&    systemctl enable mesos-master &&    systemctl
      enable mesos-slave &&    systemctl enable marathon &&    systemctl enable chronos
    Name: run
    PrependShell: true
  - CmdLine:
    - /usr/sbin/init
    Name: cmd
    PrependShell: false
  From:
    Image: giabar/gb-c7sysd
  Name: ""
  Platform: ""
  SourceCode: FROM giabar/gb-c7sysd
