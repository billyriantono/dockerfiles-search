MetaArgs: null
Stages:
- BaseName: python:2-alpine
  Commands:
  - Name: workdir
    Path: /root
  - CmdLine:
    - apk update && apk add git libffi libffi-dev gcc g++ make openssl-dev openssl
      openssh &&     cd /root/ &&     git clone --depth=1 --recursive https://github.com/byt3bl33d3r/CrackMapExec
      &&     cd /root/CrackMapExec &&     rm -rf .git &&     python setup.py install
      &&     cme --help &&     apk del git libffi-dev gcc g++ make openssl-dev &&     rm
      -rf /var/cache/apk/*
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/sh
    Name: cmd
    PrependShell: false
  From:
    Image: python:2-alpine
  Name: ""
  Platform: ""
  SourceCode: FROM python:2-alpine
