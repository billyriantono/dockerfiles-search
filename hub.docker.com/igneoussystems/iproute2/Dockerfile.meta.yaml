MetaArgs: null
Stages:
- BaseName: debian:wheezy
  Commands:
  - CmdLine:
    - "echo deb http://http.debian.net/debian wheezy-backports main > \t/etc/apt/sources.list.d/wheezy-backports.list"
    Name: run
    PrependShell: true
  - CmdLine:
    - "apt-get update && apt-get -t wheezy-backports install -y iproute2 \t&& rm -rf
      /var/lib/apt/lists/*"
    Name: run
    PrependShell: true
  - CmdLine:
    - "apt-get update && apt-get install -y bridge-utils \t&& rm -rf /var/lib/apt/lists/*"
    Name: run
    PrependShell: true
  From:
    Image: debian:wheezy
  Name: ""
  Platform: ""
  SourceCode: FROM debian:wheezy
