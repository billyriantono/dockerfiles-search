MetaArgs: null
Stages:
- BaseName: ubuntu
  Commands:
  - Maintainer: KingMario <gcyyq@hotmail.com>
    Name: maintainer
  - CmdLine:
    - apt-get update && apt-get install -y python python-pip libjpeg-dev libfreetype6
      python-dev python-setuptools zlib1g-dev curl && apt-get clean && pip install
      pillow && mkdir ~/mapCut && mkdir ~/mapCut/precut && mkdir ~/mapCut/result &&
      curl https://raw.githubusercontent.com/KingMario/Baidu-Map-Indoor-Lite/master/mapCut.py
      -o ~/mapCut/mapCut.py && chmod +x ~/mapCut/mapCut.py
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash
    Name: entrypoint
    PrependShell: true
  From:
    Image: ubuntu
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu
