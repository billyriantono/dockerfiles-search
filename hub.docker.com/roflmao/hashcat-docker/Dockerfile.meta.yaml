MetaArgs: null
Stages:
- BaseName: nvidia/cuda:7.5-runtime
  Commands:
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - Env:
    - Key: HOME
      Value: /root
    Name: env
  - CmdLine:
    - apt-get update && apt-get install -y git nvidia-opencl-icd-352 build-essential
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /home
  - CmdLine:
    - git clone https://github.com/hashcat/hashcat.git
    Name: run
    PrependShell: true
  - CmdLine:
    - cd hashcat && mkdir -p deps/OpenCL-Headers && git clone https://github.com/KhronosGroup/OpenCL-Headers
      deps/OpenCL-Headers/CL
    Name: run
    PrependShell: true
  - CmdLine:
    - cd hashcat && make linux64
    Name: run
    PrependShell: true
  From:
    Image: nvidia/cuda:7.5-runtime
  Name: ""
  Platform: ""
  SourceCode: FROM nvidia/cuda:7.5-runtime
