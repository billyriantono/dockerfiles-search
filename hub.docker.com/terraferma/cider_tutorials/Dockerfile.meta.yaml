MetaArgs: null
Stages:
- BaseName: terraferma/dev:v1.0.1
  Commands:
  - Maintainer: terraferma <terraferma@lists.columbia.edu>
    Name: maintainer
  - Name: user
    User: root
  - Name: workdir
    Path: /tmp
  - CmdLine:
    - apt-get update &&     apt-get -y --with-new-pkgs         -o Dpkg::Options::="--force-confold"
      -o Dpkg::Options::="--force-confdef" upgrade &&     apt-get -y install                        eog
      &&     apt-get clean &&     rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  - Name: user
    User: tfuser
  - Name: workdir
    Path: $TF_HOME
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - bash_aliases
    - $TF_HOME/.bash_aliases
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - WELCOME
    - $TF_HOME/WELCOME
  - CmdLine:
    - export PYTHONPATH=$TF_PREFIX/lib/python2.7/site-packages:$FENICS_PREFIX/lib/python2.7/site-packages
      &&     export PATH=$TF_PREFIX/bin:$FENICS_PREFIX/bin:$PATH &&     export LD_LIBRARY_PATH=$FENICS_PREFIX/lib:$LD_LIBRARY_PATH
      &&     export PETSC_DIR=$PETSC_PREFIX &&     export PKG_CONFIG_PATH=$FENICS_PREFIX/lib/pkgconfig:$PKG_CONFIG_PATH
      &&     export DIAMOND_CONFIG_PATH=$TF_PREFIX/share/buckettools/diamond:$DIAMOND_CONFIG_PATH
      &&     export CMAKE_MODULE_PATH=$TF_PREFIX/share/buckettools/cmake/modules:$CMAKE_MODULE_PATH
      &&     export TF_CMAKE_PATH=$TF_PREFIX/share/terraferma/cpp &&     git clone
      -q https://github.com/TerraFERMA/cider_tutorials.git &&     cd cider_tutorials
      &&     tfsimulationharness --build *.shml
    Name: run
    PrependShell: true
  - Name: user
    User: root
  From:
    Image: terraferma/dev:v1.0.1
  Name: ""
  Platform: ""
  SourceCode: FROM terraferma/dev:v1.0.1
