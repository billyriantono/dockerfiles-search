MetaArgs: null
Stages:
- BaseName: dorapro/php:7.0-alpine
  Commands:
  - CmdLine:
    - set -x     && mkdir -p /composer     && mkdir -p /app     && apk add --update
      --no-cache openssh-client git
    Name: run
    PrependShell: true
  - Env:
    - Key: COMPOSER_HOME
      Value: /composer
    Name: env
  - Env:
    - Key: PATH
      Value: /composer/vendor/bin:$PATH
    Name: env
  - Env:
    - Key: COMPOSER_ALLOW_SUPERUSER
      Value: "1"
    Name: env
  - Env:
    - Key: COMPOSER_VERSION
      Value: 1.2.0
    Name: env
  - CmdLine:
    - curl -fsSL https://getcomposer.org/installer | php -- --version=${COMPOSER_VERSION}
      --install-dir=/usr/local/bin --filename=composer     && composer global require
      hirak/prestissimo     && composer global clear-cache
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /app
  - Name: workdir
    Path: /app
  - CmdLine:
    - --version
    Name: cmd
    PrependShell: false
  - CmdLine:
    - composer
    Name: entrypoint
    PrependShell: false
  From:
    Image: dorapro/php:7.0-alpine
  Name: ""
  Platform: ""
  SourceCode: FROM dorapro/php:7.0-alpine
