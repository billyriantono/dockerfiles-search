MetaArgs: null
Stages:
- BaseName: ros:kinetic
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y -qq curl
    Name: run
    PrependShell: true
  - CmdLine:
    - curl http://repo.ros2.org/repos.key | apt-key add -
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "deb http://repo.ros2.org/ubuntu/main xenial main" > /etc/apt/sources.list.d/ros2-latest.list
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y -qq ros-r2b2-*
    Name: run
    PrependShell: true
  - Env:
    - Key: ROS_DISTRO
      Value: r2b2
    Name: env
  From:
    Image: ros:kinetic
  Name: ""
  Platform: ""
  SourceCode: FROM ros:kinetic
