MetaArgs: null
Stages:
- BaseName: debian:unstable
  Commands:
  - Maintainer: Nathan Douglas <docker@tenesm.us>
    Name: maintainer
  - CmdLine:
    - set -xe &&   apt-get update &&   apt-get -y install ssh git man git-annex
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /data
  - Name: workdir
    Path: /data
  From:
    Image: debian:unstable
  Name: ""
  Platform: ""
  SourceCode: FROM debian:unstable
