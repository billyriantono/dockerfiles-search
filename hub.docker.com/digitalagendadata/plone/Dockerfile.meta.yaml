MetaArgs: null
Stages:
- BaseName: plone:4.3.17
  Commands:
  - CmdLine:
    - apt-get update   && apt-get install -y --no-install-recommends git gcc libffi-dev
      libc6-dev
    Name: run
    PrependShell: true
  - Name: user
    User: plone
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - site.cfg
    - /plone/instance/
  - CmdLine:
    - buildout -c site.cfg
    Name: run
    PrependShell: true
  - Name: user
    User: root
  - CmdLine:
    - apt-get purge -y --auto-remove gcc libffi-dev libc6-dev   && rm -rf /var/lib/apt/lists/*   &&
      rm -rf /plone/buildout-cache/downloads/*
    Name: run
    PrependShell: true
  From:
    Image: plone:4.3.17
  Name: ""
  Platform: ""
  SourceCode: FROM plone:4.3.17
