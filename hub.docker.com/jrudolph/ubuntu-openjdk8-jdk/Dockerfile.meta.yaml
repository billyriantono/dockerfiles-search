MetaArgs: null
Stages:
- BaseName: jrudolph/ubuntu-openjdk8-jre
  Commands:
  - Maintainer: jrudolph
    Name: maintainer
  - CmdLine:
    - apt-get update && apt-get install --no-install-recommends -y openjdk-8-jdk &&
      apt-get clean && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  From:
    Image: jrudolph/ubuntu-openjdk8-jre
  Name: ""
  Platform: ""
  SourceCode: FROM jrudolph/ubuntu-openjdk8-jre
