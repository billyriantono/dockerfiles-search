MetaArgs: null
Stages:
- BaseName: ubuntu:14.04
  Commands:
  - CmdLine:
    - apt-get update -y &&       apt-get install -y software-properties-common &&       add-apt-repository
      -y ppa:pneu/munin &&       apt-get update -y &&       apt-get clean && rm -rf
      /var/cache/apt/archives/* /var/lib/apt/lists/* /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update -y &&       apt-get install -y python munin-node munin-plugins-extra
      telnet mtr wget dnsutils &&       apt-get clean &&       rm -rf /var/cache/apt/archives/*
      /var/lib/apt/lists/* /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  - CmdLine:
    - ln -s /usr/share/munin/plugins/cpu_by_process /etc/munin/plugins/cpu_by_process
      && mkdir -p /var/log/munin/; chown -R munin:munin /var/log/munin/
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - ./autoconfigure.py
    - /usr/bin/munin-node-autoconfigure
  - CmdLine:
    - chmod +x /usr/bin/munin-node-autoconfigure
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - bootstrap.sh
    - /root/bootstrap.sh
  - Name: expose
    Ports:
    - "4949"
  - CmdLine:
    - /root/bootstrap.sh
    Name: cmd
    PrependShell: true
  From:
    Image: ubuntu:14.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:14.04
