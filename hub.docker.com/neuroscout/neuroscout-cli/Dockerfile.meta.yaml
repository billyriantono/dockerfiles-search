MetaArgs: null
Stages:
- BaseName: poldracklab/fitlins:0.6.2
  Commands:
  - Name: user
    User: root
  - Key: DEBIAN_FRONTEND
    Name: arg
    Value: noninteractive
  - CmdLine:
    - apt-get update && apt-get install datalad -yq
    Name: run
    PrependShell: true
  - CmdLine:
    - git config --global user.name "Neuroscout"
    Name: run
    PrependShell: true
  - CmdLine:
    - git config --global  user.email "user@example.edu"
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - .
    - /src/neuroscout
  - CmdLine:
    - /bin/bash -c "source activate neuro       && pip install -q --no-cache-dir -e
      /src/neuroscout/"     && sync
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash -c "source activate neuro       && pip install -q --no-cache-dir --upgrade
      -r /src/neuroscout/requirements.txt"     && sync
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /work
  - CmdLine:
    - /neurodocker/startup.sh
    - neuroscout
    Name: entrypoint
    PrependShell: false
  From:
    Image: poldracklab/fitlins:0.6.2
  Name: ""
  Platform: ""
  SourceCode: FROM poldracklab/fitlins:0.6.2
