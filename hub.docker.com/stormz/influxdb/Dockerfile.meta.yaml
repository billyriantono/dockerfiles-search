MetaArgs: null
Stages:
- BaseName: buildpack-deps:jessie-curl
  Commands:
  - Env:
    - Key: VERSION
      Value: 0.13.0
    Name: env
  - CmdLine:
    - wget https://dl.influxdata.com/influxdb/releases/influxdb_${VERSION}_amd64.deb     &&
      dpkg -i influxdb_${VERSION}_amd64.deb     && rm influxdb_${VERSION}_amd64.deb
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - config.toml
    - /config.toml
  - Name: expose
    Ports:
    - "8083"
  - Name: expose
    Ports:
    - "8086"
  - Name: volume
    Volumes:
    - /data
  - CmdLine:
    - influxd
    - -config
    - /config.toml
    Name: cmd
    PrependShell: false
  From:
    Image: buildpack-deps:jessie-curl
  Name: ""
  Platform: ""
  SourceCode: from buildpack-deps:jessie-curl
