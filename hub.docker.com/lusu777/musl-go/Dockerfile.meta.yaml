MetaArgs: null
Stages:
- BaseName: golang:1.7.3-alpine
  Commands:
  - CmdLine:
    - apk add --update-cache build-base musl-dev musl-utils tar sudo
    Name: run
    PrependShell: true
  - CmdLine:
    - wget http://www.musl-libc.org/releases/musl-1.1.10.tar.gz && tar -xvf musl-1.1.10.tar.gz
      && cd musl-1.1.10 && ./configure && make && sudo make install && cd .. && rm
      musl-1.1.10.tar.gz && rm -Rf musl-1.1.10 && rm -Rf musl.1.1.10 && rm -Rf musl.1.1.10
    Name: run
    PrependShell: true
  - CmdLine:
    - rm -rf /var/cache/apk/*
    Name: run
    PrependShell: true
  From:
    Image: golang:1.7.3-alpine
  Name: ""
  Platform: ""
  SourceCode: FROM golang:1.7.3-alpine
