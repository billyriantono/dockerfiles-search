MetaArgs: null
Stages:
- BaseName: ubuntu
  Commands:
  - Maintainer: Conrad Pöpke "conrad@poepke.info"
    Name: maintainer
  - Labels:
    - Key: Description
      Value: '"Basic Docker container containg a mountebank installation. Used to
        be extended with fixed or dynamic mocks from files."'
    Name: label
  - CmdLine:
    - apt-get update && apt-get install -y npm && ln -s /usr/bin/nodejs /usr/bin/node
      && npm install -g mountebank --production
    Name: run
    PrependShell: true
  From:
    Image: ubuntu
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu
