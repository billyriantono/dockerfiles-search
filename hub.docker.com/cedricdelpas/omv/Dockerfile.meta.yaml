MetaArgs: null
Stages:
- BaseName: debian:8
  Commands:
  - Labels:
    - Key: MAINTAINER
      Value: '"Cédric DELPAS <cedric.delpas@gmail.com>'
    Name: label
  - Name: expose
    Ports:
    - "443"
    - "80"
  - CmdLine:
    - echo "deb http://packages.openmediavault.org/public erasmus main" | tee -a /etc/apt/sources.list.d/openmediavault.list     &&
      echo "deb http://packages.omv-extras.org/debian/ erasmus main" | sudo tee -a
      /etc/apt/sources.list.d/omv-extras-org-kralizec.list     && wget -O - http://packages.openmediavault.org/public/archive.key
      | apt-key add -     && apt-get update     && apt-get install openmediavault-keyring
      postfix -y --force-yes     && apt-get install php-apc openmediavault openmediavault-omvextrasorg
      -y --force-yes
    Name: run
    PrependShell: true
  - CmdLine:
    - omv-initsystem
    Name: cmd
    PrependShell: false
  - CmdLine:
    - nginx
    Name: entrypoint
    PrependShell: false
  From:
    Image: debian:8
  Name: ""
  Platform: ""
  SourceCode: FROM debian:8
