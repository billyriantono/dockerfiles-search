MetaArgs: null
Stages:
- BaseName: ubuntu
  Commands:
  - Maintainer: Jo Vandeginste "jo.vandeginste@gmail.com"
    Name: maintainer
  - CmdLine:
    - "apt-get update \t\t\t&& apt-get install -y git-core curl libmysqlclient-dev
      build-essential openssl libreadline6 libreadline6-dev curl zlib1g zlib1g-dev
      libssl-dev libyaml-dev libsqlite3-dev sqlite3 libxml2-dev libxslt-dev autoconf
      libc6-dev ncurses-dev automake libtool bison pkg-config \t\t\t&& update-locale
      LANG=C.UTF-8 LC_MESSAGES=POSIX \t\t\t&& rm -rf /var/lib/apt/lists/* # 20140918"
    Name: run
    PrependShell: true
  - CmdLine:
    - "gpg --keyserver hkp://keys.gnupg.net --recv-keys 409B6B1796C275462A1703113804BB82D39DC0E3
      \t&& curl -L https://get.rvm.io | bash -s stable \t&& /bin/bash -l -c \"rvm
      requirements\" \t&& /bin/bash -l -c \"rvm install 2.1\" \t&& /bin/bash -l -c
      \"gem install bundler --no-ri --no-rdoc\""
    Name: run
    PrependShell: true
  From:
    Image: ubuntu
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu
