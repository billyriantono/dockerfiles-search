MetaArgs: null
Stages:
- BaseName: eywalker/python:cuda
  Commands:
  - Maintainer: Edgar Y. Walker <edgar.walker@gmail.com>
    Name: maintainer
  - CmdLine:
    - apt-get update     && apt-get install -y build-essential     && apt-get install
      -y gfortran     && apt-get install -y libblas-dev     && apt-get install -y
      liblapack-dev     && apt-get install -y pkg-config     && apt-get install -y
      libfreetype6-dev     && apt-get install -y cython     && rm -rf /var/lib/apt/lists/*
      /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - requirements.txt
    - /requirements.txt
  - CmdLine:
    - pip install -r /requirements.txt     && rm /requirements.txt
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - requirements_post.txt
    - /requirements_post.txt
  - CmdLine:
    - pip install -r /requirements_post.txt     && rm /requirements_post.txt
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/sh
    - -c
    - ipython
    Name: cmd
    PrependShell: false
  From:
    Image: eywalker/python:cuda
  Name: ""
  Platform: ""
  SourceCode: FROM eywalker/python:cuda
