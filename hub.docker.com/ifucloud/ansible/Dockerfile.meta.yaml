MetaArgs: null
Stages:
- BaseName: ubuntu:16.04
  Commands:
  - CmdLine:
    - apt clean -y
    Name: run
    PrependShell: true
  - CmdLine:
    - apt update &&     apt install -y ansible &&     mkdir -p /root/.ssh
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /etc/ansible
    - /root/.ssh
  - Name: workdir
    Path: /etc/ansible
  From:
    Image: ubuntu:16.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:16.04
