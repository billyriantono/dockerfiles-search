MetaArgs:
- DefaultValue: '"10.1-cudnn7-devel-ubuntu16.04"'
  Key: NVIDIA
  ProvidedValue: null
  Value: '"10.1-cudnn7-devel-ubuntu16.04"'
Stages:
- BaseName: nvidia/cuda:${NVIDIA}
  Commands:
  - Key: PYTHON_VERSION
    Name: arg
    Value: "3.7"
  - CmdLine:
    - apt-get update && apt-get install -y --no-install-recommends          build-essential          cmake          git          curl          vim          ca-certificates          libjpeg-dev          libpng-dev
      &&     rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -o ~/miniconda.sh -O  https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh  &&      chmod
      +x ~/miniconda.sh &&      ~/miniconda.sh -b -p /opt/conda &&      rm ~/miniconda.sh
      &&      /opt/conda/bin/conda install -y python=$PYTHON_VERSION numpy pyyaml
      scipy ipython mkl mkl-include cython typing &&      /opt/conda/bin/conda install
      -y -c pytorch magma-cuda101 &&      /opt/conda/bin/conda install -y pytorch
      torchvision cudatoolkit=10.1 -c pytorch &&      /opt/conda/bin/conda clean -ya
    Name: run
    PrependShell: true
  - Env:
    - Key: PATH
      Value: /opt/conda/bin:$PATH
    Name: env
  - CmdLine:
    - pip --no-cache-dir install ninja
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /opt && git clone https://github.com/pytorch/vision.git && cd vision && pip
      install -v .
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /workspace
  - CmdLine:
    - chmod -R a+w /workspace
    Name: run
    PrependShell: true
  From:
    Image: nvidia/cuda:${NVIDIA}
  Name: ""
  Platform: ""
  SourceCode: FROM nvidia/cuda:${NVIDIA}
