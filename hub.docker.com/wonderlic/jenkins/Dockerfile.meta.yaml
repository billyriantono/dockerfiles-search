MetaArgs: null
Stages:
- BaseName: jenkins/jenkins:lts
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"Wonderlic DevOps <DevOps@wonderlic.com>"'
    Name: label
  - Name: user
    User: root
  - CmdLine:
    - "apt-get update && \tapt-get install -y --no-install-recommends zstd g++ build-essential
      libsecret-1-0 && \trm -rf /var/lib/apt/lists/*"
    Name: run
    PrependShell: true
  - Name: user
    User: jenkins
  From:
    Image: jenkins/jenkins:lts
  Name: ""
  Platform: ""
  SourceCode: FROM jenkins/jenkins:lts
