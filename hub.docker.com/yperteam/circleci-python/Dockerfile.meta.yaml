MetaArgs: null
Stages:
- BaseName: circleci/python:3.6.4
  Commands:
  - CmdLine:
    - sudo apt-get update       && sudo apt-get install locales-all -y
    Name: run
    PrependShell: true
  - CmdLine:
    - wget -qO - https://www.mongodb.org/static/pgp/server-3.4.asc | sudo apt-key
      add -       && echo "deb http://repo.mongodb.org/apt/debian jessie/mongodb-org/3.4
      main" | sudo tee /etc/apt/sources.list.d/mongodb-org-3.4.list       && sudo
      apt-get update && sudo apt-get install mongodb-org-tools -y
    Name: run
    PrependShell: true
  From:
    Image: circleci/python:3.6.4
  Name: ""
  Platform: ""
  SourceCode: FROM  circleci/python:3.6.4
