MetaArgs: null
Stages:
- BaseName: alpine:3.8
  Commands:
  - CmdLine:
    - set -x &&     apk update && apk upgrade &&     apk add --update bash netcat-openbsd
      iputils curl vim
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - https://storage.googleapis.com/kubernetes-release/release/v1.11.2/bin/linux/amd64/kubectl
    - /usr/local/bin/kubectl
  - CmdLine:
    - set -x &&     apk add --no-cache curl ca-certificates &&     chmod +x /usr/local/bin/kubectl
      &&     mkdir -p ~/.kube
    Name: run
    PrependShell: true
  - CmdLine:
    - '"while true; do date; kubectl get namespaces; sleep 10; done"'
    Name: cmd
    PrependShell: true
  From:
    Image: alpine:3.8
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.8
