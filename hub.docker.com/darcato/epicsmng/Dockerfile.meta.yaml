MetaArgs: null
Stages:
- BaseName: ubuntu:latest
  Commands:
  - Env:
    - Key: HOME
      Value: '"${HOME:-/root}"'
    Name: env
  - Env:
    - Key: PATH
      Value: '"${HOME:-/root}/.local/bin:${PATH}"'
    Name: env
  - CmdLine:
    - apt-get update     && apt-get -y install         git         build-essential         libreadline-dev         wget         libc-dev-bin         re2c
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - .
    - /epicsmng
  - CmdLine:
    - sh /epicsmng/install.sh     && rm -rf /epicsmng     && echo "" >> $HOME/.config/epicsmng/settings/user.settings     &&
      echo "base_url=\"https://github.com/epics-base/epics-base\"" >> $HOME/.config/epicsmng/settings/user.settings
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:latest
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:latest
