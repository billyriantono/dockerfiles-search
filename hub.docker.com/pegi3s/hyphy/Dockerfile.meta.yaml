MetaArgs: null
Stages:
- BaseName: ubuntu:18.04
  Commands:
  - CmdLine:
    - "apt-get update && apt-get install -y software-properties-common \t              make
      libcrypto++-dev libssl-dev openmpi-bin libopenmpi-dev                       cmake
      build-essential unzip wget"
    Name: run
    PrependShell: true
  - CmdLine:
    - wget https://github.com/veg/hyphy/archive/2.3.14.zip -O /tmp/hyphy-2.3.14.zip
      &&     unzip /tmp/hyphy-2.3.14.zip -d /opt/ &&     rm /tmp/hyphy-2.3.14.zip
      -f
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /opt/hyphy-2.3.14/ &&     cmake . && make HYPHYMP && make install
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:18.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:18.04
