MetaArgs: null
Stages:
- BaseName: ubuntu:18.04
  Commands:
  - CmdLine:
    - "apt-get -qq update && \tapt-get install -y --no-install-recommends wget \tbsdmainutils
      libgomp1 make openmpi-bin ssh \tautomake g++ libboost-dev libopenmpi-dev libsparsehash-dev"
    Name: run
    PrependShell: true
  - CmdLine:
    - "wget --no-check-certificate https://github.com/bcgsc/abyss/releases/download/2.1.0/abyss-2.1.0.tar.gz
      -O /tmp/abyss.tar.gz && \ttar zxvf /tmp/abyss.tar.gz -C /opt/ && rm /tmp/abyss.tar.gz
      && \tcd /opt/abyss-2.1.0 && \t./autogen.sh && \tmkdir build && cd build && \t../configure
      --with-mpi=/usr/lib/openmpi && \tmake install-strip && \trm -rf /tmp/abyss"
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get autoremove -y binutils automake g++ libboost-dev libopenmpi-dev libsparsehash-dev
      wget
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:18.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:18.04
