MetaArgs: null
Stages:
- BaseName: debian:jessie-backports
  Commands:
  - CmdLine:
    - apt-get update   && DEBIAN_FRONTEND=noninteractive apt-get install -t jessie-backports
      -qy socat ca-certificates   && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - hosts.allow
    - /etc/hosts.allow
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - hosts.deny
    - /etc/hosts.deny
  - CmdLine:
    - /usr/bin/socat
    Name: entrypoint
    PrependShell: false
  From:
    Image: debian:jessie-backports
  Name: ""
  Platform: ""
  SourceCode: FROM debian:jessie-backports
