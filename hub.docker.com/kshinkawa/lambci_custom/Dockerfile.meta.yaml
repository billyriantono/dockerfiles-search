MetaArgs: null
Stages:
- BaseName: lambci/lambda-base:build
  Commands:
  - Env:
    - Key: PATH
      Value: /var/lang/bin:$PATH
    - Key: LD_LIBRARY_PATH
      Value: /var/lang/lib:$LD_LIBRARY_PATH
    - Key: AWS_EXECUTION_ENV
      Value: AWS_Lambda_python3.7
    - Key: PYTHONPATH
      Value: /var/runtime
    - Key: PKG_CONFIG_PATH
      Value: /var/lang/lib/pkgconfig:/usr/lib64/pkgconfig:/usr/share/pkgconfig
    Name: env
  - CmdLine:
    - rm -rf /var/runtime /var/lang /var/rapid &&   curl https://lambci.s3.amazonaws.com/fs/python3.7.tgz
      | tar -zx -C /
    Name: run
    PrependShell: true
  - CmdLine:
    - pip install -U pip setuptools --no-cache-dir &&   pip install -U virtualenv
      pipenv --no-cache-dir &&   pip install -U awscli boto3 aws-sam-cli==0.31.0 aws-lambda-builders==0.5.0
      --no-cache-dir
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sL https://rpm.nodesource.com/setup_10.x | bash -
    Name: run
    PrependShell: true
  - CmdLine:
    - yum clean all && yum makecache fast
    Name: run
    PrependShell: true
  - CmdLine:
    - yum install -y gcc-c++ make
    Name: run
    PrependShell: true
  - CmdLine:
    - yum install -y nodejs
    Name: run
    PrependShell: true
  - CmdLine:
    - npm install -g serverless
    Name: run
    PrependShell: true
  From:
    Image: lambci/lambda-base:build
  Name: ""
  Platform: ""
  SourceCode: FROM lambci/lambda-base:build
