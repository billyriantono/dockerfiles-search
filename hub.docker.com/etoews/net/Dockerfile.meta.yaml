MetaArgs: null
Stages:
- BaseName: alpine
  Commands:
  - CmdLine:
    - "apk update && apk add \tcurl   jq   nmap \tca-certificates \tpython \tpy-pip
      \t&& rm -rf /var/cache/apk/* \t&& pip install httpie httpie-unixsocket"
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/sh
    Name: cmd
    PrependShell: false
  From:
    Image: alpine
  Name: ""
  Platform: ""
  SourceCode: FROM alpine
