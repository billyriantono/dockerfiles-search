MetaArgs: null
Stages:
- BaseName: node:7
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"Massimo Schiavon <massimo@themostaza.com>"'
    Name: label
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - CmdLine:
    - apt-get update -qq &&     apt-get install -qy curl apt-transport-https
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | apt-key add - &&     echo
      "deb https://dl.yarnpkg.com/debian/ stable main" | tee /etc/apt/sources.list.d/yarn.list
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update -qq &&     apt-get install -qy yarn libelf1 jq rsync
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /workspace
  From:
    Image: node:7
  Name: ""
  Platform: ""
  SourceCode: FROM node:7
