MetaArgs: null
Stages:
- BaseName: alpine
  Commands:
  - CmdLine:
    - "apk update \t&& apk add gcc g++ libuv make python git && rm -rf /var/cache/apk/*"
    Name: run
    PrependShell: true
  - CmdLine:
    - "cd /tmp && curl https://bootstrap.pypa.io/ez_setup.py -o - | python \t&& git
      clone https://chromium.googlesource.com/external/gyp.git \t&& cd gyp  \t&& python
      setup.py install  \t&& cd ..  \t&& rm -rf gyp"
    Name: run
    PrependShell: true
  - CmdLine:
    - "cd /tmp  \t&& rm -rf apk  \t&& apk del git"
    Name: run
    PrependShell: true
  From:
    Image: alpine
  Name: ""
  Platform: ""
  SourceCode: FROM alpine
