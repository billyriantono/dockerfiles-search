MetaArgs: null
Stages:
- BaseName: php:apache
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y libxslt-dev && docker-php-ext-install xsl
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y default-mysql-client && docker-php-ext-install
      mysqli
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y         libfreetype6-dev         libjpeg62-turbo-dev         libpng-dev     &&
      docker-php-ext-configure gd --with-freetype --with-jpeg     && docker-php-ext-install
      -j$(nproc) gd
    Name: run
    PrependShell: true
  - CmdLine:
    - a2enmod rewrite
    Name: run
    PrependShell: true
  From:
    Image: php:apache
  Name: ""
  Platform: ""
  SourceCode: FROM php:apache
