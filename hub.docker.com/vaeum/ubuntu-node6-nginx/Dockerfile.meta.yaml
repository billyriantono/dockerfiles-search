MetaArgs: null
Stages:
- BaseName: ubuntu:16.04
  Commands:
  - CmdLine:
    - apt-get update -qq &&   apt-get install wget git nginx -yqq &&   wget -cq https://nodejs.org/dist/v6.10.3/node-v6.10.3-linux-x64.tar.gz
      &&     tar -zxf node-v6.10.3-linux-x64.tar.gz &&   rm -rf /node-v6.10.3-linux-x64.tar.gz
      &&     mv node-v6.10.3-linux-x64 /usr/local/src/node &&   cd /usr/local/src/node
      &&   cp -R share/* /usr/local/share &&   ln -s /usr/local/src/node/bin/node
      /usr/local/bin/node &&   ln -s /usr/local/src/node/bin/npm /usr/local/bin/npm
      &&     rm -rf /usr/local/src/node/share &&   rm -rf /usr/local/src/node/include
      &&   rm -rf /usr/local/src/node/CHANGELOG.md &&   rm -rf /usr/local/src/node/LICENSE
      &&   rm -rf /usr/local/src/node/README.md &&     apt-get clean &&   apt-get
      autoclean &&   rm -rf /var/cache/* &&   rm -rf /tmp/* &&   rm -rf /var/tmp/*
      &&   rm -rf /var/lib/apt/lists/* &&     npm config set depth 0 &&     cd /
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:16.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:16.04
