MetaArgs: null
Stages:
- BaseName: fedora:30
  Commands:
  - CmdLine:
    - yum update -y && yum install unzip ncurses-compat-libs qemu-kvm qemu-img qemu-system-x86
      wget -y && yum clean all
    Name: run
    PrependShell: true
  - Env:
    - Key: PACKER_VERSION
      Value: 1.4.3
    - Key: TERRAFORM_VERSION
      Value: 0.12.6
    - Key: OVFTOOL_VERSION
      Value: 4.3.0-12320924
    Name: env
  - CmdLine:
    - wget -nv -P /root https://releases.hashicorp.com/packer/${PACKER_VERSION}/packer_${PACKER_VERSION}_linux_amd64.zip
      &&     unzip /root/packer_${PACKER_VERSION}_linux_amd64.zip -d /usr/local/bin
      &&     rm -vf /root/packer_${PACKER_VERSION}_linux_amd64.zip &&     wget -nv
      -P /root https://releases.hashicorp.com/terraform/${TERRAFORM_VERSION}/terraform_${TERRAFORM_VERSION}_linux_amd64.zip
      &&     unzip /root/terraform_${TERRAFORM_VERSION}_linux_amd64.zip -d /usr/local/bin
      &&     rm -vf /root/terraform_${TERRAFORM_VERSION}_linux_amd64.zip
    Name: run
    PrependShell: true
  From:
    Image: fedora:30
  Name: ""
  Platform: ""
  SourceCode: FROM fedora:30
