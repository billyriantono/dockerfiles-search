MetaArgs: null
Stages:
- BaseName: node:alpine
  Commands:
  - CmdLine:
    - apk update && apk add --update alpine-sdk python git && npm install -g @angular/cli
      && npm config set unsafe-perm=true
    Name: run
    PrependShell: true
  - Env:
    - Key: PATH
      Value: '"/usr/local/lib/node_modules/@angular/cli/bin:${PATH}"'
    Name: env
  From:
    Image: node:alpine
  Name: ""
  Platform: ""
  SourceCode: FROM node:alpine
