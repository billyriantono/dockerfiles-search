MetaArgs: null
Stages:
- BaseName: rocker/r-ver:3.6.1
  Commands:
  - CmdLine:
    - install2.r --error --deps FALSE remotes
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update -qq   && apt-get install -y --no-install-recommends     libcurl4-openssl-dev     libssl-dev     libxml2-dev     git     zlib1g-dev     qpdf
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update   && apt-get install -y openjdk-8-jdk
    Name: run
    PrependShell: true
  - CmdLine:
    - install2.r --error --deps TRUE sparklyr
    Name: run
    PrependShell: true
  - CmdLine:
    - Rscript -e 'sparklyr::spark_install("2.4.3")'
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - resources/arrow_install.sh
    - /root/.local/arrow_install.sh
  - CmdLine:
    - sh /root/.local/arrow_install.sh
    Name: run
    PrependShell: true
  - CmdLine:
    - install2.r --error --deps TRUE arrow
    Name: run
    PrependShell: true
  - CmdLine:
    - install2.r --error --deps TRUE microbenchmark
    Name: run
    PrependShell: true
  - Key: RSTUDIO_VERSION
    Name: arg
    Value: null
  - Env:
    - Key: RSTUDIO_VERSION
      Value: ${RSTUDIO_VERSION:-1.2.1335}
    Name: env
  - Key: S6_VERSION
    Name: arg
    Value: null
  - Key: PANDOC_TEMPLATES_VERSION
    Name: arg
    Value: null
  - Env:
    - Key: S6_VERSION
      Value: ${S6_VERSION:-v1.21.7.0}
    Name: env
  - Env:
    - Key: S6_BEHAVIOUR_IF_STAGE2_FAILS
      Value: "2"
    Name: env
  - Env:
    - Key: PATH
      Value: /usr/lib/rstudio-server/bin:$PATH
    Name: env
  - Env:
    - Key: PANDOC_TEMPLATES_VERSION
      Value: ${PANDOC_TEMPLATES_VERSION:-2.6}
    Name: env
  - CmdLine:
    - "apt-get update   && apt-get install -y --no-install-recommends     file     libapparmor1
      \    libedit2     lsb-release     psmisc     procps     python-setuptools     sudo
      \    wget     libclang-dev     libclang-3.8-dev     libobjc-6-dev     libclang1-3.8
      \    libclang-common-3.8-dev     libllvm3.8     libobjc4     libgc1c2   && if
      [ -z \"$RSTUDIO_VERSION\" ]; then RSTUDIO_URL=\"https://www.rstudio.org/download/latest/stable/server/debian9_64/rstudio-server-latest-amd64.deb\";
      else RSTUDIO_URL=\"http://download2.rstudio.org/server/debian9/x86_64/rstudio-server-${RSTUDIO_VERSION}-amd64.deb\";
      fi   && wget -q $RSTUDIO_URL   && dpkg -i rstudio-server-*-amd64.deb   && rm
      rstudio-server-*-amd64.deb   && ln -s /usr/lib/rstudio-server/bin/pandoc/pandoc
      /usr/local/bin   && ln -s /usr/lib/rstudio-server/bin/pandoc/pandoc-citeproc
      /usr/local/bin   && git clone --recursive --branch ${PANDOC_TEMPLATES_VERSION}
      https://github.com/jgm/pandoc-templates   && mkdir -p /opt/pandoc/templates
      \  && cp -r pandoc-templates*/* /opt/pandoc/templates && rm -rf pandoc-templates*
      \  && mkdir /root/.pandoc && ln -s /opt/pandoc/templates /root/.pandoc/templates
      \  && apt-get clean   && rm -rf /var/lib/apt/lists/   && mkdir -p /etc/R   &&
      echo '\\n    \\n# Configure httr to perform out-of-band authentication if HTTR_LOCALHOST
      \    \\n# is not set since a redirect to localhost may not work depending upon
      \    \\n# where this Docker container is running.     \\nif(is.na(Sys.getenv(\"HTTR_LOCALHOST\",
      unset=NA))) {     \\n  options(httr_oob_default = TRUE)     \\n}' >> /usr/local/lib/R/etc/Rprofile.site
      \  && echo \"PATH=${PATH}\" >> /usr/local/lib/R/etc/Renviron   && useradd rstudio
      \  && echo \"rstudio:rstudio\" | chpasswd \t&& mkdir /home/rstudio \t&& chown
      rstudio:rstudio /home/rstudio \t&& addgroup rstudio staff   &&  echo 'rsession-which-r=/usr/local/bin/R'
      >> /etc/rstudio/rserver.conf   && echo 'lock-type=advisory' >> /etc/rstudio/file-locks
      \  && git config --system credential.helper 'cache --timeout=3600'   && git
      config --system push.default simple   && wget -P /tmp/ https://github.com/just-containers/s6-overlay/releases/download/${S6_VERSION}/s6-overlay-amd64.tar.gz
      \  && tar xzf /tmp/s6-overlay-amd64.tar.gz -C /   && mkdir -p /etc/services.d/rstudio
      \  && echo '#!/usr/bin/with-contenv bash           \\n## load /etc/environment
      vars first:   \t\t  \\n for line in $( cat /etc/environment ) ; do export $line
      ; done           \\n exec /usr/lib/rstudio-server/bin/rserver --server-daemonize
      0'           > /etc/services.d/rstudio/run   && echo '#!/bin/bash           \\n
      rstudio-server stop'           > /etc/services.d/rstudio/finish   && mkdir -p
      /home/rstudio/.rstudio/monitored/user-settings   && echo 'alwaysSaveHistory=\"0\"
      \          \\nloadRData=\"0\"           \\nsaveAction=\"0\"'           > /home/rstudio/.rstudio/monitored/user-settings/user-settings
      \  && chown -R rstudio:rstudio /home/rstudio/.rstudio"
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - resources/userconf.sh
    - /etc/cont-init.d/userconf
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - resources/disable_auth_rserver.conf
    - /etc/rstudio/disable_auth_rserver.conf
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - resources/pam-helper.sh
    - /usr/lib/rstudio-server/bin/pam-helper
  - Name: expose
    Ports:
    - "8787"
  - Name: volume
    Volumes:
    - /home/rstudio/kitematic
  - CmdLine:
    - /init
    Name: cmd
    PrependShell: false
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - resources/spark_script.R
    - /root/.local/spark_script.R
  - CmdLine:
    - install2.r --error --deps TRUE bookdown
    Name: run
    PrependShell: true
  - CmdLine:
    - install2.r --error --deps TRUE data.table
    Name: run
    PrependShell: true
  - CmdLine:
    - Rscript -e 'remotes::install_version("highcharter", "0.7.0")'
    Name: run
    PrependShell: true
  - CmdLine:
    - install2.r --error microbenchmark
    Name: run
    PrependShell: true
  - CmdLine:
    - install2.r --error nycflights13
    Name: run
    PrependShell: true
  - CmdLine:
    - Rscript -e 'remotes::install_git("https://gitlab.com/jozefhajnala/jhaddins")'
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get clean   && apt-get autoremove   && rm -rf /var/lib/apt/lists/   && rm
      -rf /tmp/downloaded_packages/ /tmp/*.rds
    Name: run
    PrependShell: true
  From:
    Image: rocker/r-ver:3.6.1
  Name: ""
  Platform: ""
  SourceCode: FROM rocker/r-ver:3.6.1
