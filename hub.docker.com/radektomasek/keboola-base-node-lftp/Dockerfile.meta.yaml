MetaArgs: null
Stages:
- BaseName: radektomasek/keboola-base-node-build-tools
  Commands:
  - Maintainer: Radek Tomasek <radek.tomasek@gmail.com>
    Name: maintainer
  - Name: workdir
    Path: /tmp
  - CmdLine:
    - curl -O http://lftp.yar.ru/ftp/lftp-4.8.4.tar.gz && tar xzvf lftp-4.8.4.tar.gz
      && cd lftp-4.8.4 && ./configure --with-gnutls --without-openssl --prefix=${HOME}/local
      && make && make install && mkdir ~/.lftp/ && echo "set ftp:ssl-force true;set
      ftp:ssl-protect-data true;set ftp:ssl-allow true;set ssl:verify-certificate
      no;" >> ~/.lftp/rc && ln -s ~/local/bin/lftp /usr/bin/lftp
    Name: run
    PrependShell: true
  From:
    Image: radektomasek/keboola-base-node-build-tools
  Name: ""
  Platform: ""
  SourceCode: FROM radektomasek/keboola-base-node-build-tools
