MetaArgs: null
Stages:
- BaseName: elixir:1.7.3
  Commands:
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - CmdLine:
    - mix local.hex --force &&     mix local.rebar --force     && wget -qO- https://deb.nodesource.com/setup_8.x
      | bash -     && apt-get install nodejs -y
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /opt/app
  - Env:
    - Key: MIX_ENV
      Value: prod
    - Key: REPLACE_OS_VARS
      Value: "true"
    Name: env
  From:
    Image: elixir:1.7.3
  Name: ""
  Platform: ""
  SourceCode: FROM elixir:1.7.3
