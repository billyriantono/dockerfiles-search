MetaArgs: null
Stages:
- BaseName: python:3-alpine
  Commands:
  - CmdLine:
    - apk add --update build-base linux-headers libxml2 libxml2-dev libxslt libxslt-dev
      libjpeg-turbo libjpeg-turbo-dev aria2 &&   pip install udemy-dl==0.2.2a1 &&   apk
      del build-base linux-headers libxml2-dev libxslt-dev libjpeg-turbo-dev &&   rm
      -rf /var/cache/apk/*
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir /udemy
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /udemy
  - CmdLine:
    - udemy-dl
    Name: entrypoint
    PrependShell: false
  From:
    Image: python:3-alpine
  Name: ""
  Platform: ""
  SourceCode: FROM python:3-alpine
