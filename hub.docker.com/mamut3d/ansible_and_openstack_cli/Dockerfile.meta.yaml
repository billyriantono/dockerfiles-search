MetaArgs: null
Stages:
- BaseName: mamut3d/openstack_cli
  Commands:
  - Maintainer: Mamut3D
    Name: maintainer
  - Labels:
    - Key: Description
      Value: '"Openstack client tools with ansible client"'
    Name: label
  - CmdLine:
    - apt-get update && apt-get install -y software-properties-common &&   apt-add-repository
      ppa:ansible/ansible &&   apt-get update && apt-get install -y   ansible   qemu-utils   wget   curl   &&
      rm -rf /var/cache/apk/*
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash
    Name: cmd
    PrependShell: false
  From:
    Image: mamut3d/openstack_cli
  Name: ""
  Platform: ""
  SourceCode: FROM mamut3d/openstack_cli
