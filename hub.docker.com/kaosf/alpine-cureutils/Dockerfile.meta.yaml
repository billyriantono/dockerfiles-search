MetaArgs: null
Stages:
- BaseName: inzinger/alpine-ruby
  Commands:
  - Maintainer: ka <ka.kaosf@gmail.com>
    Name: maintainer
  - CmdLine:
    - apk --update --no-cache add ca-certificates git ruby-json ruby-bigdecimal &&   rm
      -rf /var/cache/apk/* &&   git clone https://github.com/greymd/cureutils.git
      &&   cd cureutils &&   gem install bundler rake &&   bundle install &&   rake
      build &&   find ./pkg/ -type f | head -n 1 | xargs gem install &&   cd .. &&   rm
      -rf cureutils
    Name: run
    PrependShell: true
  From:
    Image: inzinger/alpine-ruby
  Name: ""
  Platform: ""
  SourceCode: FROM inzinger/alpine-ruby
