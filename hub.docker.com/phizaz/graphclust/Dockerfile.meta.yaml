MetaArgs: null
Stages:
- BaseName: phizaz/bioconda:python3
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - ViennaRNA-2.4.0.tar.gz
    - /tmp/viennarna.tar.gz
  - CmdLine:
    - mkdir -p /tmp/viennarna     && tar -xf /tmp/viennarna.tar.gz -C /tmp/viennarna
      --strip-components=1     && cd /tmp/viennarna     && chmod +x ./configure     &&
      ./configure --prefix=/usr/local     && make -j4     && make install     && rm
      -rf /tmp/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - locarna-1.9.2.tar.gz
    - /tmp/locarna.tar.gz
  - CmdLine:
    - mkdir -p /tmp/locarna     && tar -xf /tmp/locarna.tar.gz -C /tmp/locarna --strip-components=1     &&
      cd /tmp/locarna     && chmod +x ./configure     && ./configure --prefix=/usr/local     &&
      make -j4     && make install     && rm -rf /tmp/*
    Name: run
    PrependShell: true
  - CmdLine:
    - conda install -y rnaz=2.1 && conda clean --yes --all
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - infernal-1.0.2.tar.gz
    - /tmp/infernal.tar.gz
  - CmdLine:
    - mkdir -p /tmp/infernal     && tar -xf /tmp/infernal.tar.gz -C /tmp/infernal
      --strip-components=1     && cd /tmp/infernal     && chmod +x ./configure     &&
      ./configure --prefix=/usr/local     && make -j4     && make install     && rm
      -rf /tmp/*
    Name: run
    PrependShell: true
  - CmdLine:
    - conda install -y cmfinder=0.2 && conda clean --yes --all
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - RNAshapes-2.1.6.tar.gz
    - /tmp/rnashapes.tar.gz
  - CmdLine:
    - mkdir -p /tmp/rnashapes     && tar -xf /tmp/rnashapes.tar.gz -C /tmp/rnashapes
      --strip-components=1     && cd /tmp/rnashapes     && chmod +x ./configure     &&
      ./configure --prefix=/usr/local     && make -j4     && make install     && rm
      -rf /tmp/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - GraphClust-0.7.6.tar.gz
    - /tmp/graphclust.tar.gz
  - CmdLine:
    - mkdir -p /tmp/graphclust     && tar -xf /tmp/graphclust.tar.gz -C /tmp/graphclust
      --strip-components=1     && cd /tmp/graphclust     && chmod +x ./configure     &&
      ./configure --prefix=/usr/local     && make -j4     && make install     && rm
      -rf /tmp/*
    Name: run
    PrependShell: true
  - CmdLine:
    - conda install -y perl-math-round perl-threaded && conda clean --yes --all
    Name: run
    PrependShell: true
  - CmdLine:
    - conda install -y octave && conda clean --yes --all
    Name: run
    PrependShell: true
  - Name: user
    User: docker
  - CmdLine:
    - mkdir -p /home/docker/workdir
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /home/docker/workdir
  - Env:
    - Key: WORKDIR
      Value: /home/docker/workdir
    Name: env
  From:
    Image: phizaz/bioconda:python3
  Name: ""
  Platform: ""
  SourceCode: FROM phizaz/bioconda:python3
