MetaArgs: null
Stages:
- BaseName: debian:latest
  Commands:
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - CmdLine:
    - 'apt-get update;       apt-get install -y       texlive-full;     apt-get clean;     rm
      -rf /var/lib/apt/lists/*;     rm -rf /usr/share/doc/ # delete 1.5GB of docs'
    Name: run
    PrependShell: true
  From:
    Image: debian:latest
  Name: ""
  Platform: ""
  SourceCode: FROM debian:latest
