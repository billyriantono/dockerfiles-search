MetaArgs: null
Stages:
- BaseName: amd64/ubuntu:bionic
  Commands:
  - CmdLine:
    - echo 'Dir::Cache "";' > /etc/apt/apt.conf.d/02nocache &&     echo 'Dir::Cache::archives
      "";' >> /etc/apt/apt.conf.d/02nocache &&     apt-get update &&     DEBIAN_FRONTEND=noninteractive  apt-get
      -y install --no-install-recommends         locales &&     rm -rf /var/lib/apt/lists/*
      &&     rm -rf /var/cache/apt/archives &&     localedef -i en_US -c -f UTF-8
      -A /usr/share/locale/locale.alias en_US.UTF-8
    Name: run
    PrependShell: true
  - Env:
    - Key: LANG
      Value: en_US.utf8
    Name: env
  From:
    Image: amd64/ubuntu:bionic
  Name: ""
  Platform: ""
  SourceCode: FROM amd64/ubuntu:bionic
