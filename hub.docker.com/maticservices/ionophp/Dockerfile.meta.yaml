MetaArgs: null
Stages:
- BaseName: php:5.6-apache
  Commands:
  - CmdLine:
    - apt-get update         && apt-get install -y software-properties-common         &&
      apt-get update         && apt-get install -y         libmcrypt-dev libpng-dev         libmemcached11
      libmemcachedutil2 build-essential libmemcached-dev libz-dev         librabbitmq4
      librabbitmq-dev             mysql-client             openssl             wget             git             unzip             cron             vim             memcached             supervisor             sudo             acl         &&
      pecl install memcached-2.2.0         && docker-php-ext-enable memcached         &&
      pecl install amqp         && docker-php-ext-enable amqp         && pecl install
      xdebug-2.5.0         && docker-php-ext-enable xdebug         && curl -L https://github.com/hnw/php-timecop/archive/master.tar.gz
      | tar -xz         && cd php-timecop-master && phpize && ./configure && make
      && make install         && docker-php-ext-enable timecop         && curl -sS
      https://getcomposer.org/installer | php -- --install-dir=/usr/bin/ --filename=composer         &&
      rm -rf /var/lib/apt/lists/*         && apt-get remove -y build-essential libmemcached-dev
      libz-dev         && apt-get autoremove -y         && rm -rf /tmp/pear
    Name: run
    PrependShell: true
  - CmdLine:
    - docker-php-ext-install         pdo         pcntl         pdo_mysql         mcrypt         zip         gd
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - php.ini
    - /usr/local/etc/php/php.ini
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - default.conf
    - /etc/apache2/sites-enabled/000-default.conf
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - supervisord.conf
    - /etc/supervisor/supervisord.conf
  - CmdLine:
    - ln -s /app /var/www/site
    Name: run
    PrependShell: true
  - CmdLine:
    - a2enmod headers rewrite
    Name: run
    PrependShell: true
  - CmdLine:
    - systemctl enable memcached
    Name: run
    PrependShell: true
  From:
    Image: php:5.6-apache
  Name: ""
  Platform: ""
  SourceCode: FROM php:5.6-apache
