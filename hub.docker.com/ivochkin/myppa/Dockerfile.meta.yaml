MetaArgs: null
Stages:
- BaseName: ubuntu:trusty
  Commands:
  - Maintainer: Stanislav Ivochkin "isn@extrn.org"
    Name: maintainer
  - CmdLine:
    - apt-get update   && apt-get install --no-install-recommends --no-install-suggests
      -y     ca-certificates     build-essential     binutils     fakeroot     cmake     git     autotools-dev     autoconf     autogen     automake     libtool     wget     curl
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:trusty
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:trusty
