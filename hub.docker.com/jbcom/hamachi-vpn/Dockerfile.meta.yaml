MetaArgs: null
Stages:
- BaseName: ubuntu:18.04
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - hamachi.sh
    - /usr/local/bin/
  - CmdLine:
    - apt-get update &&     DEBIAN_FRONTEND=noninteractive apt-get install -yqq --no-install-recommends         curl         psmisc         python-pip         python-setuptools         python-wheel         supervisor
      &&     pip install --no-cache-dir supervisor-stdout &&     curl https://www.vpn.net/installers/logmein-hamachi_2.1.0.198-1_amd64.deb
      -o /tmp/hamachi.deb &&     dpkg -i /tmp/hamachi.deb &&     /etc/init.d/logmein-hamachi
      stop &&     rm -f /etc/init.d/logmein-hamachi &&     rm -rf /tmp/* &&     chmod
      755 /usr/local/bin/hamachi.sh
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - supervisord.conf
    - /etc/supervisor/supervisord.conf
  - CmdLine:
    - supervisord
    - -c
    - /etc/supervisor/supervisord.conf
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:18.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:18.04
