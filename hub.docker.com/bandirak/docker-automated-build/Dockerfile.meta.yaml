MetaArgs: null
Stages:
- BaseName: fedora
  Commands:
  - Maintainer: http://fedoraproject.org/wiki/Cloud
    Name: maintainer
  - CmdLine:
    - dnf -y install nginx && dnf clean all
    Name: run
    PrependShell: true
  - CmdLine:
    - dnf -y update && dnf clean all
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "daemon off;" >> /etc/nginx/nginx.conf
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "nginx on Fedora Container" > /usr/share/nginx/html/index.html
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "80"
  - CmdLine:
    - /usr/sbin/nginx
    Name: cmd
    PrependShell: false
  From:
    Image: fedora
  Name: ""
  Platform: ""
  SourceCode: FROM fedora
