MetaArgs: null
Stages:
- BaseName: nginx:stable-alpine
  Commands:
  - Env:
    - Key: ID
      Value: '"e92d4093-dbe9-4d6a-b615-e4971ee62fac"'
    - Key: VER
      Value: "3.19"
    Name: env
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - conf/
    - /
  - Chown: ""
    Name: add
    SourcesAndDest:
    - entrypoint.sh
    - /entrypoint.sh
  - CmdLine:
    - "apk update \t&& apk add curl bash supervisor \t&& mkdir -m 777 /var/log/v2ray
      \t&& chmod -R 777 /var/log/ \t&& touch /run/supervisord.pid \t&& chmod 777 /run/supervisord.pid
      \t&& chmod +x /entrypoint.sh \t&& touch /var/run/nginx.pid \t&& chown -R nginx:nginx
      /var/run/nginx.pid \t&& chown -R nginx:nginx /var/log/nginx \t&& chown -R nginx:nginx
      /etc/nginx/ \t&& rm -rf /var/cache/apk/* \t&& curl -L -H \"Cache-Control: no-cache\"
      -o v2ray.zip http://github.com/v2ray/v2ray-core/releases/download/v$VER/v2ray-linux-64.zip
      \t&& unzip v2ray.zip \t&& mv ./v2ray-v$VER-linux-64/v2ray /usr/local/bin/ \t&&
      mv ./v2ray-v$VER-linux-64/v2ctl /usr/local/bin/ \t&& chmod +x /usr/local/bin/*
      \t&& chmod 777 /etc/ \t&& chmod 666 /etc/config.json \t&& rm -rf v2ray.zip \t&&
      rm -rf v2ray-v$VER-linux-64"
    Name: run
    PrependShell: true
  - CmdLine:
    - /entrypoint.sh
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - v2ray --config /etc/config.json
    Name: cmd
    PrependShell: false
  From:
    Image: nginx:stable-alpine
  Name: ""
  Platform: ""
  SourceCode: from nginx:stable-alpine
