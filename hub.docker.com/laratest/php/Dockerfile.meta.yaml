MetaArgs: null
Stages:
- BaseName: php:7.1-alpine
  Commands:
  - CmdLine:
    - apk --no-cache --update add curl     vim     freetype-dev     libjpeg-turbo-dev     libpng-dev     openssl-dev     libmcrypt     libmcrypt-dev     &&
      docker-php-ext-configure mcrypt     && docker-php-ext-install     mcrypt     pdo_mysql     tokenizer     zip     &&
      docker-php-ext-configure gd     --enable-gd-native-ttf     --with-jpeg-dir=/usr/lib     --with-freetype-dir=/usr/include/freetype2
      &&     docker-php-ext-install gd     && docker-php-ext-install gd &&     docker-php-ext-configure
      gd         --enable-gd-native-ttf         --with-jpeg-dir=/usr/lib         --with-freetype-dir=/usr/include/freetype2
      &&     docker-php-ext-install gd &&     apk add --update --no-cache --virtual
      .build-deps autoconf build-base php7-pcntl &&     docker-php-ext-install pcntl
      &&     pecl -q install xdebug-2.6.1     && rm -rf /var/cache/apk/* /tmp/* /var/tmp/*     &&
      apk del .build-deps
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -s http://getcomposer.org/installer | php &&     echo "export PATH=${PATH}:/var/www/vendor/bin"
      >> ~/.bashrc &&     mv composer.phar /usr/local/bin/composer &&     . ~/.bashrc
      &&     export COMPOSER_ALLOW_SUPERUSER=1 &&     composer global require "squizlabs/php_codesniffer=*"
      hirak/prestissimo         --no-interaction --no-progress --no-ansi --no-scripts
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /var/www
  - CmdLine:
    - php
    Name: cmd
    PrependShell: false
  From:
    Image: php:7.1-alpine
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.1-alpine
