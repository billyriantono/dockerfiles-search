MetaArgs: null
Stages:
- BaseName: php:7.0-apache
  Commands:
  - CmdLine:
    - "apt-get update && \tapt-get install -y --no-install-recommends             zip
      unzip dialog locales             git-core             g++             zlib1g-dev
      \            libicu-dev               libfreetype6-dev             libjpeg62-turbo-dev
      \            libpng-dev         && docker-php-ext-configure intl         &&
      docker-php-ext-configure gd --with-freetype-dir=/usr/include/ --with-jpeg-dir=/usr/include/
      \        && docker-php-ext-install  -j$(nproc) zip pdo pdo_mysql intl gd bcmath
      \        && rm -rf /var/lib/apt/lists/* \t&& a2enmod rewrite \t&& curl -sS https://getcomposer.org/installer
      | php \t&& mv composer.phar /usr/local/bin/composer"
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - apache2.conf
    - /etc/apache2/
  From:
    Image: php:7.0-apache
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.0-apache
