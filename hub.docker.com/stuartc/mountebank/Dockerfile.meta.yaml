MetaArgs: null
Stages:
- BaseName: ubuntu
  Commands:
  - Maintainer: Stuart Clark "stuart.clark@Jahingo.com"
    Name: maintainer
  - CmdLine:
    - "apt-get update && apt-get install -y curl ca-certificates --no-install-recommends
      && rm -rf /var/lib/apt/lists/* \t&& curl -SL \"https://s3.amazonaws.com/mountebank/v1.2/mountebank_1.2.30_amd64.deb\"
      -o mountebank.deb \t&& dpkg --install mountebank.deb \t&& rm mountebank.deb
      \t&& apt-get purge -y --auto-remove curl"
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "2525"
  - CmdLine:
    - mb
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu
