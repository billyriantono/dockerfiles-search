MetaArgs:
- DefaultValue: null
  Key: OS
  ProvidedValue: null
  Value: null
Stages:
- BaseName: cangjians/libcangjie:${OS}
  Commands:
  - Maintainer: Cangjians (https://cangjians.github.io)
    Name: maintainer
  - Name: workdir
    Path: /usr/local/src/pycangjie
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - '"."'
    - '"./"'
  - CmdLine:
    - ./autogen.sh --prefix=/usr &&   make &&   if grep -q ID_LIKE=debian /usr/lib/os-release;
      then     make install pyexecdir=/usr/lib/python3/dist-packages;   else     make
      install;   fi
    Name: run
    PrependShell: true
  From:
    Image: cangjians/libcangjie:${OS}
  Name: ""
  Platform: ""
  SourceCode: FROM cangjians/libcangjie:${OS}
