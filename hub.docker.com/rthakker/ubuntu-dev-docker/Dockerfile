# Dockerfile for creatx-django ( master branch )

FROM ubuntu:wily
# Environment variables
# None so far.

# Install packages
RUN apt-get update && \
    apt-get install python-virtualenv python-dev build-essential expect \
    build-essential g++ gcc \
    perl curl git openssh-server \
    elinks vim tmux -y
