MetaArgs: null
Stages:
- BaseName: kamaninga/bootstrap
  Commands:
  - Name: volume
    Volumes:
    - /etc/letsencrypt
  - Name: volume
    Volumes:
    - /var/www
  - CmdLine:
    - /usr/local/bin/apk-install         python         py-crypto         py2-pip         py2-dnspython         certbot     &&
      pip install --upgrade pip     && hash -r     && pip install pyRFC3339 configobj
      ConfigArgParse     && docker-image-cleanup
    Name: run
    PrependShell: true
  From:
    Image: kamaninga/bootstrap
  Name: ""
  Platform: ""
  SourceCode: FROM kamaninga/bootstrap
