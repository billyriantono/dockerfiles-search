MetaArgs: null
Stages:
- BaseName: jenkins/jenkins:lts
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"Hugo Josefson <hugo@josefson.org> (https://www.hugojosefson.com)"'
    Name: label
  - Name: user
    User: root
  - CmdLine:
    - (echo "deb http://deb.debian.org/debian stretch-backports main" >> /etc/apt/sources.list)   &&
      apt-get update   && apt-get -t stretch-backports dist-upgrade -y   && apt-get
      -t stretch-backports install -y vim libltdl7   && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Name: user
    User: jenkins
  From:
    Image: jenkins/jenkins:lts
  Name: ""
  Platform: ""
  SourceCode: FROM jenkins/jenkins:lts
