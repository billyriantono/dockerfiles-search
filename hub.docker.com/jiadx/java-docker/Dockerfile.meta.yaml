MetaArgs: null
Stages:
- BaseName: centos:6.7
  Commands:
  - CmdLine:
    - yum install -y epel-release &&  yum makecache &&  yum install -y man passwd
      sudo tar which git mlocate bzip2 net-tools openssh-clients openssh-server
    Name: run
    PrependShell: true
  - CmdLine:
    - 'cd /tmp &&     curl -L -O -H "Cookie: oraclelicense=accept-securebackup-cookie"
      -k "http://download.oracle.com/otn-pub/java/jdk/8u60-b27/jdk-8u60-linux-x64.tar.gz"
      &&     mkdir -p /opt/java && tar xf jdk-8u60-linux-x64.tar.gz -C /opt/java &&     rm
      -f jdk-8u60-linux-x64.tar.gz &&     ln -s /opt/java/jdk* /opt/jdk1.8'
    Name: run
    PrependShell: true
  - CmdLine:
    - yum clean all
    Name: run
    PrependShell: true
  - Env:
    - Key: JAVA_HOME
      Value: /opt/jdk1.8
    Name: env
  - Env:
    - Key: PATH
      Value: $JAVA_HOME/bin:$PATH
    Name: env
  - CmdLine:
    - java -version
    Name: run
    PrependShell: true
  - CmdLine:
    - bash
    Name: cmd
    PrependShell: false
  - Name: workdir
    Path: /
  From:
    Image: centos:6.7
  Name: ""
  Platform: ""
  SourceCode: FROM centos:6.7
