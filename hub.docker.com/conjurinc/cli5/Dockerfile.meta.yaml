MetaArgs: null
Stages:
- BaseName: ruby:2.4.1
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y --no-install-recommends   jq curl vim nano   &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir -p /usr/src &&     cd /usr/src &&     git clone https://github.com/conjurinc/api-ruby.git
      -b possum &&     cd api-ruby &&     rake install
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - standalone.entrypoint
    - /bin/entry
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - .
    - /usr/src/cli-ruby
  - CmdLine:
    - cd /usr/src/api-ruby && git fetch &&     if [ $(git rev-parse HEAD origin/possum
      | uniq | wc -l) -gt 1 ]; then       git reset --hard origin/possum && rake install
      ;     fi
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /usr/src/cli-ruby && rake install
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/entry
    Name: entrypoint
    PrependShell: false
  From:
    Image: ruby:2.4.1
  Name: ""
  Platform: ""
  SourceCode: FROM ruby:2.4.1
