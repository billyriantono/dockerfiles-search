MetaArgs: null
Stages:
- BaseName: debian:wheezy
  Commands:
  - Maintainer: leafsoar <kltwjt@gmail.com>
    Name: maintainer
  - CmdLine:
    - "apt-get update \t&& apt-get install -y rinetd \t&& rm -rf /var/lib/apt/lists/*"
    Name: run
    PrependShell: true
  - CmdLine:
    - "{ \techo '#!/bin/bash'; \techo 'set -e'; \techo 'echo \"0.0.0.0        8000
      \   $1      $2\" >> /rinetd.conf'; \techo 'echo \"logfile /var/log/rinetd.log\"
      >>/rinetd.conf'; \techo '/usr/sbin/rinetd -f -c /rinetd.conf';     } > /rinetd-entrypoint.sh"
    Name: run
    PrependShell: true
  - CmdLine:
    - chmod +x /rinetd-entrypoint.sh
    Name: run
    PrependShell: true
  - CmdLine:
    - /rinetd-entrypoint.sh
    Name: entrypoint
    PrependShell: false
  From:
    Image: debian:wheezy
  Name: ""
  Platform: ""
  SourceCode: FROM debian:wheezy
