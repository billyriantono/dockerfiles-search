MetaArgs: null
Stages:
- BaseName: php:5.6-apache
  Commands:
  - CmdLine:
    - apt-get update     && DEBIAN_FRONTEND=noninteractive apt-get install -y php5-xdebug     &&
      DEBIAN_FRONTEND=noninteractive apt-get install -y python     && DEBIAN_FRONTEND=noninteractive
      apt-get install -y graphviz
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -L -O https://github.com/jokkedk/webgrind/archive/master.tar.gz     &&
      mkdir /var/www/html/webgrind     && tar xf master.tar.gz -C /var/www/html/webgrind
      --strip-components=1     && rm master.tar.gz     && sed 's/\/usr\/local\/bin\/dot/\/usr\/bin\/dot/g'
      -i /var/www/html/webgrind/config.php     && sed 's/\/tmp/\/data/g' -i /var/www/html/webgrind/config.php
    Name: run
    PrependShell: true
  From:
    Image: php:5.6-apache
  Name: ""
  Platform: ""
  SourceCode: FROM php:5.6-apache
