MetaArgs: null
Stages:
- BaseName: bitnami/minideb:stretch
  Commands:
  - Maintainer: Johannes Köster <johannes.koester@tu-dortmund.de>
    Name: maintainer
  - Chown: ""
    Name: add
    SourcesAndDest:
    - .
    - /tmp/repo
  - Name: workdir
    Path: /tmp/repo
  - Env:
    - Key: PATH
      Value: /opt/conda/bin:${PATH}
    Name: env
  - Env:
    - Key: LANG
      Value: C.UTF-8
    Name: env
  - Env:
    - Key: SHELL
      Value: /bin/bash
    Name: env
  - CmdLine:
    - /bin/bash -c "install_packages wget bzip2 ca-certificates gnupg2 squashfs-tools
      git &&     wget -O- http://neuro.debian.net/lists/xenial.us-ca.full > /etc/apt/sources.list.d/neurodebian.sources.list
      &&     wget -O- http://neuro.debian.net/_static/neuro.debian.net.asc | apt-key
      add - &&     install_packages singularity-container &&     wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh
      &&     bash Miniconda3-latest-Linux-x86_64.sh -b -p /opt/conda &&     rm Miniconda3-latest-Linux-x86_64.sh
      &&     conda env create -n snakemake --file /tmp/repo/environment.yml &&     conda
      clean --all -y &&     source activate snakemake &&     which python &&     pip
      install ."
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "source activate snakemake" > ~/.bashrc
    Name: run
    PrependShell: true
  - Env:
    - Key: PATH
      Value: /opt/conda/envs/snakemake/bin:${PATH}
    Name: env
  From:
    Image: bitnami/minideb:stretch
  Name: ""
  Platform: ""
  SourceCode: FROM bitnami/minideb:stretch
