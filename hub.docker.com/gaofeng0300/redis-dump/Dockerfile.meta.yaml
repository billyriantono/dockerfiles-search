MetaArgs: null
Stages:
- BaseName: redis
  Commands:
  - CmdLine:
    - apt-get update && apt-get install ruby-dev -y     && apt-get install make -y     &&
      apt-get install gcc -y     && apt-get clean     && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - gem install redis-dump -V
    Name: run
    PrependShell: true
  From:
    Image: redis
  Name: ""
  Platform: ""
  SourceCode: FROM redis
