MetaArgs: null
Stages:
- BaseName: ruby:2.3
  Commands:
  - CmdLine:
    - apt-get update -y && apt-get install sudo vim openssh-server git python-pip
      apache2 lsb-release -y && apt-get clean all
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "deb http://deb.goaccess.io/ $(lsb_release -cs) main" >> /etc/apt/sources.list.d/goaccess.list
    Name: run
    PrependShell: true
  - CmdLine:
    - wget -O - http://deb.goaccess.io/gnugpg.key | sudo apt-key add -
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update -y && apt-get install -y goaccess geoip-database && apt-get clean
      all
    Name: run
    PrependShell: true
  - CmdLine:
    - pip install supervisor
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir /var/run/sshd
    Name: run
    PrependShell: true
  - CmdLine:
    - echo 'root:changeme' | chpasswd
    Name: run
    PrependShell: true
  - CmdLine:
    - sed -i 's/PermitRootLogin without-password/PermitRootLogin yes/' /etc/ssh/sshd_config
    Name: run
    PrependShell: true
  - CmdLine:
    - sed 's@session\s*required\s*pam_loginuid.so@session optional pam_loginuid.so@g'
      -i /etc/pam.d/sshd
    Name: run
    PrependShell: true
  - CmdLine:
    - echo 'cd /usr/src/app' >> /root/.bash_profile
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "22"
    - "8888"
  From:
    Image: ruby:2.3
  Name: ""
  Platform: ""
  SourceCode: FROM ruby:2.3
