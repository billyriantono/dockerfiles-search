MetaArgs: null
Stages:
- BaseName: rocker/r-ver
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y     sudo     gdebi-core     pandoc     pandoc-citeproc     libcurl4-gnutls-dev     libcairo2-dev     libxt-dev     xtail     wget     build-essential     libxml2-dev     libssl-dev     libsodium-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - wget --no-verbose https://download3.rstudio.org/ubuntu-14.04/x86_64/VERSION
      -O "version.txt" &&     VERSION=$(cat version.txt)  &&     wget --no-verbose
      "https://download3.rstudio.org/ubuntu-14.04/x86_64/shiny-server-$VERSION-amd64.deb"
      -O ss-latest.deb &&     gdebi -n ss-latest.deb &&     rm -f version.txt ss-latest.deb
      &&     . /etc/environment &&     R -e "install.packages(c('devtools'), repos='$MRAN')"
      &&     R -e "install.packages(c('shiny', 'rmarkdown'), repos='$MRAN')" &&     R
      -e "devtools::install_github('paulc91/shinyauthr')" &&     R -e "devtools::install_github('ramnathv/rCharts')"
      &&     R -e "install.packages(c('shinydashboard', 'DT', 'glue', 'dplyr', 'forcats',
      'shinycssloaders'), repos='$MRAN')" &&     cp -R /usr/local/lib/R/site-library/shiny/examples/*
      /srv/shiny-server/ &&     chown shiny:shiny /var/lib/shiny-server
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "3838"
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - shiny-server.sh
    - /usr/bin/shiny-server.sh
  - CmdLine:
    - chmod
    - +x
    - /usr/bin/shiny-server.sh
    Name: run
    PrependShell: false
  - CmdLine:
    - /usr/bin/shiny-server.sh
    Name: cmd
    PrependShell: false
  From:
    Image: rocker/r-ver
  Name: ""
  Platform: ""
  SourceCode: FROM rocker/r-ver
