MetaArgs: null
Stages:
- BaseName: phusion/baseimage:0.9.16
  Commands:
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    - Key: HOME
      Value: '"/root"'
    - Key: TERM
      Value: xterm
    - Key: LANG
      Value: en_US.UTF-8
    - Key: LANGUAGE
      Value: en_US:en
    - Key: LC_ALL
      Value: en_US.UTF-8
    Name: env
  - Chown: ""
    Name: add
    SourcesAndDest:
    - src/
    - /root/
  - Name: volume
    Volumes:
    - /config
  - CmdLine:
    - locale-gen en_US.UTF-8 && mv /root/firstrun.sh /etc/my_init.d/firstrun.sh &&
      chmod +x /etc/my_init.d/firstrun.sh && configOPTS="--enable-cyrus --enable-python
      --enable-swig --enable-tcl --enable-perl --disable-ipv6" && buildDepsTemp="build-essential
      pkg-config make autoconf automake tcl8.6-dev libicu-dev swig3.0" && buildDepsPerm="git-core
      python3-dev libpython3-dev libsasl2-dev libssl-dev libperl-dev" && runtimeDeps="tcl8.6
      supervisor libicu52 libperl5.18" && usermod -u 99 nobody && usermod -g 100 nobody
      && mv /root/excludes /etc/dpkg/dpkg.cfg.d/excludes && echo "deb http://archive.ubuntu.com/ubuntu/
      trusty-backports main restricted universe multiverse" >> /etc/apt/sources.list
      && echo "deb-src http://archive.ubuntu.com/ubuntu/ trusty-backports main restricted
      universe multiverse" >> /etc/apt/sources.list && apt-get update -qq && apt-get
      install --no-install-recommends $buildDepsTemp $buildDepsPerm  -qy && cd /tmp
      && git clone https://github.com/znc/znc.git --recursive && cd /tmp/znc && git
      clean -xdf && ./autogen.sh && ./configure $configOPTS && make && make install
      && apt-get purge --remove $buildDepsTemp -y && apt-get autoremove -y && apt-get
      install --no-install-recommends $buildDepsPerm $runtimeDeps -qy && cd / && apt-get
      clean && rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/* /usr/share/man /usr/share/groff
      /usr/share/info /usr/share/lintian /usr/share/linda /var/cache/man && (( find
      /usr/share/doc -depth -type f ! -name copyright|xargs rm || true )) && (( find
      /usr/share/doc -empty|xargs rmdir || true ))
    Name: run
    PrependShell: true
  From:
    Image: phusion/baseimage:0.9.16
  Name: ""
  Platform: ""
  SourceCode: FROM phusion/baseimage:0.9.16
