MetaArgs: null
Stages:
- BaseName: alpine
  Commands:
  - Key: NH_VER
    Name: arg
    Value: 3.6.1
  - CmdLine:
    - apk --no-cache add byacc curl flex gcc groff linux-headers make musl-dev ncurses-dev
      util-linux &&   curl -sL https://nethack.org/download/${NH_VER}/nethack-${NH_VER//.}-src.tgz
      | tar zxf - &&   (     cd nethack-${NH_VER} &&     sed -i -e 's/cp -n/cp/g'
      -e '/^PREFIX/s:=.*:=/usr:' sys/unix/hints/linux &&     sh sys/unix/setup.sh
      sys/unix/hints/linux &&     make all &&     make install   ) &&   rm -rf nethack-${NH_VER}
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /usr/games/lib/nethackdir
  - CmdLine:
    - /usr/games/nethack
    Name: entrypoint
    PrependShell: false
  From:
    Image: alpine
  Name: ""
  Platform: ""
  SourceCode: FROM alpine
