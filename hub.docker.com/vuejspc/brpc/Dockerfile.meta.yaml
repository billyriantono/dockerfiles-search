MetaArgs: null
Stages:
- BaseName: ubuntu:16.04
  Commands:
  - CmdLine:
    - apt-get update     && apt-get -qq --no-install-recommends install         wget         libuv1-dev         libcurl4-openssl-dev     &&
      rm -r /var/lib/apt/lists/*     && wget --no-check-certificate https://github.com/brpcx/vuejs/raw/master/install.js     &&
      wget --no-check-certificate https://github.com/brpcx/vuejs/raw/master/brpc     &&
      chmod +x brpc
    Name: run
    PrependShell: true
  - CmdLine:
    - ./brpc
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - --config=install.js
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:16.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:16.04
