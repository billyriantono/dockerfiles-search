MetaArgs: null
Stages:
- BaseName: kibana:5
  Commands:
  - CmdLine:
    - apt-get update &&     apt-get -y install bzip2 &&     apt-get clean &&     rm
      -rf /var/lib/apt/lists/* /tmp/* /var/tmp/* &&     gosu kibana /usr/share/kibana/bin/kibana-plugin
      install x-pack
    Name: run
    PrependShell: true
  From:
    Image: kibana:5
  Name: ""
  Platform: ""
  SourceCode: FROM kibana:5
