MetaArgs: null
Stages:
- BaseName: rust:latest
  Commands:
  - CmdLine:
    - apt-get update &&   apt-get install -y protobuf-compiler cmake &&   apt-get
      install --no-install-recommends -y nano net-tools tcpdump iproute2 netcat ngrep
      atop gdb strace &&   apt-get clean &&   rm -r /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /starcoin
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - rust-toolchain
    - /starcoin/rust-toolchain
  - CmdLine:
    - rustup install $(cat rust-toolchain)
    Name: run
    PrependShell: true
  From:
    Image: rust:latest
  Name: ""
  Platform: ""
  SourceCode: FROM rust:latest
