MetaArgs: null
Stages:
- BaseName: mariadb
  Commands:
  - CmdLine:
    - apt-get update  && apt-get upgrade -y --force-yes -o Dpkg::Options::="--force-confnew"
      --no-install-recommends  && apt-get autoremove  && apt-get clean -y  && rm -rf
      /tmp/*  && rm -rf /var/tmp/*  && for logs in `find /var/log -type f`; do > $logs;
      done  && rm -rf /usr/share/locale/*  && rm -rf /usr/share/man/*  && rm -rf /usr/share/doc/*  &&
      rm -rf /var/lib/apt/lists/*  && rm -f /var/cache/apt/*.bin
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - setup/
    - /
  - CmdLine:
    - chmod +x /usr/local/bin/*     && chmod 777 /usr/local/bin/*
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /tmp     && touch test.txt
    Name: run
    PrependShell: true
  - CmdLine:
    - /usr/local/bin/entrypoint.sh
    Name: entrypoint
    PrependShell: false
  From:
    Image: mariadb
  Name: ""
  Platform: ""
  SourceCode: FROM mariadb
