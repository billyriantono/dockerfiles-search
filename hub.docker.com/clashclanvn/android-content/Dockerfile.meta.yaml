MetaArgs: null
Stages:
- BaseName: buildkite/agent:2
  Commands:
  - CmdLine:
    - "apk --no-cache add bash gawk sed grep bc coreutils jq \tgroff less python py-pip
      && \tmkdir -p /aws && \tpip install awscli"
    Name: run
    PrependShell: true
  From:
    Image: buildkite/agent:2
  Name: ""
  Platform: ""
  SourceCode: FROM buildkite/agent:2
