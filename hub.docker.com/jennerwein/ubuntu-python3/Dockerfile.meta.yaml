MetaArgs: null
Stages:
- BaseName: ubuntu:18.04
  Commands:
  - CmdLine:
    - apt-get update -y     && apt-get install -y vim     && apt-get install -y python3-pip     &&
      apt-get clean     && apt-get autoclean     && rm -rf /var/cache/*     && rm
      -rf /tmp/*     && rm -rf /var/tmp/*     && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - python3
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:18.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:18.04
