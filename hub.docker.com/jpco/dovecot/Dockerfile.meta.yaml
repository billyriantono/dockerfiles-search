MetaArgs: null
Stages:
- BaseName: codingwell/rpi-raspbian-qemu
  Commands:
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    - Key: DOMAIN
      Value: example.com
    - Key: DOVECOT_SSL_CERT_PATH
      Value: </etc/dovecot/dovecot.pem
    - Key: DOVECOT_SSL_CERT_KEY
      Value: </etc/dovecot/private/dovecot.pem
    - Key: VMAILUSER_ID
      Value: "5000"
    - Key: POSTMASTER
      Value: postmaster@example.com
    Name: env
  - CmdLine:
    - cross-build-start
    Name: run
    PrependShell: false
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - assets/bin/
    - /bin/
  - CmdLine:
    - apt-get update && apt-get -y install --no-install-recommends  ca-certificates  dovecot-imapd  dovecot-lmtpd  dovecot-sqlite  rsyslog
      &&  cp -r /etc/dovecot/conf.d /etc/dovecot/conf.d.bak &&  cd /usr/share/dovecot
      && ./mkcert.sh &&  chmod a+x /bin/*.sh && chmod a+x /bin/confset && touch /var/mail/dovecot-users
      &&  apt-get clean &&  rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - assets/etc/dovecot
    - /etc/dovecot/
  - CmdLine:
    - cross-build-end
    Name: run
    PrependShell: false
  - CmdLine:
    - entrypoint.sh
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - dovecot
    - -F
    Name: cmd
    PrependShell: false
  From:
    Image: codingwell/rpi-raspbian-qemu
  Name: ""
  Platform: ""
  SourceCode: FROM codingwell/rpi-raspbian-qemu
