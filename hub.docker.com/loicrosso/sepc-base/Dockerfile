FROM alpine:3.9
LABEL maintainer="https://github.com/Loic-R"
LABEL Description="This image is used as base for other docker images of docker-SPEC repo"
LABEL version="Alpha-1.0"

RUN apk update \
    && apk add --no-cache bash openssh gcc libc-dev g++ cmake make autoconf automake pkgconfig git