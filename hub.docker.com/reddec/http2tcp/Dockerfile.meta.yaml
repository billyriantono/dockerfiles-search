MetaArgs: null
Stages:
- BaseName: golang:onbuild
  Commands:
  - Name: expose
    Ports:
    - "9000"
  - CmdLine:
    - mkdir -p /etc/http2tcp && touch /etc/http2tcp/rules.conf
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - '"/etc/http2tcp/"'
  - CmdLine:
    - app
    - -b
    - :9000
    - /etc/http2tcp/rules.conf
    Name: cmd
    PrependShell: false
  From:
    Image: golang:onbuild
  Name: ""
  Platform: ""
  SourceCode: FROM golang:onbuild
