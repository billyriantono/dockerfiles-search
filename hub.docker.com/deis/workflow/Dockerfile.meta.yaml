MetaArgs: null
Stages:
- BaseName: alpine:3.2
  Commands:
  - CmdLine:
    - apk add --update-cache curl bash openssl sudo && rm -rf /var/cache/apk/*
    Name: run
    PrependShell: true
  - CmdLine:
    - apk add --update-cache curl tar     && curl -sSL https://github.com/coreos/etcd/releases/download/v2.2.1/etcd-v2.2.1-linux-amd64.tar.gz     |
      tar -vxz -C /usr/local/bin --strip=1 etcd-v2.2.1-linux-amd64/etcdctl     &&
      chown root:root /usr/local/bin/etcdctl     && curl -sSL -o /usr/local/bin/confd
      https://github.com/kelseyhightower/confd/releases/download/v0.10.0/confd-0.10.0-linux-amd64     &&
      chmod +x /usr/local/bin/confd     && apk del --purge curl tar     && rm -rf
      /var/cache/apk/*
    Name: run
    PrependShell: true
  - CmdLine:
    - /app/bin/boot
    Name: cmd
    PrependShell: false
  - Name: expose
    Ports:
    - "8000"
  - Name: workdir
    Path: /app
  - Chown: ""
    Name: add
    SourcesAndDest:
    - build.sh
    - /app/tmp/build.sh
  - Chown: ""
    Name: add
    SourcesAndDest:
    - requirements.txt
    - /app/requirements.txt
  - CmdLine:
    - DOCKER_BUILD=true /app/tmp/build.sh && rm -rf /app/*
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - .
    - /app
  - CmdLine:
    - './manage.py collectstatic --settings=deis.settings --noinput  # create static
      resources'
    Name: run
    PrependShell: true
  - Env:
    - Key: DEIS_RELEASE
      Value: 1.13.0-dev
    Name: env
  From:
    Image: alpine:3.2
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.2
