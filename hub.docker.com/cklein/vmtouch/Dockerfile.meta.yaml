MetaArgs: null
Stages:
- BaseName: ubuntu:16.10
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y     build-essential     git  && rm -rf
      /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /usr/share/  && git clone https://github.com/hoytech/vmtouch.git  && cd vmtouch  &&
      make  && make install
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /data
  - CmdLine:
    - /usr/local/bin/vmtouch
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - -l
    - /data
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:16.10
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:16.10
