MetaArgs: null
Stages:
- BaseName: php:5.6-fpm
  Commands:
  - Maintainer: Jono Warren <jono@justpark.com>
    Name: maintainer
  - Chown: ""
    Name: add
    SourcesAndDest:
    - ./setup.sh
    - /tmp/
  - CmdLine:
    - /bin/bash /tmp/setup.sh
    Name: run
    PrependShell: true
  - CmdLine:
    - "apt-get update && apt-get install -y \tlibpng12-dev \tlibjpeg-dev \tnginx \t&&
      docker-php-ext-configure gd --with-png-dir=/usr --with-jpeg-dir=/usr \t&& docker-php-ext-install
      mysqli gd \t&& apt-get clean \t&& rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*"
    Name: run
    PrependShell: true
  From:
    Image: php:5.6-fpm
  Name: ""
  Platform: ""
  SourceCode: FROM php:5.6-fpm
