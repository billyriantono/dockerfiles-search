MetaArgs: null
Stages:
- BaseName: ruby:latest
  Commands:
  - CmdLine:
    - apt-get update && apt-get -qq install nodejs -y
    Name: run
    PrependShell: true
  - CmdLine:
    - gem install rails
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /git/automation
  - Name: expose
    Ports:
    - "3000"
  - CmdLine:
    - groupadd -r felipe && useradd -r -g felipe felipe
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get install -y gcc make cron vim
    Name: run
    PrependShell: true
  - CmdLine:
    - bash
    - -c
    - bundle install; /etc/init.d/cron start; rails server -b 0.0.0.0 -p 3000
    Name: cmd
    PrependShell: false
  From:
    Image: ruby:latest
  Name: ""
  Platform: ""
  SourceCode: FROM ruby:latest
