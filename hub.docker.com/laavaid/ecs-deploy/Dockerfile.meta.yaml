MetaArgs: null
Stages:
- BaseName: python:3-alpine
  Commands:
  - Key: ECS_DEPLOY_VERSION
    Name: arg
    Value: null
  - Env:
    - Key: ECS_DEPLOY_VERSION
      Value: 1.10.0
    Name: env
  - CmdLine:
    - apk add --no-cache gettext ca-certificates make curl bash &&     update-ca-certificates
      &&     pip3 install --upgrade pip &&     pip3 install awscli &&     pip3 install
      --no-cache-dir boto3 pyyaml
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -o /usr/local/bin/ecs-cli https://amazon-ecs-cli.s3.amazonaws.com/ecs-cli-linux-amd64-latest
    Name: run
    PrependShell: true
  - CmdLine:
    - chmod +x /usr/local/bin/ecs-cli
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - ecs-deploy
    - /ecs-deploy
  - CmdLine:
    - pip3 install /ecs-deploy
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /root/.aws
  - Name: volume
    Volumes:
    - /opt/app
  - Name: workdir
    Path: /opt/app
  - CmdLine:
    - /usr/local/bin/ecs
    Name: cmd
    PrependShell: false
  From:
    Image: python:3-alpine
  Name: ""
  Platform: ""
  SourceCode: FROM python:3-alpine
