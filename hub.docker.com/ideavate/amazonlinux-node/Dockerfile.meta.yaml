MetaArgs: null
Stages:
- BaseName: ideavate/amazonlinux-node:12
  Commands:
  - CmdLine:
    - yum update -y
    Name: run
    PrependShell: true
  - CmdLine:
    - yum install -y python3-pip
    Name: run
    PrependShell: true
  - CmdLine:
    - pip3 install awsebcli awscli --upgrade
    Name: run
    PrependShell: true
  - CmdLine:
    - yum install -y yum-utils  && yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo  &&
      yum install -y docker-ce-cli
    Name: run
    PrependShell: true
  - CmdLine:
    - yum install -y git sudo tar bzip2
    Name: run
    PrependShell: true
  - CmdLine:
    - curl --silent --location https://dl.yarnpkg.com/rpm/yarn.repo | tee /etc/yum.repos.d/yarn.repo  &&
      yum install -y yarn
    Name: run
    PrependShell: true
  - CmdLine:
    - 'useradd -m -g docker builder && echo "builder ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/builder'
    Name: run
    PrependShell: true
  - CmdLine:
    - chmod a=rwx -R /home/builder
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /home/builder
  - Name: user
    User: builder
  From:
    Image: ideavate/amazonlinux-node:12
  Name: ""
  Platform: ""
  SourceCode: FROM ideavate/amazonlinux-node:12
