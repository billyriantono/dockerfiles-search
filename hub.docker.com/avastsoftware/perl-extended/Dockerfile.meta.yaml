MetaArgs: null
Stages:
- BaseName: perl:5.26
  Commands:
  - Maintainer: Viruslab Systems, Avast Software
    Name: maintainer
  - Env:
    - Key: PERL5LIB
      Value: /usr/share/perl5
    Name: env
  - CmdLine:
    - apt-get update     && apt-get -y install --no-install-recommends postgresql     &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - cpanfile
    - /root/cpanfile
  - CmdLine:
    - cpanm --verbose --notest Term::ReadKey && rm -rf ~/.cpanm
    Name: run
    PrependShell: true
  - CmdLine:
    - cpanm --verbose App::cpm Promises && rm -rf ~/.cpanm
    Name: run
    PrependShell: true
  - CmdLine:
    - cpm install --test --verbose -g && rm -rf ~/.cpanm
    Name: run
    PrependShell: true
  From:
    Image: perl:5.26
  Name: ""
  Platform: ""
  SourceCode: FROM perl:5.26
