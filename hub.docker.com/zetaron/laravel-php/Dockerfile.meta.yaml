MetaArgs: null
Stages:
- BaseName: php:7-fpm
  Commands:
  - Name: workdir
    Path: /var/www/html
  - Name: expose
    Ports:
    - "9000"
  - CmdLine:
    - /usr/bin/start-php-fpm
    Name: cmd
    PrependShell: false
  - Chown: ""
    Name: add
    SourcesAndDest:
    - start-php-fpm.sh
    - /usr/bin/start-php-fpm
  - Chown: ""
    Name: add
    SourcesAndDest:
    - php-fpm.conf
    - /usr/local/etc/php-fpm.conf
  - CmdLine:
    - curl -o /usr/bin/docker-php-pecl-install -L https://raw.githubusercontent.com/helderco/docker-php/master/template/bin/docker-php-pecl-install     &&
      chmod +x /usr/bin/docker-php-pecl-install
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y         libmcrypt-dev         mysql-client         libpq-dev         postgresql-client         libsqlite3-dev         libssl-dev     &&
      docker-php-ext-install tokenizer                               mbstring                               pdo     &&
      docker-php-ext-install mcrypt                               pdo_mysql                               pdo_pgsql                               pdo_sqlite     &&
      docker-php-pecl-install mongodb
    Name: run
    PrependShell: true
  From:
    Image: php:7-fpm
  Name: ""
  Platform: ""
  SourceCode: FROM php:7-fpm
