MetaArgs: null
Stages:
- BaseName: golang:1.6
  Commands:
  - Maintainer: Marc Lallaouret <mlallaouret@gmail.com>
    Name: maintainer
  - CmdLine:
    - apt-get update     && apt-get install flex bison -y     && apt-get clean
    Name: run
    PrependShell: true
  - CmdLine:
    - wget http://www.tcpdump.org/release/libpcap-1.7.4.tar.gz && tar xzf libpcap-1.7.4.tar.gz     &&
      cd libpcap-1.7.4     && ./configure && make install
    Name: run
    PrependShell: true
  From:
    Image: golang:1.6
  Name: ""
  Platform: ""
  SourceCode: FROM golang:1.6
