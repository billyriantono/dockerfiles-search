MetaArgs: null
Stages:
- BaseName: normisg/symfony-ci-base:latest
  Commands:
  - CmdLine:
    - wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | apt-key add
      -     && echo "deb http://dl.google.com/linux/chrome/deb/ stable main" | tee
      /etc/apt/sources.list.d/google-chrome.list     && apt-get update && apt-get
      install -y google-chrome-stable rabbitmq-server     && apt-get clean     &&
      rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - ./docker/
    - /
  From:
    Image: normisg/symfony-ci-base:latest
  Name: ""
  Platform: ""
  SourceCode: FROM normisg/symfony-ci-base:latest
