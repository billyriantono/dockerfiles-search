MetaArgs: null
Stages:
- BaseName: emiketic/essentials:latest
  Commands:
  - CmdLine:
    - apk add --no-cache --update         build-base         python2 py2-pip         php7         nodejs
      npm         go  && pip install --upgrade pip  && npm -g install npm ;
    Name: run
    PrependShell: true
  - CmdLine:
    - git clone git://github.com/apiaryio/drafter.git  && cd ./drafter  && git checkout
      v0.1.9  && git submodule update --init --recursive  && ./configure  && make  &&
      make install  && cd ..  && rm -fr ./drafter ;
    Name: run
    PrependShell: true
  - CmdLine:
    - wget https://github.com/pixelfusion/blueman/releases/download/1.3.1/blueman.phar
      -O /usr/local/bin/blueman  && chmod u+x /usr/local/bin/blueman ;
    Name: run
    PrependShell: true
  - CmdLine:
    - pip install apiary2postman
    Name: run
    PrependShell: true
  - CmdLine:
    - wget https://github.com/bukalapak/snowboard/releases/download/v1.7.0/snowboard-v1.7.0.linux-amd64.tar.gz
      -O snowboard.tar.gz  && tar -zxvf snowboard.tar.gz  && cp ./snowboard /usr/local/bin/snowboard  &&
      rm -f snowboard* ;
    Name: run
    PrependShell: true
  - CmdLine:
    - npm install -g --unsafe-perm aglio
    Name: run
    PrependShell: true
  - CmdLine:
    - rm -fr ~/.cache/pip && npm cache clean -f
    Name: run
    PrependShell: true
  From:
    Image: emiketic/essentials:latest
  Name: ""
  Platform: ""
  SourceCode: FROM emiketic/essentials:latest
