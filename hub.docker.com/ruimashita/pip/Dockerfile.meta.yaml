MetaArgs: null
Stages:
- BaseName: ruimashita/python
  Commands:
  - Maintainer: takuya.wakisaka@moldweorp.com
    Name: maintainer
  - CmdLine:
    - wget https://bootstrap.pypa.io/get-pip.py && python get-pip.py && rm get-pip.py
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y --no-install-recommends     libffi-dev     libssl-dev     &&
      apt-get clean     && rm -rf /var/lib/apt/lists/     && pip install --no-cache-dir
      ndg-httpsclient
    Name: run
    PrependShell: true
  From:
    Image: ruimashita/python
  Name: ""
  Platform: ""
  SourceCode: FROM ruimashita/python
