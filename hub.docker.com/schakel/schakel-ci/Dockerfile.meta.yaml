MetaArgs: null
Stages:
- BaseName: php:7.2
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y     sqlite     zlib1g-dev     libicu-dev     libmagickwand-dev     gnupg     libssl-dev     libcurl4-openssl-dev     pkg-config
    Name: run
    PrependShell: true
  - CmdLine:
    - pecl install imagick     && docker-php-ext-enable imagick     && docker-php-ext-install
      zip     && docker-php-ext-install curl     && docker-php-ext-install pdo_mysql     &&
      docker-php-ext-install json     && docker-php-ext-install intl
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "memory_limit = -1;" > $PHP_INI_DIR/conf.d/memory_limit.ini
    Name: run
    PrependShell: true
  - Env:
    - Key: COMPOSER_ALLOW_SUPERUSER
      Value: "1"
    Name: env
  - CmdLine:
    - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin
      --filename=composer
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sL https://deb.nodesource.com/setup_10.x | bash && apt-get install -y
      nodejs
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get clean
    Name: run
    PrependShell: true
  - CmdLine:
    - npm install -g yarn
    Name: run
    PrependShell: true
  From:
    Image: php:7.2
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.2
