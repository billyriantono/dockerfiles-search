MetaArgs: null
Stages:
- BaseName: circleci/python:3.6-node
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"Kitware, Inc. <kitware@kitware.com>"'
    Name: label
  - Name: user
    User: root
  - CmdLine:
    - apt-get update   && apt-get install --assume-yes     libldap2-dev     libsasl2-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update   && apt-get install --assume-yes     cmake   && git clone "https://github.com/universal-ctags/ctags.git"
      "./ctags"   && cd ./ctags   && ./autogen.sh   && ./configure   && make   &&
      make install   && cd ..   && rm -rf ./ctags   && pip3 install --no-cache --upgrade     pip     setuptools     tox
    Name: run
    PrependShell: true
  - Name: user
    User: circleci
  From:
    Image: circleci/python:3.6-node
  Name: ""
  Platform: ""
  SourceCode: FROM circleci/python:3.6-node
