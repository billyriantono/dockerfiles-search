MetaArgs: null
Stages:
- BaseName: duruo850/base
  Commands:
  - CmdLine:
    - apt-get update  && DEBIAN_FRONTEND=noninteractive apt-get install -y wget &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - DEBIAN_FRONTEND=noninteractive &&     cd /tmp &&     wget http://www.rabbitmq.com/rabbitmq-signing-key-public.asc
      &&     apt-key add rabbitmq-signing-key-public.asc &&     echo "deb http://www.rabbitmq.com/debian/
      testing main" | tee /etc/apt/sources.list.d/rabbitmq.list &&     apt-get update
      &&     apt-get install -y --force-yes rabbitmq-server &&     rabbitmq-plugins
      enable rabbitmq_management &&     service rabbitmq-server stop &&     apt-get
      clean &&     rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update  && DEBIAN_FRONTEND=noninteractive apt-get install -y psmisc
      && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - run
    - /usr/local/bin/run
  - Chown: ""
    Name: add
    SourcesAndDest:
    - first_run
    - /usr/local/bin/first_run
  - CmdLine:
    - chmod +x /usr/local/bin/*
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "5672"
  - Name: expose
    Ports:
    - "15672"
  - Name: volume
    Volumes:
    - /var/log/rabbitmq
  - CmdLine:
    - /usr/local/bin/run
    Name: cmd
    PrependShell: false
  From:
    Image: duruo850/base
  Name: ""
  Platform: ""
  SourceCode: FROM duruo850/base
