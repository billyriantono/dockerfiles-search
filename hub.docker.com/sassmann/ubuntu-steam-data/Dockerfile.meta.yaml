MetaArgs: null
Stages:
- BaseName: ubuntu:18.04
  Commands:
  - Env:
    - Key: user
      Value: steam
    Name: env
  - Env:
    - Key: data
      Value: /home/steam
    Name: env
  - CmdLine:
    - groupadd $user && useradd -s /bin/false -g $user $user
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir -p $data && chown -R $user:$user $data
    Name: run
    PrependShell: true
  - Name: user
    User: steam
  - CmdLine:
    - echo "<driconf></driconf>" > /home/$user/.drirc
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - $data
  From:
    Image: ubuntu:18.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:18.04
