FROM debian:stable

LABEL maintainer="oprax@me.com" 

RUN apt-get update && apt-get upgrade -y
RUN apt-get install -y gnupg openssl openssh-client git ca-certificates curl wget lsb-release apt-transport-https apt-utils

