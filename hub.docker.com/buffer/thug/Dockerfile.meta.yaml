MetaArgs: null
Stages:
- BaseName: ubuntu:18.04
  Commands:
  - Maintainer: Lenny Zeltser (@lennyzeltser, www.zeltser.com)
    Name: maintainer
  - Name: user
    User: root
  - CmdLine:
    - apt-get update &&   apt-get install -y --no-install-recommends     build-essential     sudo     python     python3     python3-dev     python3-setuptools     python3-wheel     libboost-dev     libboost-python-dev     libboost-system-dev     python3-pip     libxml2-dev     libxslt-dev     git     libtool     graphviz-dev     automake     libffi-dev     graphviz     libfuzzy-dev     libfuzzy2     libjpeg-dev     libffi-dev     pkg-config     autoconf
      &&   rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - pip3 install --upgrade pip
    Name: run
    PrependShell: true
  - CmdLine:
    - pip3 install --upgrade setuptools
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /home
  - CmdLine:
    - git clone https://github.com/buffer/thug.git &&   pip3 install thug/.travis/stpyv8-7.9.317.31-cp36-cp36m-linux_x86_64.whl
      &&   rm -rf thug
    Name: run
    PrependShell: true
  - CmdLine:
    - git clone https://github.com/buffer/libemu.git &&   cd libemu &&   autoreconf
      -v -i &&   ./configure &&   make install &&   cd .. &&   rm -rf libemu
    Name: run
    PrependShell: true
  - CmdLine:
    - ldconfig
    Name: run
    PrependShell: true
  - CmdLine:
    - pip3 install thug
    Name: run
    PrependShell: true
  - CmdLine:
    - groupadd -r thug &&   useradd -r -g thug -d /home/thug -s /sbin/nologin -c "Thug
      User" thug &&   mkdir -p /home/thug /tmp/thug/logs &&   chown -R thug:thug /home/thug
      /tmp/thug/logs
    Name: run
    PrependShell: true
  - Name: user
    User: thug
  - Env:
    - Key: HOME
      Value: /home/thug
    Name: env
  - Env:
    - Key: USER
      Value: thug
    Name: env
  - Name: workdir
    Path: /home/thug
  - Name: volume
    Volumes:
    - /tmp/thug/logs
  - CmdLine:
    - thug
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:18.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:18.04
