MetaArgs: null
Stages:
- BaseName: alpine:3.6
  Commands:
  - CmdLine:
    - apk --no-cache add php5-apache2 phpldapadmin=1.2.3-r4
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir -p /run/apache2 &&    rm -rf /var/www/localhost/htdocs &&    ln -sf /usr/share/webapps/phpldapadmin/htdocs/
      /var/www/localhost/ &&    cp /etc/phpldapadmin/config.php.example /etc/phpldapadmin/config.php
      &&    sed -i 's#AllowOverride none#AllowOverride All#' /etc/apache2/httpd.conf
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "80"
  - CmdLine:
    - httpd
    - -D
    - FOREGROUND
    Name: cmd
    PrependShell: false
  From:
    Image: alpine:3.6
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.6
