MetaArgs: null
Stages:
- BaseName: alpine:latest
  Commands:
  - Maintainer: oh@bootjp.me
    Name: maintainer
  - CmdLine:
    - apk add --update alpine-sdk openssl-dev && apk add --no-cache git && git clone
      https://github.com/giltene/wrk2.git && cd wrk2 && make && mv wrk /bin/
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/sh
    - -c
    - while sleep 86400; do :; done
    Name: entrypoint
    PrependShell: false
  From:
    Image: alpine:latest
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:latest
