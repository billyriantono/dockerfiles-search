MetaArgs: null
Stages:
- BaseName: jangrewe/gitlab-ci-android
  Commands:
  - CmdLine:
    - curl -sL https://deb.nodesource.com/setup_8.x -o nodesource_setup.sh && bash
      nodesource_setup.sh && apt update && apt install -y nodejs && curl https://install.meteor.com/
      | sh
    Name: run
    PrependShell: true
  From:
    Image: jangrewe/gitlab-ci-android
  Name: ""
  Platform: ""
  SourceCode: FROM jangrewe/gitlab-ci-android
