MetaArgs: null
Stages:
- BaseName: nvidia/cuda:8.0-cudnn6-devel-ubuntu16.04
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y       build-essential       cmake       git       rpm       libatlas-base-dev       libatlas-dev       libboost-all-dev       libgflags-dev       libgoogle-glog-dev       libhdf5-dev       libleveldb-dev       liblmdb-dev       libopencv-dev       libprotobuf-dev       libsnappy-dev       lsb-release       protobuf-compiler       python-dev       python-numpy       python-pip       python-setuptools       python-scipy       sudo       wget     &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - pip install --upgrade pip
    Name: run
    PrependShell: true
  - CmdLine:
    - git clone https://github.com/CMU-Perceptual-Computing-Lab/openpose     && cd
      openpose && mkdir -p build && cd build     && cmake .. && make -j"$(nproc)"
    Name: run
    PrependShell: true
  - CmdLine:
    - cd openpose/build && make install
    Name: run
    PrependShell: true
  - CmdLine:
    - git clone https://github.com/gflags/gflags.git     && cd gflags && mkdir -p
      build && cd build     && cmake .. && make -j"$(nproc)" && make install
    Name: run
    PrependShell: true
  - CmdLine:
    - cd / && git clone https://github.com/chriamue/dressup.git && cd dressup && mkdir
      build && cd build && cmake -DOpenPose=/openpose/build/cmake .. && make
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /dressup/build
  - CmdLine:
    - ./dressup
    Name: cmd
    PrependShell: true
  From:
    Image: nvidia/cuda:8.0-cudnn6-devel-ubuntu16.04
  Name: ""
  Platform: ""
  SourceCode: FROM nvidia/cuda:8.0-cudnn6-devel-ubuntu16.04
