MetaArgs: null
Stages:
- As: builder
  BaseName: napnap75/rpi-alpine-base:latest
  Commands:
  - CmdLine:
    - "apk add --no-cache curl \t&& while [ \"$DOWNLOAD_URL\" == \"\" ] ; do DOWNLOAD_URL=$(curl
      -s https://api.github.com/repos/restic/restic/releases/latest | grep \"browser_download_url\"
      | grep \"linux_arm\\.\" | cut -d\\\" -f4) ; done \t&& curl --retry 3 -L -o restic.bz2
      ${DOWNLOAD_URL} \t&& bunzip2 restic.bz2 \t&& chmod +x restic"
    Name: run
    PrependShell: true
  From:
    Image: napnap75/rpi-alpine-base:latest
  Name: builder
  Platform: ""
  SourceCode: FROM napnap75/rpi-alpine-base:latest as builder
- BaseName: napnap75/rpi-alpine-base:latest
  Commands:
  - CmdLine:
    - apk add --no-cache curl jq openssh-client
    Name: run
    PrependShell: true
  - Chown: ""
    From: builder
    Name: copy
    SourcesAndDest:
    - restic
    - /usr/bin
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - restic
    - /usr/local/bin
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - docker-backup.sh
    - /usr/bin
  - CmdLine:
    - /usr/bin/docker-backup.sh
    Name: cmd
    PrependShell: false
  From:
    Image: napnap75/rpi-alpine-base:latest
  Name: ""
  Platform: ""
  SourceCode: FROM napnap75/rpi-alpine-base:latest
