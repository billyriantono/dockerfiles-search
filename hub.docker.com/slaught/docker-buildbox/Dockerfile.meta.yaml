MetaArgs: null
Stages:
- BaseName: debian:stretch
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y --no-install-recommends     git                 ssh                 tar                 gzip                ca-certificates     build-essential     autoconf            automake            bzip2               file                libtool             make                patch               xz-utils            procps              curl                wget                sudo                gpg                 dirmngr           &&
      apt-get clean    && update-ca-certificates
    Name: run
    PrependShell: true
  From:
    Image: debian:stretch
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch
