MetaArgs: null
Stages:
- BaseName: continuumio/miniconda3
  Commands:
  - CmdLine:
    - "/opt/conda/bin/conda config --add channels conda-forge     && /opt/conda/bin/conda
      upgrade conda -y --quiet     && /opt/conda/bin/conda upgrade --all -y --quiet
      \t&& /opt/conda/bin/conda install \t\t\t\tjupyter \t\t\t\tipywidgets \t\t\t\tbqplot
      \t\t\t\tipyvolume \t\t\t\tipyleaflet \t\t\t\tpythreejs \t\t\t\tipyevents \t\t\t-y
      --quiet \t&& /opt/conda/bin/conda install -c wwt pywwt -y --quiet \t&& /opt/conda/bin/conda
      update --all -y --quiet \t&& /opt/conda/bin/ipython kernel install --name widgets-tutorial
      --display-name widgets-tutorial --sys-prefix \t&& mkdir /opt/notebooks \t&&
      cd /opt/notebooks \t&& git clone https://github.com/jupyter-widgets/tutorial.git"
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - 8888/tcp
  - CmdLine:
    - /opt/conda/bin/jupyter
    - notebook
    - --allow-root
    - --notebook-dir='/opt/notebooks'
    - --ip=0.0.0.0
    - --port=8888
    - --no-browser
    Name: cmd
    PrependShell: false
  From:
    Image: continuumio/miniconda3
  Name: ""
  Platform: ""
  SourceCode: FROM continuumio/miniconda3
