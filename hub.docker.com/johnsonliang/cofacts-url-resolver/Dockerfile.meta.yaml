MetaArgs: null
Stages:
- BaseName: node:10-slim
  Commands:
  - CmdLine:
    - apt-get update && apt-get -yq upgrade && apt-get install   && apt-get autoremove
      && apt-get autoclean
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y wget --no-install-recommends     && wget
      -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | apt-key add
      -     && sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/
      stable main" >> /etc/apt/sources.list.d/google.list'     && apt-get update     &&
      apt-get install -y google-chrome-unstable fonts-ipafont-gothic fonts-wqy-zenhei
      fonts-thai-tlwg fonts-kacst ttf-freefont       --no-install-recommends     &&
      rm -rf /var/lib/apt/lists/*     && apt-get purge --auto-remove -y curl     &&
      rm -rf /src/*.deb
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - https://github.com/Yelp/dumb-init/releases/download/v1.2.1/dumb-init_1.2.1_amd64
    - /usr/local/bin/dumb-init
  - CmdLine:
    - chmod +x /usr/local/bin/dumb-init
    Name: run
    PrependShell: true
  - CmdLine:
    - groupadd -r pptruser && useradd -r -g pptruser -G audio,video pptruser     &&
      mkdir -p /home/pptruser/Downloads     && chown -R pptruser:pptruser /home/pptruser
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /srv/www
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - package.json
    - package-lock.json
    - ./
  - CmdLine:
    - npm install --production
    Name: run
    PrependShell: true
  - CmdLine:
    - npm i puppeteer@1.8.0
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - .
    - .
  - CmdLine:
    - npm run compile
    Name: run
    PrependShell: true
  - Name: user
    User: pptruser
  - CmdLine:
    - dumb-init
    - --
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - node
    - index.js
    Name: cmd
    PrependShell: false
  From:
    Image: node:10-slim
  Name: ""
  Platform: ""
  SourceCode: FROM node:10-slim
