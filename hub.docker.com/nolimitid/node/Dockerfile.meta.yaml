MetaArgs: null
Stages:
- BaseName: nodesource/trusty:0.12.7
  Commands:
  - Maintainer: maman <achmad@mahardi.me>
    Name: maintainer
  - Env:
    - Key: NODE_ENV
      Value: development
    Name: env
  - CmdLine:
    - apt-get update &&   apt-get upgrade -y --force-yes &&   apt-get install -y --force-yes
      build-essential curl ca-certificates bzip2 rsync ssh &&   apt-get autoremove
      -y &&   apt-get clean all
    Name: run
    PrependShell: true
  From:
    Image: nodesource/trusty:0.12.7
  Name: ""
  Platform: ""
  SourceCode: FROM nodesource/trusty:0.12.7
