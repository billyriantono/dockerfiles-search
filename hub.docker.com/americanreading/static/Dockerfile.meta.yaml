MetaArgs: null
Stages:
- BaseName: nginx:1.17
  Commands:
  - CmdLine:
    - apt-get update &&   apt-get -y install     authbind &&   apt-get clean &&   rm
      -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir /usr/local/src/app &&   touch /var/run/nginx.pid &&   touch /etc/authbind/byport/80
      &&   chown www-data:www-data /etc/authbind/byport/80 &&   chmod 500 /etc/authbind/byport/80
      &&   chown -R www-data:www-data /var/run/nginx.pid &&   chown -R www-data:www-data
      /var/cache/nginx &&   chown -R www-data:www-data /etc/nginx &&   chown -R www-data:www-data
      /usr/share/nginx/html
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - ./nginx.conf
    - /etc/nginx/nginx.conf
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - ./site.conf
    - /etc/nginx/conf.d/default.conf
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - ./entrypoint
    - /usr/local/bin/entrypoint
  - Name: user
    User: www-data
  - CmdLine:
    - entrypoint
    Name: entrypoint
    PrependShell: false
  From:
    Image: nginx:1.17
  Name: ""
  Platform: ""
  SourceCode: FROM nginx:1.17
