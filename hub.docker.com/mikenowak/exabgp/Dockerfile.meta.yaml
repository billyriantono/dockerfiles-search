MetaArgs: null
Stages:
- BaseName: alpine:3.7
  Commands:
  - Maintainer: Mike Nowak <https://github.com/mikenowak>
    Name: maintainer
  - CmdLine:
    - apk --no-cache add wget curl python python-dev py-setuptools coreutils netcat-openbsd
      bash     && apk --no-cache add --virtual build-dependencies build-base py-pip      &&
      mkdir -p /usr/etc/exabgp     && pip install ipaddr exabgp ipy requests     &&
      apk del build-dependencies
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - entrypoint.sh
    - /
  - Chown: ""
    Name: add
    SourcesAndDest:
    - exabgp.conf.example
    - /usr/etc/exabgp/
  - CmdLine:
    - /entrypoint.sh
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - exabgp
    Name: cmd
    PrependShell: false
  - Name: volume
    Volumes:
    - /usr/etc/exabgp
  - Name: expose
    Ports:
    - "179"
  From:
    Image: alpine:3.7
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.7
