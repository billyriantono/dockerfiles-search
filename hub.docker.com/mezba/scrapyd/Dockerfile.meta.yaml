MetaArgs: null
Stages:
- BaseName: ubuntu:18.04
  Commands:
  - CmdLine:
    - set -xe     && apt-get update     && apt-get install -y python3                           python3-dev                           python3-pip                           libxml2-dev                           libxslt1-dev                           zlib1g-dev                           libffi-dev                           libssl-dev     &&
      pip3 install scrapyd==1.2.1     && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - ./scrapyd.conf
    - /etc/scrapyd/
  - Name: volume
    Volumes:
    - /etc/scrapyd/
    - /var/lib/scrapyd/
  - Name: expose
    Ports:
    - "6800"
  - CmdLine:
    - scrapyd
    - --pidfile=
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:18.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:18.04
