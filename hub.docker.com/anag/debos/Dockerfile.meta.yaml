MetaArgs: null
Stages:
- BaseName: debian:stretch-slim
  Commands:
  - Key: DEBIAN_FRONTEND
    Name: arg
    Value: noninteractive
  - CmdLine:
    - apt-get update && apt-get install --no-install-recommends -y procps
    Name: run
    PrependShell: true
  - Env:
    - Key: HOME
      Value: /tmp
    Name: env
  - Env:
    - Key: GOPATH
      Value: /usr/local/go
    Name: env
  - Env:
    - Key: PATH
      Value: $PATH:/usr/local/go/bin
    Name: env
  - CmdLine:
    - apt-get update && apt-get install --no-install-recommends -y     apt-transport-https     ca-certificates
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install --no-install-recommends -y     golang-go     git     libostree-dev     gcc     libc6-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - go get github.com/go-debos/debos/cmd/debos &&     go install github.com/go-debos/debos/cmd/debos
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install --no-install-recommends -y     debootstrap     pkg-config     busybox     linux-image-amd64     qemu-system-x86     qemu-user-static     systemd-container     binfmt-support     parted     dosfstools     e2fsprogs
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install --no-install-recommends -y     python     python-requests     xz-utils
    Name: run
    PrependShell: true
  From:
    Image: debian:stretch-slim
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch-slim
