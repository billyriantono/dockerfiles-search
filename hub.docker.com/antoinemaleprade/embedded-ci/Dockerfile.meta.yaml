MetaArgs: null
Stages:
- BaseName: ubuntu:latest
  Commands:
  - CmdLine:
    - apt-get update && apt-get upgrade -y
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get install -y         git         python         python-pip         build-essential         cmake
    Name: run
    PrependShell: true
  - CmdLine:
    - pip install -q     platformio
    Name: run
    PrependShell: true
  - CmdLine:
    - git clone https://github.com/uncrustify/uncrustify.git &&     cd uncrustify
      &&     mkdir build &&     cd build/ &&     cmake .. &&     cmake --build . &&     cp
      ./uncrustify /usr/bin/
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:latest
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:latest
