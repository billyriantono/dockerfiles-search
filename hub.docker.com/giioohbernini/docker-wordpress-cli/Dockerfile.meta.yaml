MetaArgs: null
Stages:
- BaseName: wordpress:latest
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - uploads.ini
    - /usr/local/etc/php/conf.d/uploads.ini
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - wp-su.sh
    - /bin/wp
  - CmdLine:
    - rm -rf /usr/local/etc/php/conf.d/opcache-recommended.ini &&   apt-get update
      && apt-get install -y sudo less &&   curl -o /bin/wp-cli.phar https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar
      &&   chmod +x /bin/wp-cli.phar /bin/wp &&   apt-get clean &&   rm -rf /var/lib/apt/lists/*
      /tmp/* /var/tmp/*
    Name: run
    PrependShell: true
  From:
    Image: wordpress:latest
  Name: ""
  Platform: ""
  SourceCode: FROM wordpress:latest
