MetaArgs: null
Stages:
- BaseName: fluent/fluentd:latest
  Commands:
  - Maintainer: Stéphie <stephanie.chhim@onepark.fr>
    Name: maintainer
  - CmdLine:
    - apk add --update --virtual .build-deps         sudo build-base ruby-dev   &&
      sudo gem install \fluent-plugin-s3   && sudo gem sources --clear-all    && apk
      del .build-deps    && rm -rf /var/cache/apk/*              /home/fluent/.gem/ruby/2.3.0/cache/*.gem
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "24224"
  - CmdLine:
    - mkdir -p /var/log/fluent
    Name: run
    PrependShell: true
  - CmdLine:
    - chmod 0777 /var/log/fluent
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir /conf
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /conf
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - conf/fluent.conf
    - fluent.conf
  - CmdLine:
    - fluentd -c fluent.conf
    Name: cmd
    PrependShell: true
  From:
    Image: fluent/fluentd:latest
  Name: ""
  Platform: ""
  SourceCode: FROM fluent/fluentd:latest
