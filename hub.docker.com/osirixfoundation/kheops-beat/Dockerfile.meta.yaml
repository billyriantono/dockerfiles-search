MetaArgs: null
Stages:
- BaseName: alpine:3.6
  Commands:
  - Name: workdir
    Path: /install
  - Env:
    - Key: FILEBEAT_VERSION
      Value: 7.1.1
    Name: env
  - Env:
    - Key: METRICBEAT_VERSION
      Value: 7.1.1
    Name: env
  - CmdLine:
    - apk add --no-cache curl
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir deb
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -L -O https://artifacts.elastic.co/downloads/beats/metricbeat/metricbeat-${METRICBEAT_VERSION}-amd64.deb
    Name: run
    PrependShell: true
  - CmdLine:
    - mv metricbeat-${METRICBEAT_VERSION}-amd64.deb deb/metricbeat-amd64.deb
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -L -O https://artifacts.elastic.co/downloads/beats/filebeat/filebeat-${FILEBEAT_VERSION}-amd64.deb
    Name: run
    PrependShell: true
  - CmdLine:
    - mv filebeat-${FILEBEAT_VERSION}-amd64.deb deb/filebeat-amd64.deb
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir rpm
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -L -O https://artifacts.elastic.co/downloads/beats/metricbeat/metricbeat-${METRICBEAT_VERSION}-x86_64.rpm
    Name: run
    PrependShell: true
  - CmdLine:
    - mv metricbeat-${METRICBEAT_VERSION}-x86_64.rpm rpm/metricbeat-x86_64.rpm
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -L -O https://artifacts.elastic.co/downloads/beats/filebeat/filebeat-${FILEBEAT_VERSION}-x86_64.rpm
    Name: run
    PrependShell: true
  - CmdLine:
    - mv filebeat-${FILEBEAT_VERSION}-x86_64.rpm rpm/filebeat-x86_64.rpm
    Name: run
    PrependShell: true
  From:
    Image: alpine:3.6
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.6
