MetaArgs: null
Stages:
- BaseName: fluent/fluentd:edge
  Commands:
  - CmdLine:
    - apk update     && apk upgrade     && apk add --no-cache ca-certificates ruby
      ruby-irb     && apk add --no-cache --virtual .build-deps build-base ruby-dev
      curl-dev libffi-dev     && fluent-gem install fluent-plugin-mongo     && gem
      install fluent-plugin-systemd -v 0.2.0
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "24224"
    - "5140"
  - CmdLine:
    - exec fluentd -c /fluentd/etc/$FLUENTD_CONF -p /fluentd/plugins $FLUENTD_OPT
    Name: cmd
    PrependShell: true
  From:
    Image: fluent/fluentd:edge
  Name: ""
  Platform: ""
  SourceCode: FROM fluent/fluentd:edge
