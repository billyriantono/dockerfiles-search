MetaArgs: null
Stages:
- BaseName: ubuntu:16.04
  Commands:
  - CmdLine:
    - "apt-get update && \tapt-get -y install openssh-server python && \techo 'root:root'
      |chpasswd && \tsed -ri 's/^#?PermitRootLogin\\s+.*/PermitRootLogin yes/' /etc/ssh/sshd_config
      && \tsed -ri 's/UsePAM yes/#UsePAM yes/g' /etc/ssh/sshd_config && \tmkdir /root/.ssh
      && \tmkdir /var/run/sshd && \tapt-get clean &&     rm -rf /var/lib/apt/lists/*
      /tmp/* /var/tmp/*"
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "22"
  - CmdLine:
    - /usr/sbin/sshd
    - -D
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:16.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:16.04
