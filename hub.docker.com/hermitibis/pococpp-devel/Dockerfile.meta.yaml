MetaArgs: null
Stages:
- BaseName: centos:7
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"Chen Di <di.chen16@outlook.com>"'
    Name: label
  - CmdLine:
    - yum -y groupinstall "Development Tools"
    Name: run
    PrependShell: true
  - CmdLine:
    - yum -y install unixODBC unixODBC-devel wxBase wxGTK SDL wxGTK-gl
    Name: run
    PrependShell: true
  - CmdLine:
    - yum -y install gcc glibc-devel make ncurses-devel openssl-devel xmlto perl wget
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /app
  - CmdLine:
    - "wget https://pocoproject.org/releases/poco-1.9.2/poco-1.9.2-all.tar.gz && \t\ttar
      xvf poco-1.9.2-all.tar.gz &&         cd poco-1.9.2-all/ &&         ./configure
      --omit=Data/ODBC,Data/MySQL &&         make && make install &&         cd ../
      && rm -rf poco-1.9.2-all/ && \trm -rf  poco-1.9.2-all.tar.gz"
    Name: run
    PrependShell: true
  From:
    Image: centos:7
  Name: ""
  Platform: ""
  SourceCode: FROM centos:7
