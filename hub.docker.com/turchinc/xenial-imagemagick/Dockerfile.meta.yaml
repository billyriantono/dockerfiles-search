MetaArgs: null
Stages:
- BaseName: turchinc/xenial-libreoffice
  Commands:
  - Labels:
    - Key: tag
      Value: '"turchinc/xenial-imagemagick"'
    - Key: vendor
      Value: '"Bertsch Innovation GmbH"'
    Name: label
  - Maintainer: Chris Turchin <chris.turchin@bertschinnovation.com>
    Name: maintainer
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - CmdLine:
    - "apt-get update && \tapt-get install -y wget dcraw ffmpeg ghostscript libmagick++-dev"
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /tmp && wget http://www.imagemagick.org/download/ImageMagick.tar.gz
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /tmp && tar xzf ImageMagick.tar.gz
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /tmp/ImageMagick* && ./configure && make &&  make install && ldconfig /usr/local
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - policy.xml
    - /etc/ImageMagick-6/policy.xml
  - CmdLine:
    - "apt-get autoclean && apt-get --purge -y autoremove && \t\tapt-get clean &&
      rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*"
    Name: run
    PrependShell: true
  From:
    Image: turchinc/xenial-libreoffice
  Name: ""
  Platform: ""
  SourceCode: FROM turchinc/xenial-libreoffice
