MetaArgs: null
Stages:
- BaseName: golang:alpine
  Commands:
  - CmdLine:
    - apk add --no-cache git protobuf make openssh-client build-base postgresql-client
    Name: run
    PrependShell: true
  - CmdLine:
    - apk add --no-cache --virtual build-dependencies unzip wget     && wget -O -
      -q https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh
      | sh -s -- -b /usr/bin v1.19.1     && apk del build-dependencies     && go version     &&
      golangci-lint --version
    Name: run
    PrependShell: true
  From:
    Image: golang:alpine
  Name: ""
  Platform: ""
  SourceCode: FROM golang:alpine
