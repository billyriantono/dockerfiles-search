MetaArgs: null
Stages:
- BaseName: dennis1f/docker-amd64-toolchain:base
  Commands:
  - Labels:
    - Key: Description
      Value: '"Foo"'
    - Key: Vendor
      Value: '"Foo"'
    - Key: Version
      Value: '"1.0"'
    Name: label
  - CmdLine:
    - apt-get update   && apt-get install --no-install-recommends -y       libpulse-dev   &&
      apt-get autoremove -y   && apt-get clean   && rm -rf /var/lib/apt/lists*   &&
      rm -rf /tmp/* /var/tmp/*   && rm -rf /usr/share/man/*
    Name: run
    PrependShell: true
  From:
    Image: dennis1f/docker-amd64-toolchain:base
  Name: ""
  Platform: ""
  SourceCode: FROM dennis1f/docker-amd64-toolchain:base
