FROM maven:3.6.0-jdk-8
RUN apt-get install -y curl && curl -sL https://deb.nodesource.com/setup_9.x | bash - \
                              && apt-get install -y nodejs \
                              && curl -L https://www.npmjs.com/install.sh | sh
