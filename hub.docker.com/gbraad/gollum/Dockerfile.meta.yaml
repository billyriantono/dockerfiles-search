MetaArgs: null
Stages:
- BaseName: registry.gitlab.com/gbraad/fedora:24
  Commands:
  - Maintainer: Gerard Braad <me@gbraad.nl>
    Name: maintainer
  - CmdLine:
    - dnf install -y ruby libicu git ;     dnf install -y ruby-devel @development-tools
      @rpm-development-tools @c-development libicu-devel zlib-devel ;     gem install
      gollum --no-ri --no-rdoc ;     dnf history -y undo 3 ;     dnf clean all ;     mkdir
      -p /workspace
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "4567"
  - Name: volume
    Volumes:
    - /workspace
  - Name: workdir
    Path: /workspace
  - CmdLine:
    - gollum
    Name: cmd
    PrependShell: true
  From:
    Image: registry.gitlab.com/gbraad/fedora:24
  Name: ""
  Platform: ""
  SourceCode: FROM registry.gitlab.com/gbraad/fedora:24
