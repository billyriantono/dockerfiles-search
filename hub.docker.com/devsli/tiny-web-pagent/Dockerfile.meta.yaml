MetaArgs: null
Stages:
- BaseName: circleci/php:7.2.8-fpm-node-browsers
  Commands:
  - CmdLine:
    - sudo apt-get update && sudo apt install -y libpng-dev libzip-dev default-libmysqlclient-dev
      curl apt-transport-https
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo docker-php-ext-install gd zip pdo_mysql mysqli
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo apt-get install -y libfreetype6-dev libjpeg62-turbo-dev libpng-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo docker-php-ext-install -j$(nproc) iconv
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo docker-php-ext-configure gd --with-freetype-dir=/usr/include/ --with-jpeg-dir=/usr/include/
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo docker-php-ext-install -j$(nproc) gd
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo docker-php-ext-install -j$(nproc) pcntl
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo docker-php-ext-install -j$(nproc) exif
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo chmod 777 /opt
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo curl https://packages.microsoft.com/keys/microsoft.asc | sudo apt-key add
      - &&     sudo sh  -c 'curl https://packages.microsoft.com/config/debian/8/prod.list
      > /etc/apt/sources.list.d/mssql-release.list' &&     sudo apt-get install -y
      locales &&     sudo sh -c 'echo "en_US.UTF-8 UTF-8" > /etc/locale.gen' &&     sudo
      locale-gen &&     sudo apt-get update &&     sudo ACCEPT_EULA=Y apt-get install
      msodbcsql &&     sudo apt-get install unixodbc-dev &&     sudo pecl install
      sqlsrv &&     sudo pecl install pdo_sqlsrv &&     sudo docker-php-ext-enable
      sqlsrv pdo_sqlsrv
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "deb http://deb.debian.org/debian jessie main" | sudo tee -a /etc/apt/sources.list
      &&     sudo apt-get update &&     sudo apt-get install libssl1.0.0
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash
    Name: entrypoint
    PrependShell: false
  From:
    Image: circleci/php:7.2.8-fpm-node-browsers
  Name: ""
  Platform: ""
  SourceCode: FROM circleci/php:7.2.8-fpm-node-browsers
