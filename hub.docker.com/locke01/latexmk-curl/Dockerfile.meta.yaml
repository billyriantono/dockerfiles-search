MetaArgs: null
Stages:
- BaseName: tianon/latex
  Commands:
  - Maintainer: locke01
    Name: maintainer
  - CmdLine:
    - "apt-get update && apt-get install -y curl && \tapt-get autoclean && apt-get
      clean && \trm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*"
    Name: run
    PrependShell: true
  From:
    Image: tianon/latex
  Name: ""
  Platform: ""
  SourceCode: FROM tianon/latex
