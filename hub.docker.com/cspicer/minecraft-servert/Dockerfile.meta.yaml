MetaArgs: null
Stages:
- BaseName: alpine:3.6
  Commands:
  - CmdLine:
    - apk --no-cache add     python     python-dev     py-pip     openssl-dev     py-virtualenv     gcc     libffi-dev     musl-dev     jpeg-dev     make     libressl     linux-headers     git     libevent-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - virtualenv -p python2.7 /synapse &&    source /synapse/bin/activate &&    pip
      install --upgrade pip &&    pip install --upgrade setuptools &&    pip install
      https://github.com/matrix-org/synapse/tarball/master
    Name: run
    PrependShell: true
  - CmdLine:
    - git clone https://github.com/coturn/coturn.git /coturn &&    cd /coturn &&    ./configure
      &&    make &&    make install
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - scripts/run.sh
    - /
  - CmdLine:
    - /run.sh
    Name: entrypoint
    PrependShell: false
  From:
    Image: alpine:3.6
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.6
