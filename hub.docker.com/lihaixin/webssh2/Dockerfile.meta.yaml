MetaArgs: null
Stages:
- BaseName: node:8.6-alpine
  Commands:
  - CmdLine:
    - "apk update  && \tapk add --no-cache --update git  && \tgit clone https://github.com/billchurch/WebSSH2.git
      && \tcd WebSSH2/ && \tcp -r app/ /usr/src/ && \trm -rf WebSSH2/ && \tcd /usr/src/
      \   && \tapk del git && \tnpm install --production"
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /usr/src
  - Name: expose
    Ports:
    - "2222"
  - CmdLine:
    - npm run start
    Name: cmd
    PrependShell: true
  From:
    Image: node:8.6-alpine
  Name: ""
  Platform: ""
  SourceCode: FROM node:8.6-alpine
