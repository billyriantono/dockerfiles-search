MetaArgs: null
Stages:
- BaseName: ubuntu
  Commands:
  - CmdLine:
    - apt-get update -y     && apt-get install -y     git     python-pip     python-numpy     python-dev
      cmake     zlib1g-dev     libjpeg-dev     xvfb     libav-tools     xorg-dev     python-opengl     libboost-all-dev     libsdl2-dev     swig
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /usr/src     && git clone https://github.com/openai/gym.git     && cd gym     &&
      pip install -e .
    Name: run
    PrependShell: true
  From:
    Image: ubuntu
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu
