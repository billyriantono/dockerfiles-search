MetaArgs: null
Stages:
- BaseName: php:7.0.13-apache
  Commands:
  - Maintainer: Suparit Krityakien <suparit@wongnai.com>
    Name: maintainer
  - CmdLine:
    - a2enmod rewrite expires headers
    Name: run
    PrependShell: true
  - CmdLine:
    - "apt-get update && apt-get install -y libpng12-dev libjpeg-dev && rm -rf /var/lib/apt/lists/*
      \t&& docker-php-ext-configure gd --with-png-dir=/usr --with-jpeg-dir=/usr \t&&
      docker-php-ext-install gd mysqli opcache \t&& { \t\techo 'opcache.memory_consumption=192';
      \t\techo 'opcache.interned_strings_buffer=32'; \t\techo 'opcache.max_accelerated_files=10000';
      \t\techo 'opcache.revalidate_freq=120'; \t\techo 'opcache.validate_timestamps=0';
      \t\techo 'opcache.fast_shutdown=1'; \t\techo 'opcache.enable_cli=1'; \t} > /usr/local/etc/php/conf.d/opcache-recommended.ini"
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /var/www/html
  - Name: expose
    Ports:
    - "80"
  - CmdLine:
    - apache2-foreground
    Name: cmd
    PrependShell: false
  From:
    Image: php:7.0.13-apache
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.0.13-apache
