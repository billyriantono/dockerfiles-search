MetaArgs: null
Stages:
- BaseName: java:8
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y curl bzip2
    Name: run
    PrependShell: true
  - CmdLine:
    - curl https://aphyr.com/riemann/riemann-0.2.8.tar.bz2 | tar -C /opt -xj
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /config
  - Name: expose
    Ports:
    - "5555"
  - Name: expose
    Ports:
    - "5556"
  - CmdLine:
    - "if [ -f /config/riemann.config ]; then CONF_PATH=/config/riemann.config ; else
      CONF_PATH=/opt/riemann-0.2.8/etc/riemann.config ; fi && \t   /opt/riemann-0.2.8/bin/riemann
      $CONF_PATH"
    Name: entrypoint
    PrependShell: true
  From:
    Image: java:8
  Name: ""
  Platform: ""
  SourceCode: FROM java:8
