MetaArgs: null
Stages:
- BaseName: alpine:edge
  Commands:
  - CmdLine:
    - apk --no-cache add bash jq tar curl &&     curl -skL "$(curl -s https://api.github.com/repos/prometheus/alertmanager/releases/latest|jq
      --raw-output '.assets[] | .browser_download_url' | grep 'linux-amd64')" > /tmp/am.tgz
      &&     tar xzf /tmp/am.tgz -C /tmp --wildcards */amtool -O > /usr/local/bin/amtool
      &&     rm /tmp/am.tgz &&     chmod +x /usr/local/bin/amtool &&     apk del tar
      curl
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - check
    - /opt/resource/check
  - Chown: ""
    Name: add
    SourcesAndDest:
    - in
    - /opt/resource/in
  - Chown: ""
    Name: add
    SourcesAndDest:
    - out
    - /opt/resource/out
  - Chown: ""
    Name: add
    SourcesAndDest:
    - common.sh
    - /opt/resource/common.sh
  - CmdLine:
    - chmod +x /opt/resource/*
    Name: run
    PrependShell: true
  From:
    Image: alpine:edge
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:edge
