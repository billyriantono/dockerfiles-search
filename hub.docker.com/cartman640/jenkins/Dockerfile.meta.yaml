MetaArgs: null
Stages:
- BaseName: jenkins:alpine
  Commands:
  - Maintainer: Allan Ramsay
    Name: maintainer
  - Name: user
    User: root
  - CmdLine:
    - apk add --update build-base python-dev python py-pip libffi-dev openssl-dev
      openssh docker &&     pip install ansible &&     apk del build-base python-dev
      py-pip libffi-dev openssl-dev && rm -rf /var/cache/apk/*
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - drunner
    - /drunner
  - CmdLine:
    - adduser jenkins docker && adduser jenkins ping
    Name: run
    PrependShell: true
  - Name: user
    User: jenkins
  From:
    Image: jenkins:alpine
  Name: ""
  Platform: ""
  SourceCode: FROM jenkins:alpine
