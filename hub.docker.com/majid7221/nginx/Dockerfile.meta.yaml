MetaArgs: null
Stages:
- BaseName: majid7221/debian:stretch
  Commands:
  - CmdLine:
    - "set -ex \t&& apt-key adv --no-tty --keyserver ha.pool.sks-keyservers.net --recv-keys
      573BFD6B3D8FBC641079A6ABABF5BD827BD9BF62 \t&& echo \"deb https://nginx.org/packages/mainline/debian/
      stretch nginx\" > /etc/apt/sources.list.d/nginx.list \t&& apt-get update \t&&
      apt-get install --no-install-recommends -y \t    nginx \t    nginx-module-xslt
      \t    nginx-module-geoip \t    nginx-module-image-filter \t    nginx-module-njs
      \t    gettext-base \t&& rm -rf /var/lib/{apt,dpkg,cache,log}/ /tmp/* /var/tmp/*
      \t&& ln -sf /dev/stdout /var/log/nginx/access.log \t&& ln -sf /dev/stderr /var/log/nginx/error.log"
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "443"
    - "80"
  - Name: stopsignal
    Signal: SIGTERM
  - CmdLine:
    - nginx
    - -g
    - daemon off;
    Name: cmd
    PrependShell: false
  From:
    Image: majid7221/debian:stretch
  Name: ""
  Platform: ""
  SourceCode: FROM majid7221/debian:stretch
