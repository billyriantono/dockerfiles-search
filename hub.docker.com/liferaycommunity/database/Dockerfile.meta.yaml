MetaArgs: null
Stages:
- BaseName: mariadb:10.2
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y python-pip && rm -rf /var/lib/apt && pip
      install s3cmd
    Name: run
    PrependShell: true
  - Chown: mysql:mysql
    From: ""
    Name: copy
    SourcesAndDest:
    - rootfs/
    - /
  From:
    Image: mariadb:10.2
  Name: ""
  Platform: ""
  SourceCode: FROM mariadb:10.2
