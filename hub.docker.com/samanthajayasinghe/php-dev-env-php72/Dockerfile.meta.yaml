MetaArgs: null
Stages:
- BaseName: php:7.2-fpm
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - config/php.ini
    - /usr/local/etc/php/
  - CmdLine:
    - "apt-get update && apt-get install -y         apt-transport-https \t\tlibfreetype6-dev
      \t\tlibjpeg62-turbo-dev \t\tlibmcrypt-dev \t\tlibpng-dev \t\tlibxml2-dev \t\tlibicu-dev
      \t\tlibpq-dev \t\tlocales \t\tgnupg2 \t&& docker-php-ext-install -j$(nproc)
      xmlrpc \t    zip \t    intl \t    soap \t    opcache \t    pdo_pgsql \t    pdo_mysql
      \t    pgsql \t    mysqli \t&& docker-php-ext-configure gd --with-freetype-dir=/usr/include/
      --with-jpeg-dir=/usr/include/ \t&& docker-php-ext-install -j$(nproc) gd"
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y tzdata
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "${TZ}" > /etc/timezone     && dpkg-reconfigure --frontend noninteractive
      tzdata
    Name: run
    PrependShell: true
  From:
    Image: php:7.2-fpm
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.2-fpm
