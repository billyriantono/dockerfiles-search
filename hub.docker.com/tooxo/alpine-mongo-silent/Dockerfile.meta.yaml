MetaArgs: null
Stages:
- BaseName: alpine:edge
  Commands:
  - CmdLine:
    - echo 'http://dl-cdn.alpinelinux.org/alpine/v3.9/main' >> /etc/apk/repositories  &&
      echo 'http://dl-cdn.alpinelinux.org/alpine/v3.9/community' >> /etc/apk/repositories
    Name: run
    PrependShell: true
  - CmdLine:
    - apk update  && apk add dumb-init  && apk add --no-cache mongodb
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir /data  && mkdir /data/db
    Name: run
    PrependShell: true
  - CmdLine:
    - /usr/bin/dumb-init
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - mongod --bind_ip 0.0.0.0 | grep -Ev "conn|init"
    Name: cmd
    PrependShell: true
  From:
    Image: alpine:edge
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:edge
