MetaArgs: null
Stages:
- BaseName: ubuntu:xenial
  Commands:
  - Maintainer: alljoynsville
    Name: maintainer
  - CmdLine:
    - "apt-get update && \tapt-get update && \tapt-get -y upgrade && \tapt-get -y
      install less sudo wget vim python build-essential subversion libncurses5-dev
      unzip cmake \t                   zlib1g-dev gawk gcc-multilib flex git-core
      gettext libssl-dev && \tapt-get clean && \tuseradd -m builder && \techo 'builder
      ALL=NOPASSWD: ALL' > /etc/sudoers.d/builder && \trm -rf /var/lib/apt/lists/*
      /tmp/* /var/tmp/*"
    Name: run
    PrependShell: true
  - Name: user
    User: builder
  - Name: workdir
    Path: /home/builder
  - CmdLine:
    - /bin/bash
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:xenial
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:xenial
