MetaArgs: null
Stages:
- BaseName: siwatinc/nodejsubuntu_base_image
  Commands:
  - Key: CACHEBUST
    Name: arg
    Value: "1"
  - CmdLine:
    - npm install -g --unsafe-perm homebridge
    Name: run
    PrependShell: true
  - CmdLine:
    - 'timeout 10s homebridge || :'
    Name: run
    PrependShell: true
  - CmdLine:
    - touch /root/.homebridge/config.json
    Name: run
    PrependShell: true
  - CmdLine:
    - npm install -g --unsafe-perm homebridge-config-ui-x
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update --fix-missing
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get -y install wget git
    Name: run
    PrependShell: true
  - CmdLine:
    - 'apt-get update && wget -nc https://raw.githubusercontent.com/SiwatINC/unraid-ca-repository/master/docker-template/config.json
      -O "/root/.homebridge/config.json" || : && apt-get -y install $aptpackages ||
      : && npm install -g --unsafe-perm $packages && (homebridge -I &>> /root/.homebridge/log.txt)
      && tail -f /dev/null'
    Name: cmd
    PrependShell: true
  From:
    Image: siwatinc/nodejsubuntu_base_image
  Name: ""
  Platform: ""
  SourceCode: FROM siwatinc/nodejsubuntu_base_image
