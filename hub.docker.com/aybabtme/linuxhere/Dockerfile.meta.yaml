MetaArgs: null
Stages:
- BaseName: debian:sid
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"antoinegrondin@gmail.com"'
    Name: label
  - Labels:
    - Key: description
      Value: '"A quick linux environment to launch from macOS."'
    Name: label
  - CmdLine:
    - /bin/bash -c 'apt-get update && apt-get install -y --force-yes     sudo     tmux     git     vim-nox     dstat     htop     clang     gcc     fish     curl     jq     build-essential     cmake     python-dev     golang;'
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash -c ' fish_path=$(which fish); echo $fish_path >> /etc/shells; chsh
      root -s $fish_path;'
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash -c 'apt-get install -y --force-yes     dmsetup     automake     autoconf     libdevmapper-dev     libvirt-dev     dh-make     cpio;'
    Name: run
    PrependShell: true
  From:
    Image: debian:sid
  Name: ""
  Platform: ""
  SourceCode: FROM debian:sid
