MetaArgs: null
Stages:
- BaseName: php:7.3-fpm
  Commands:
  - Env:
    - Key: APT_LISTCHANGES_FRONTEND
      Value: mail
    Name: env
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - CmdLine:
    - "apt-get update && apt-get install -y -o DPkg::options::='--force-confdef' -o
      Dpkg::Options::='--force-confold'         libfreetype6-dev         libjpeg62-turbo-dev
      \        libpng16-16 libpng-dev libpng-tools \timagemagick \tlibgraphicsmagick1-dev
      \tlibmagickwand-dev \tlibcurl4         wget \tcurl \tlibcurl4-gnutls-dev \tlibicu-dev
      \tlibc-client2007e-dev \tlibc-client2007e \tlibkrb5-dev \tlibmariadbclient-dev-compat
      \tlibzip-dev \tgit     && apt-get clean     && apt-get autoremove -y     &&
      docker-php-ext-install -j$(nproc) pdo_mysql mysqli zip iconv intl curl     &&
      pecl install imagick     && docker-php-ext-enable imagick     && docker-php-ext-configure
      gd --with-freetype-dir=/usr/include/ --with-jpeg-dir=/usr/include/     && docker-php-ext-configure
      imap --with-kerberos --with-imap-ssl     && docker-php-ext-install -j$(nproc)
      gd imap     && rm -rf /var/lib/apt/lists/*     && curl --output composer -Ss
      https://getcomposer.org/download/1.2.0/composer.phar     && mv composer /usr/bin/composer
      \    && chmod 755 /usr/bin/composer     && chown root:root /usr/bin/composer
      \    && curl --output /etc/ssl/certs/rds.pem https://s3.amazonaws.com/rds-downloads/rds-ca-2015-eu-west-1.pem"
    Name: run
    PrependShell: true
  From:
    Image: php:7.3-fpm
  Name: ""
  Platform: ""
  SourceCode: FROM php:7.3-fpm
