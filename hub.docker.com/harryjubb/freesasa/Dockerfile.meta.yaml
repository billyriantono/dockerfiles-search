MetaArgs: null
Stages:
- BaseName: alpine:3.8
  Commands:
  - Chown: ""
    Name: add
    SourcesAndDest:
    - https://github.com/mittinatten/freesasa/releases/download/2.0.3/freesasa-2.0.3.tar.gz
    - .
  - CmdLine:
    - tar -xzf freesasa-2.0.3.tar.gz freesasa-2.0.3
    Name: run
    PrependShell: true
  - Name: workdir
    Path: freesasa-2.0.3
  - CmdLine:
    - apk add --no-cache   libxml2-dev   json-c-dev
    Name: run
    PrependShell: true
  - CmdLine:
    - apk add --no-cache --virtual .build-dependencies   build-base   && ./configure
      && make && make install   && apk del .build-dependencies
    Name: run
    PrependShell: true
  - Name: workdir
    Path: ..
  - CmdLine:
    - rm ./freesasa-2.0.3.tar.gz && rm -rf ./freesasa-2.0.3
    Name: run
    PrependShell: true
  From:
    Image: alpine:3.8
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.8
