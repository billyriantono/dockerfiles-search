MetaArgs: null
Stages:
- BaseName: ubuntu:latest
  Commands:
  - Maintainer: SpaceinvaderOne
    Name: maintainer
  - CmdLine:
    - apt-get update && apt-get -y install qemu git python python-pip bash rsync
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - .
    - /Macinabox
  - Name: volume
    Volumes:
    - /config
  - Name: volume
    Volumes:
    - /image
  - Name: volume
    Volumes:
    - /xml
  - CmdLine:
    - './Macinabox/unraid.sh $flavour $vminstall || : && bash && tail -f /dev/null'
    Name: cmd
    PrependShell: true
  From:
    Image: ubuntu:latest
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:latest
