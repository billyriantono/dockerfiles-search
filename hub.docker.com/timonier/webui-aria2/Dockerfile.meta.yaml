MetaArgs: null
Stages:
- BaseName: timonier/nginx:latest
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"Morgan AUCHEDE <morgan.auchede@gmail.com>"'
    Name: label
  - CmdLine:
    - set -e -u -x     && apk add --no-cache --no-progress --virtual BUILD_DEPS curl     &&
      curl --location "https://github.com/ziahamza/webui-aria2/archive/master.tar.gz"
      | tar --directory /tmp --extract --gzip     && mkdir -p /opt     && mv /tmp/webui-aria2-master
      /opt/webui-aria2     && apk del --no-progress BUILD_DEPS     && rm -f -r /tmp/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - rootfs/
    - /
  From:
    Image: timonier/nginx:latest
  Name: ""
  Platform: ""
  SourceCode: FROM timonier/nginx:latest
