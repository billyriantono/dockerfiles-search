MetaArgs: null
Stages:
- BaseName: debian:stretch
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y   freeglut3   libgtk2.0-dev   libwxgtk3.0-dev   libwx-perl   libxmu-dev   libgl1-mesa-glx   libgl1-mesa-dri   xdg-utils   --no-install-recommends   &&
      rm -rf /var/lib/apt/lists/*   && apt-get autoremove -y   && apt-get autoclean
    Name: run
    PrependShell: true
  - CmdLine:
    - groupadd slic3r   && useradd -g slic3r --create-home --home-dir /home/slic3r
      slic3r   && mkdir -p /Slic3r   && chown slic3r:slic3r /Slic3r
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /Slic3r
  - Chown: ""
    Name: add
    SourcesAndDest:
    - getLatestSlic3rRelease.sh
    - /Slic3r
  - CmdLine:
    - chmod +x /Slic3r/getLatestSlic3rRelease.sh
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y   jq   curl   ca-certificates   unzip   bzip2   git   --no-install-recommends   &&
      latestSlic3r=$(/Slic3r/getLatestSlic3rRelease.sh url)   && slic3rReleaseName=$(/Slic3r/getLatestSlic3rRelease.sh
      name)   && curl -sSL ${latestSlic3r} > ${slic3rReleaseName}   && rm -f /Slic3r/releaseInfo.json   &&
      mkdir -p /Slic3r/slic3r-dist   && tar -xjf ${slic3rReleaseName} -C /Slic3r/slic3r-dist
      --strip-components 1   && rm -f /Slic3r/${slic3rReleaseName}   && rm -rf /var/lib/apt/lists/*   &&
      apt-get purge -y --auto-remove jq unzip bzip2   && apt-get autoclean   && chown
      -R slic3r:slic3r /Slic3r /home/slic3r
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - LICENSE-slic3r
    - /
  - Name: user
    User: slic3r
  - CmdLine:
    - mkdir -p /home/slic3r/.local/share/
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /home/slic3r/
  - CmdLine:
    - /Slic3r/slic3r-dist/prusa-slicer
    Name: entrypoint
    PrependShell: false
  From:
    Image: debian:stretch
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch
