MetaArgs: null
Stages:
- BaseName: registry.cn-hangzhou.aliyuncs.com/xialintai/phpnginx
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - /
    - /var/www/html
  - CmdLine:
    - echo "v@"$(date +%Y%m%d%H%M%S) >/v
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir -p /opt/logs && chmod 0777 /opt/logs
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get -y update && apt-get -y install  ssh     && mkdir /var/run/sshd && chmod
      0755 /var/run/sshd     && sed -i 's/^PermitRootLogin.*/PermitRootLogin yes/'
      /etc/ssh/sshd_config     && sed -i 's/^PasswordAuthentication.*/PermitRootLogin
      yes/' /etc/ssh/sshd_config     && sed -i 's/#PasswordAuthentication.*/PermitRootLogin
      yes/' /etc/ssh/sshd_config
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /root/.ssh/
  - CmdLine:
    - /usr/sbin/sshd && tail -f /etc/hosts
    Name: cmd
    PrependShell: true
  - Name: volume
    Volumes:
    - /var/www/html/Business/
  - Name: expose
    Ports:
    - "22"
  - CmdLine:
    - bash -c "/usr/sbin/sshd; phpunit ;  sh Shell/websitesuggest.sh;nginx; php-fpm"
    Name: cmd
    PrependShell: true
  From:
    Image: registry.cn-hangzhou.aliyuncs.com/xialintai/phpnginx
  Name: ""
  Platform: ""
  SourceCode: FROM registry.cn-hangzhou.aliyuncs.com/xialintai/phpnginx
