MetaArgs: null
Stages:
- BaseName: bitnami/minideb:jessie
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"wcollani@gmail.com"'
    - Key: version
      Value: '"v0.1.0"'
    Name: label
  - CmdLine:
    - "install_packages git ssh tar gzip unzip ca-certificates \t apt-transport-https
      curl python-software-properties wget"
    Name: run
    PrependShell: true
  - CmdLine:
    - "curl -fsSL https://download.docker.com/linux/debian/gpg | apt-key add - &&
      \t echo \"deb [arch=amd64] https://download.docker.com/linux/debian jessie stable\"
      >> /etc/apt/sources.list"
    Name: run
    PrependShell: true
  - CmdLine:
    - install_packages docker-ce
    Name: run
    PrependShell: true
  - CmdLine:
    - "wget https://github.com/tcnksm/ghr/releases/download/v0.5.4/ghr_v0.5.4_linux_amd64.zip
      && \t unzip ghr_v0.5.4_linux_amd64.zip && rm ghr_v0.5.4_linux_amd64.zip && \t
      mv /ghr /usr/local/bin/ghr"
    Name: run
    PrependShell: true
  From:
    Image: bitnami/minideb:jessie
  Name: ""
  Platform: ""
  SourceCode: FROM bitnami/minideb:jessie
