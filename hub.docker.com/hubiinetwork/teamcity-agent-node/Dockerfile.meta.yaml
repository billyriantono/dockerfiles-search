MetaArgs: null
Stages:
- BaseName: jetbrains/teamcity-agent
  Commands:
  - CmdLine:
    - apt-get update &&   apt-get install curl sudo -y &&   curl -sL https://deb.nodesource.com/setup_8.x
      | sudo -E bash - &&   apt-get install -y nodejs build-essential &&   npm install
      npm@latest -g
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get install ruby-full -y
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -O https://bootstrap.pypa.io/get-pip.py &&   python get-pip.py &&   pip
      install awscli --upgrade --user
    Name: run
    PrependShell: true
  - Env:
    - Key: PATH
      Value: /root/.local/bin:$PATH
    Name: env
  - CmdLine:
    - apt-get install -y apt-transport-https &&   curl -s https://packages.cloud.google.com/apt/doc/apt-key.gpg
      | apt-key add - &&   echo "deb http://apt.kubernetes.io/ kubernetes-xenial main"
      > /etc/apt/sources.list.d/kubernetes.list &&   apt-get update &&   apt-get install
      -y kubectl
    Name: run
    PrependShell: true
  - CmdLine:
    - '"/run-services.sh"'
    Name: cmd
    PrependShell: true
  From:
    Image: jetbrains/teamcity-agent
  Name: ""
  Platform: ""
  SourceCode: FROM jetbrains/teamcity-agent
