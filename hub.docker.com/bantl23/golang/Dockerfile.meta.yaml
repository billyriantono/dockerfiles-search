MetaArgs: null
Stages:
- BaseName: ubuntu:trusty
  Commands:
  - CmdLine:
    - apt-get update &&     apt-get install -y software-properties-common &&     add-apt-repository
      -y ppa:ubuntu-lxc/lxd-stable &&     apt-get update &&     apt-get install -y     curl     wget     man-db     golang     git-core     vim
    Name: run
    PrependShell: true
  - CmdLine:
    - useradd -m -G sudo docker
    Name: run
    PrependShell: true
  - CmdLine:
    - echo '%sudo ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers
    Name: run
    PrependShell: true
  - Name: user
    User: docker
  - CmdLine:
    - mkdir -p /home/docker/go
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:trusty
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:trusty
