MetaArgs: null
Stages:
- BaseName: centos:6
  Commands:
  - CmdLine:
    - yum upgrade -y &&   yum install -y rpmdevtools git
    Name: run
    PrependShell: true
  - CmdLine:
    - git clone https://github.com/jbraeuer/yum-s3-plugin.git &&   cd yum-s3-plugin
      &&   chmod +x package
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /data
  - CmdLine:
    - cd yum-s3-plugin &&    ./package -d &&   cp -v /yum-s3-plugin/RPMS/noarch/*
      /data/
    Name: cmd
    PrependShell: true
  From:
    Image: centos:6
  Name: ""
  Platform: ""
  SourceCode: FROM centos:6
