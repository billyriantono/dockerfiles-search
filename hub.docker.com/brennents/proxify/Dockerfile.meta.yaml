MetaArgs: null
Stages:
- BaseName: ubuntu:16.04
  Commands:
  - Name: expose
    Ports:
    - "80"
  - Name: expose
    Ports:
    - "443"
  - Name: expose
    Ports:
    - "7080"
  - CmdLine:
    - "apt-get update \t&& apt-get install -y haproxy openssl \t&& rm -rf /var/lib/apt/lists/*
      /tmp/* /var/tmp/*  \t&& apt-get clean"
    Name: run
    PrependShell: true
  - CmdLine:
    - openssl req -subj '/CN=proxify/O=PROXIFY/C=US' -new -newkey rsa:2048 -days 3650
      -nodes -x509 -keyout /etc/haproxy/proxify.key -out /etc/haproxy/proxify.crt     &&
      cat /etc/haproxy/proxify.crt  /etc/haproxy/proxify.key >  /etc/haproxy/proxify.pem
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - lib/haproxy.cfg
    - /etc/haproxy/haproxy.cfg
  - CmdLine:
    - haproxy -f /etc/haproxy/haproxy.cfg -p "/var/run/haproxy.pid"
    Name: cmd
    PrependShell: true
  From:
    Image: ubuntu:16.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:16.04
