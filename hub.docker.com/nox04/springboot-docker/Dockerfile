# Base image
FROM openjdk:8-jre-alpine

# Set deployment directory
WORKDIR /project

# Expose 8080 port
EXPOSE 8080

# Set the startup command to run your binary
CMD ["java", "-jar", "./sara-0.0.1.jar"]
