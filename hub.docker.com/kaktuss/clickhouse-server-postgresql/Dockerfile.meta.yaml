MetaArgs: null
Stages:
- BaseName: yandex/clickhouse-server:18.14.13
  Commands:
  - CmdLine:
    - apt-get update   && apt-get install --no-install-recommends --no-install-suggests
      -y     odbc-postgresql     odbcinst     unixodbc   && rm -rf /var/lib/apt/lists/*
      /var/cache/debconf   && apt-get clean
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - odbcinst.ini
    - /etc/odbcinst.ini
  From:
    Image: yandex/clickhouse-server:18.14.13
  Name: ""
  Platform: ""
  SourceCode: FROM yandex/clickhouse-server:18.14.13
