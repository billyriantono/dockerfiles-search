MetaArgs: null
Stages:
- BaseName: alpine:edge
  Commands:
  - Maintainer: Onni Hakala - Geniem Oy. <onni.hakala@geniem.com>
    Name: maintainer
  - CmdLine:
    - cd /tmp/ &&     apk --update add curl &&     curl -L "https://github.com/dustinblackman/phantomized/releases/download/2.1.1/dockerized-phantomjs.tar.gz"     |
      tar xz -C / &&     curl -L https://bitbucket.org/ariya/phantomjs/downloads/phantomjs-2.1.1-linux-x86_64.tar.bz2     |
      tar -xjC /tmp &&     mv /tmp/phantomjs-2.1.1-linux-x86_64/bin/phantomjs /usr/local/bin/
      &&     chmod +rx /usr/local/bin/phantomjs &&     apk add bash less vim nano
      git mysql-client nginx ca-certificates openssh-client     libssh2 libpng freetype
      libjpeg-turbo libgcc libxml2 libstdc++ icu-libs libltdl libmcrypt     msmtp     tzdata
      &&     apk add -u musl &&     apk add nodejs &&     apk add --update-cache --repository
      http://dl-4.alpinelinux.org/alpine/edge/testing/     php7-pdo_mysql php7-mysqli
      php7-mysqlnd php7-mcrypt     php7-xdebug     php7 php7-session php7-fpm php7-json
      php7-zlib php7-xml php7-pdo     php7-gd php7-curl php7-opcache php7-ctype php7-mbstring
      php7-soap     php7-intl php7-bcmath php7-dom php7-xmlreader php7-openssl php7-phar
      php7-redis  &&     ln -s /etc/php7 /etc/php &&     ln -s /usr/bin/php7 /usr/bin/php
      &&     ln -s /usr/sbin/php-fpm7 /usr/bin/php-fpm &&     ln -s /usr/lib/php7
      /usr/lib/php &&     deluser nginx &&     curl -L https://github.com/just-containers/s6-overlay/releases/download/v1.17.2.0/s6-overlay-amd64.tar.gz     |
      tar -xvzC /  &&     curl -L https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar
      -o /usr/local/bin/wp-cli &&     chmod +rx /usr/local/bin/wp-cli &&     curl
      -L https://raw.githubusercontent.com/kvz/cronlock/master/cronlock -o /usr/local/bin/cronlock
      &&     chmod +rx /usr/local/bin/cronlock &&     curl -L https://getcomposer.org/installer
      -o composer-setup.php &&     php composer-setup.php &&     rm  composer-setup.php
      &&     mv composer.phar /usr/local/bin/composer &&     chmod +rx /usr/local/bin/composer
      &&     composer create-project wp-coding-standards/wpcs:dev-master --no-interaction
      --no-dev /var/lib/wpcs &&     apk --update add ruby libxslt &&     apk add --virtual
      build_deps build-base ruby-dev libc-dev linux-headers     openssl-dev postgresql-dev
      libxml2-dev libxslt-dev &&     gem install json rspec rspec-retry poltergeist
      capybara --no-ri --no-rdoc &&     apk del build_deps &&     rm -rf /var/cache/apk/*
      &&     rm -rf /tmp/*
    Name: run
    PrependShell: true
  From:
    Image: alpine:edge
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:edge
