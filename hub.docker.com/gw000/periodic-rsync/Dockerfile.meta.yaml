MetaArgs: null
Stages:
- BaseName: debian:jessie
  Commands:
  - Maintainer: gw0 [http://gw.tnode.com/] <gw.2016@tnode.com>
    Name: maintainer
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - CmdLine:
    - apt-get update -qq  && apt-get install --no-install-recommends -y     cron     rsync     openssh-client  &&
      apt-get clean  && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - rm -f /etc/cron.*/*
    Name: run
    PrependShell: true
  - CmdLine:
    - cron
    - -f
    Name: cmd
    PrependShell: false
  From:
    Image: debian:jessie
  Name: ""
  Platform: ""
  SourceCode: FROM debian:jessie
