MetaArgs: null
Stages:
- BaseName: dtank/core:latest
  Commands:
  - Maintainer: benharker@mac.com
    Name: maintainer
  - CmdLine:
    - wget -O - http://ppa.moosefs.com/moosefs.key | apt-key add - && echo "deb http://ppa.moosefs.com/current/apt/ubuntu/trusty
      trusty main" >> /etc/apt/sources.list.d/moosefs.list ;
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update ; apt-get install -y moosefs-chunkserver nano sed ; rm -rf /var/lib/apt/lists/*
      && mv /etc/mfs/mfschunkserver.cfg.sample /etc/mfs/mfschunkserver.cfg ; mv /etc/mfs/mfshdd.cfg.sample
      /etc/mfs/mfshdd.cfg ; echo "/home/chunks/ -5GiB" >> /etc/mfs/mfshdd.cfg ; /bin/sed
      s/# LABELS =/LABELS = D/g /etc/mfs/mfschunkserver.cfg ; /bin/echo "LABELS =
      Z" >> /etc/mfs/mfschunkserver.cfg ;
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/mkdir /home/chunks ; /bin/chmod -R 777 /home/chunks ;
    Name: run
    PrependShell: true
  - Name: expose
    Ports:
    - "9419"
  - Name: expose
    Ports:
    - "9420"
  - Name: expose
    Ports:
    - "9421"
  - Name: expose
    Ports:
    - "9422"
  - CmdLine:
    - /usr/sbin/mfschunkserver -f start
    Name: cmd
    PrependShell: false
  From:
    Image: dtank/core:latest
  Name: ""
  Platform: ""
  SourceCode: FROM dtank/core:latest
