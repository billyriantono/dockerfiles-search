MetaArgs: null
Stages:
- BaseName: ubuntu:14.04
  Commands:
  - CmdLine:
    - apt-get update -qq &&   apt-get install -y     openssh-server     sudo     &&   rm
      -fr /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - 'groupadd -r ubuntu &&     useradd -rmg ubuntu ubuntu &&     echo ''ubuntu ALL=(ALL)
      NOPASSWD: ALL'' >> /etc/sudoers'
    Name: run
    PrependShell: true
  - CmdLine:
    - su - ubuntu -c "mkdir ~/.ssh"
    Name: run
    PrependShell: true
  - CmdLine:
    - sed -i "s/UsePrivilegeSeparation.*/UsePrivilegeSeparation no/g" /etc/ssh/sshd_config
      &&   sed -i "s/StrictModes.*/StrictModes no/g" /etc/ssh/sshd_config &&   touch
      /home/ubuntu/.Xauthority &&   update-locale
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - docker-entrypoint.sh
    - /
  - CmdLine:
    - /docker-entrypoint.sh
    Name: cmd
    PrependShell: false
  - Name: expose
    Ports:
    - "22"
  From:
    Image: ubuntu:14.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:14.04
