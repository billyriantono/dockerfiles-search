MetaArgs: null
Stages:
- BaseName: kalilinux/kali-linux-docker
  Commands:
  - Maintainer: Matthew Schwartz "@schwartz1375"
    Name: maintainer
  - Name: workdir
    Path: /root
  - Name: user
    User: root
  - CmdLine:
    - "DEBIAN_FRONTEND=noninteractive \tapt-get -y update && apt-get -y \tdist-upgrade
      && apt-get clean && apt-get install -y\tnmap vim tmux\tmetasploit-framework"
    Name: run
    PrependShell: true
  - CmdLine:
    - localedef -f UTF-8 -i en_US en_US.UTF-8
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /root/.msf4
  - Name: volume
    Volumes:
    - /tmp/data
  - CmdLine:
    - /bin/bash
    Name: cmd
    PrependShell: false
  From:
    Image: kalilinux/kali-linux-docker
  Name: ""
  Platform: ""
  SourceCode: FROM  kalilinux/kali-linux-docker
