MetaArgs: null
Stages:
- BaseName: jenkins/jenkins:lts
  Commands:
  - Name: user
    User: root
  - CmdLine:
    - apt-get update     && apt-get install -y zip     && curl -sL https://deb.nodesource.com/setup_8.x
      | bash -     && curl -fsSL https://get.docker.com/ | sh     && apt-get install
      -y nodejs     && apt-get install -y build-essential     && apt-get install -y
      python-pip     && pip install docker-compose     && npm config set unsafe-perm=true     &&
      npm i -g web-ext     && apt-get clean
    Name: run
    PrependShell: true
  - CmdLine:
    - groupadd -g 113 docker-host     && usermod -a -G docker-host jenkins
    Name: run
    PrependShell: true
  From:
    Image: jenkins/jenkins:lts
  Name: ""
  Platform: ""
  SourceCode: FROM jenkins/jenkins:lts
