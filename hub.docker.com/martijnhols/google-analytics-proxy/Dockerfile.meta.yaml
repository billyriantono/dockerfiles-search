MetaArgs: null
Stages:
- BaseName: nginx:alpine
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - ./nginx.conf
    - /etc/nginx/conf.d/default.template
  - CmdLine:
    - "GAPROXY_HOST=\"${GAPROXY_HOST:=$VIRTUAL_HOST}\" \tenvsubst < /etc/nginx/conf.d/default.template
      > /etc/nginx/conf.d/default.conf \t&& echo \"Starting nginx\" \t&& exec nginx
      -g 'daemon off;'"
    Name: cmd
    PrependShell: true
  From:
    Image: nginx:alpine
  Name: ""
  Platform: ""
  SourceCode: FROM nginx:alpine
