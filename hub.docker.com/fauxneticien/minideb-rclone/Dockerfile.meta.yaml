MetaArgs: null
Stages:
- BaseName: bitnami/minideb
  Commands:
  - CmdLine:
    - "apt-get update && \tapt-get install -y ca-certificates wget unzip && \twget
      https://downloads.rclone.org/rclone-current-linux-amd64.zip && \tunzip rclone-current-linux-amd64.zip
      && \tcd rclone-*-linux-amd64 && \tcp rclone /usr/bin/ && \tchmod 755 /usr/bin/rclone
      && \trm -R /rclone* && \trclone --version"
    Name: run
    PrependShell: true
  From:
    Image: bitnami/minideb
  Name: ""
  Platform: ""
  SourceCode: FROM bitnami/minideb
