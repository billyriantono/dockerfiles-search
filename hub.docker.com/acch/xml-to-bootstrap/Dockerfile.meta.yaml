MetaArgs: null
Stages:
- BaseName: debian:stretch
  Commands:
  - Maintainer: Achim Christ
    Name: maintainer
  - Key: DEBIAN_FRONTEND
    Name: arg
    Value: noninteractive
  - CmdLine:
    - apt-get -qq update && apt-get -qqy install   curl   gnupg && curl -sL https://deb.nodesource.com/setup_6.x
      | bash - && apt-get -qqy install   git   g++   gcc   graphicsmagick   make   npm   openjdk-8-jre   python   xsltproc
      && npm install -g bower grunt-cli && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - useradd -d /build build && mkdir /build && chown build:build /build
    Name: run
    PrependShell: true
  - Name: user
    User: build
  - Name: workdir
    Path: /build
  - CmdLine:
    - git clone https://github.com/acch/XML-to-bootstrap.git .
    Name: run
    PrependShell: true
  - CmdLine:
    - npm install && bower install
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - sass/sample/customvars.scss
    - sass/*.scss
    - /build/sass/
  - CmdLine:
    - grunt init
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - src/
    - /build/src
  - Name: volume
    Volumes:
    - /build/src
    - /build/publish
  - Name: expose
    Ports:
    - "8000"
  - CmdLine:
    - grunt
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - default
    Name: cmd
    PrependShell: false
  From:
    Image: debian:stretch
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch
