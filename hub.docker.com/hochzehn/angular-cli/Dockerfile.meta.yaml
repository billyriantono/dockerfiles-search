MetaArgs: null
Stages:
- BaseName: node:10.16-stretch
  Commands:
  - Env:
    - Key: ANGULAR_CLI_VERSION
      Value: 8.3.20
    Name: env
  - CmdLine:
    - chown -R node:node /usr/local/lib/node_modules  && chown -R node:node /usr/local/bin
    Name: run
    PrependShell: true
  - Name: user
    User: node
  - CmdLine:
    - npm install -g @angular/cli@"$ANGULAR_CLI_VERSION"  && ng version
    Name: run
    PrependShell: true
  - Name: user
    User: root
  - CmdLine:
    - apt-get update  && apt-get install -y --no-install-recommends chromium  && apt-get
      autoremove -y  && apt-get clean  && rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*  &&
      /usr/bin/chromium --version
    Name: run
    PrependShell: true
  - Env:
    - Key: CHROME_BIN
      Value: /usr/bin/chromium
    Name: env
  - Name: workdir
    Path: /app
  - Name: user
    User: node
  - CmdLine:
    - ng
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - version
    Name: cmd
    PrependShell: false
  From:
    Image: node:10.16-stretch
  Name: ""
  Platform: ""
  SourceCode: FROM node:10.16-stretch
