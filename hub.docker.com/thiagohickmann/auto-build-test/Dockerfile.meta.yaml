MetaArgs: null
Stages:
- BaseName: debian:stretch
  Commands:
  - CmdLine:
    - "dpkg --add-architecture i386 \t\t&& apt-get update && apt-get install -y wget
      gnupg2         && wget https://dl.winehq.org/wine-builds/Release.key \t\t&&
      apt-key add Release.key         && apt-get install -y apt-transport-https         &&
      echo \"deb https://dl.winehq.org/wine-builds/debian/ stretch main\" >> /etc/apt/sources.list
      \t\t&& apt-get update && apt-get install -y --no-install-recommends \t\t\twinehq-stable
      \t\t&& rm -rf /var/lib/apt/lists/*"
    Name: run
    PrependShell: true
  From:
    Image: debian:stretch
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stretch
