FROM mpixel/alpine.git_python
MAINTAINER Max Pixel <foss.docker@m-pixel.com>
RUN apk add curl && curl https://storage.googleapis.com/git-repo-downloads/repo > /bin/repo && chmod a+x /bin/repo && apk del curl
