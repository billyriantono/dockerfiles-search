MetaArgs: null
Stages:
- BaseName: circleci/golang:1.13.5-stretch-node-browsers-legacy
  Commands:
  - Maintainer: ops@spaceback.me
    Name: maintainer
  - CmdLine:
    - wget "s3.amazonaws.com/aws-cli/awscli-bundle.zip" -O "awscli-bundle.zip" &&     unzip
      awscli-bundle.zip &&     sudo apt-get install groff-base &&     sudo ./awscli-bundle/install
      -i /usr/local/aws -b /usr/local/bin/aws &&     rm awscli-bundle.zip &&     rm
      -rf awscli-bundle
    Name: run
    PrependShell: true
  - CmdLine:
    - sudo curl -sL https://deb.nodesource.com/setup_10.x | sudo -E bash - &&     sudo
      sudo apt-get install -y nodejs &&     sudo npm install newman -g
    Name: run
    PrependShell: true
  - CmdLine:
    - set -x     VER="17.12.1-ce"     curl -L -o /tmp/docker-$VER.tgz https://download.docker.com/linux/static/stable/x86_64/docker-$VER.tgz     tar
      -xz -C /tmp -f /tmp/docker-$VER.tgz     mv /tmp/docker/* /usr/bin
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /
  - CmdLine:
    - /bin/bash
    Name: entrypoint
    PrependShell: false
  From:
    Image: circleci/golang:1.13.5-stretch-node-browsers-legacy
  Name: ""
  Platform: ""
  SourceCode: FROM circleci/golang:1.13.5-stretch-node-browsers-legacy
