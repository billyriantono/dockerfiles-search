MetaArgs:
- DefaultValue: '"${DOCKER_IMAGE:-node:8-alpine}"'
  Key: DOCKER_IMAGE
  ProvidedValue: null
  Value: '"${DOCKER_IMAGE:-node:8-alpine}"'
Stages:
- BaseName: ${DOCKER_IMAGE}
  Commands:
  - Key: KAMUS_CLI_VERSION
    Name: arg
    Value: '"${KAMUS_CLI_VERSION:-0.2.3}"'
  - CmdLine:
    - npm install -g @soluto-asurion/kamus-cli@${KAMUS_CLI_VERSION}
    Name: run
    PrependShell: true
  - CmdLine:
    - /usr/local/bin/kamus-cli
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - --help
    Name: cmd
    PrependShell: false
  From:
    Image: ${DOCKER_IMAGE}
  Name: ""
  Platform: ""
  SourceCode: FROM ${DOCKER_IMAGE}
