MetaArgs: null
Stages:
- BaseName: ubuntu:16.04
  Commands:
  - CmdLine:
    - "apt-get update \t&& apt-get install -y unzip wget build-essential \t\tcmake
      git pkg-config libswscale-dev \t\tlibtbb2 libtbb-dev libjpeg-dev \t\tlibpng-dev
      libtiff-dev libjasper-dev"
    Name: run
    PrependShell: true
  - CmdLine:
    - "cd \t&& wget https://github.com/opencv/opencv/archive/3.1.0.zip \t&& unzip
      3.1.0.zip \t&& cd opencv-3.1.0 \t&& mkdir build \t&& cd build \t&& cmake -DWITH_IPP=OFF
      .. \t&& make -j$(nproc) \t&& make install \t&& cd     && rm 3.1.0.zip     &&
      rm -rf opencv-3.1.0"
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get install -y libatlas-base-dev &&     git clone --recursive https://github.com/dmlc/mxnet/
      && cd mxnet &&     cp make/config.mk . &&     make -j$(nproc) &&     cp lib/*
      /usr/lib &&     cd .. && rm -rf mxnet
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:16.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:16.04
