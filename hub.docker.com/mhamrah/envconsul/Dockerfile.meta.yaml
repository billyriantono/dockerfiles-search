MetaArgs: null
Stages:
- BaseName: debian:jessie
  Commands:
  - Maintainer: Michael Hamrah <m@hamrah.com>
    Name: maintainer
  - CmdLine:
    - apt-get update &&   apt-get install -y wget net-tools netcat &&   wget -q --no-check-certificate
      -O /tmp/consul.tar.gz https://github.com/hashicorp/envconsul/releases/download/v0.3.0/envconsul_0.3.0_linux_amd64.tar.gz
      &&   tar -xzvf /tmp/consul.tar.gz -C /usr/bin/ --strip-components=1 &&   apt-get
      clean &&   rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/* *.gz
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - envconsul-config.hcl
    - /etc/envconsul-config.hcl
  - Chown: ""
    Name: add
    SourcesAndDest:
    - envconsul-launch
    - /usr/bin/envconsul-launch
  - CmdLine:
    - chmod +x /usr/bin/envconsul-launch
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install curl -y
    Name: run
    PrependShell: true
  - CmdLine:
    - envconsul-launch
    Name: entrypoint
    PrependShell: false
  From:
    Image: debian:jessie
  Name: ""
  Platform: ""
  SourceCode: FROM debian:jessie
