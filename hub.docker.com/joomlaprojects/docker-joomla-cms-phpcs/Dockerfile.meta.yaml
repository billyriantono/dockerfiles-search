MetaArgs: null
Stages:
- BaseName: php:7.0-cli
  Commands:
  - Maintainer: Robert Deutz
    Name: maintainer
  - CmdLine:
    - apt-get update     && apt-get install -y curl zip unzip git software-properties-common     &&
      php -r "readfile('http://getcomposer.org/installer');" | php -- --install-dir=/usr/bin/
      --filename=composer     && apt-get remove -y --purge software-properties-common     &&
      apt-get clean     && rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*     && mkdir
      /opt/tools
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - composer.json
    - /opt/tools
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - check_codestyle.sh
    - /opt
  - CmdLine:
    - cd /opt/tools && composer install
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /opt
  - CmdLine:
    - /opt/check_codestyle.sh
    Name: entrypoint
    PrependShell: false
  From:
    Image: php:7.0-cli
  Name: ""
  Platform: ""
  SourceCode: FROM  php:7.0-cli
