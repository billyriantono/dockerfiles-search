MetaArgs: null
Stages:
- BaseName: python:3.6-slim
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - locales.txt
    - /etc/locale.gen
  - CmdLine:
    - apt-get -qq update     && apt-get install -y --no-install-recommends         wget
      git-core tmux vim locales less         binutils libproj-dev gdal-bin     &&
      rm -rf /var/lib/apt/lists/*     && echo 'alias ll="ls -l"' >> ~/.bashrc     &&
      echo 'alias la="ls -la"' >> ~/.bashrc     && locale-gen
    Name: run
    PrependShell: true
  - CmdLine:
    - pip3 install --upgrade pip
    Name: run
    PrependShell: true
  - CmdLine:
    - mkdir -p /app
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /app
  From:
    Image: python:3.6-slim
  Name: ""
  Platform: ""
  SourceCode: FROM python:3.6-slim
