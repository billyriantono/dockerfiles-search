MetaArgs: null
Stages:
- BaseName: bitriseio/docker-android
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y         curl         apt-transport-https         &&
      curl -sSL https://dl.google.com/linux/linux_signing_key.pub | apt-key add -         &&
      echo "deb [arch=amd64] https://dl.google.com/linux/chrome/deb/ stable main"
      > /etc/apt/sources.list.d/google.list         && apt-get update && apt-get install
      -y dbus-x11 git xvfb google-chrome-stable curl --no-install-recommends         &&
      apt-get purge --auto-remove -y curl apt-transport-https         && rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - Chown: ""
    Name: add
    SourcesAndDest:
    - xvfb-chrome
    - /usr/bin/xvfb-chrome
  - CmdLine:
    - chmod a+x /usr/bin/xvfb-chrome
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /bitrise/src
  From:
    Image: bitriseio/docker-android
  Name: ""
  Platform: ""
  SourceCode: FROM bitriseio/docker-android
