MetaArgs: null
Stages:
- BaseName: ubuntu:14.04
  Commands:
  - CmdLine:
    - rm /bin/sh && ln -s /bin/bash /bin/sh
    Name: run
    PrependShell: true
  - CmdLine:
    - DEBIAN_FRONTEND=noninteractive apt-get update     && apt-get install -y -q --no-install-recommends         zip         unzip         apt-transport-https         build-essential         ca-certificates         curl         git         libssl-dev         python         rsync         vim         software-properties-common         wget     &&
      sed -i '1s/^/force_color_prompt=yes\n/' /root/.bashrc
    Name: run
    PrependShell: true
  - CmdLine:
    - bash
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:14.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:14.04
