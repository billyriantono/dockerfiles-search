MetaArgs: null
Stages:
- BaseName: debian:stable
  Commands:
  - CmdLine:
    - apt-get update -q -y && apt-get install --no-install-recommends -y libc-dev
      gcc make locales gdb valgrind sed grep bash tmux curl ca-certificates git &&
      rm -rf /var/lib/apt/lists/*
    Name: run
    PrependShell: true
  - CmdLine:
    - export DEBIAN_FRONTEND=noninteractive; echo "en_US.UTF-8 UTF-8" > /etc/locale.gen
      &&     locale-gen en_US.UTF-8 &&     dpkg-reconfigure locales &&     /usr/sbin/update-locale
      LANG=en_US.UTF-8
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /data
  - CmdLine:
    - /bin/bash
    Name: entrypoint
    PrependShell: false
  From:
    Image: debian:stable
  Name: ""
  Platform: ""
  SourceCode: FROM debian:stable
