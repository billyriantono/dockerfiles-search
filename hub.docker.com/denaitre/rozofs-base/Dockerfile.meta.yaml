MetaArgs: null
Stages:
- BaseName: debian:latest
  Commands:
  - Maintainer: denaitre, dimitri.pertin@univ-nantes.fr
    Name: maintainer
  - CmdLine:
    - apt-get -y update && apt-get install -y     wget     lsb-release
    Name: run
    PrependShell: true
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - CmdLine:
    - echo "#!/bin/sh\nexit 0" > /usr/sbin/policy-rc.d
    Name: run
    PrependShell: true
  - CmdLine:
    - wget -O - http://54.225.196.202/debian/devel@rozofs.com.gpg.key | apt-key add
      -
    Name: run
    PrependShell: true
  - CmdLine:
    - echo deb  http://54.225.196.202/debian/master $(lsb_release -sc) main | tee
      /etc/apt/sources.list.d/rozofs.list
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get -y update && apt-get install -y     rozofs-manager*
    Name: run
    PrependShell: true
  - CmdLine:
    - tail -f /var/log/dmesg
    Name: cmd
    PrependShell: true
  From:
    Image: debian:latest
  Name: ""
  Platform: ""
  SourceCode: FROM debian:latest
