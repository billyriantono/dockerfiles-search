MetaArgs: null
Stages:
- BaseName: bradrydzewski/base
  Commands:
  - CmdLine:
    - "sudo add-apt-repository ppa:webupd8team/java && \tsudo apt-get update && \tsudo
      echo oracle-java8-installer shared/accepted-oracle-license-v1-1 select true
      | sudo debconf-set-selections && \tsudo apt-get install oracle-java8-installer
      && \tsudo apt-get install -qq libstdc++6:i386 lib32z1 expect && \tcurl -L https://raw.github.com/embarkmobile/android-sdk-installer/version-2/android-sdk-installer
      | bash /dev/stdin \"--install=build-tools-21.1.0,android-21,extra\""
    Name: run
    PrependShell: true
  From:
    Image: bradrydzewski/base
  Name: ""
  Platform: ""
  SourceCode: FROM bradrydzewski/base
