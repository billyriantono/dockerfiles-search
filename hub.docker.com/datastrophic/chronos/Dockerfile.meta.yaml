MetaArgs: null
Stages:
- BaseName: datastrophic/mesos-java:1.1.0
  Commands:
  - Key: CHRONOS_VERSION
    Name: arg
    Value: v3.0.1
  - CmdLine:
    - mkdir -p /chronos /chronos-build
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y git maven curl build-essential &&     curl
      -sL https://deb.nodesource.com/setup_7.x | bash - &&     apt-get install -y
      nodejs
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /chronos-build && curl -Lo cronos-${CHRONOS_VERSION}.tar.gz https://github.com/mesos/chronos/archive/${CHRONOS_VERSION}.tar.gz
      &&     tar -xzvf cronos-${CHRONOS_VERSION}.tar.gz --strip-components 1 &&     mvn
      versions:set -DnewVersion=${CHRONOS_VERSION} && mvn package && cp target/chronos-${CHRONOS_VERSION}.jar
      /chronos/chronos.jar &&     rm -rf /chronos-build /root/.m2 /root/.npm
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - entrypoint.sh
    - /
  - CmdLine:
    - /entrypoint.sh
    Name: entrypoint
    PrependShell: false
  From:
    Image: datastrophic/mesos-java:1.1.0
  Name: ""
  Platform: ""
  SourceCode: FROM datastrophic/mesos-java:1.1.0
