MetaArgs: null
Stages:
- BaseName: ubuntu:latest
  Commands:
  - CmdLine:
    - apt-get update &&     apt-get install -y wget nodejs npm openssl zip vim-common
      git &&     npm cache clean &&     npm install n -g &&     n stable &&     ln
      -sf /usr/local/bin/node /usr/bin/node &&     npm update -g npm &&     npm install
      -g gulp
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - crx_build.sh
    - /usr/bin/
  - Name: volume
    Volumes:
    - /app
  - Name: workdir
    Path: /app
  - CmdLine:
    - bash
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:latest
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:latest
