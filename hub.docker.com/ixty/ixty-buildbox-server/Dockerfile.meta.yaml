MetaArgs: null
Stages:
- BaseName: ubuntu:14.04
  Commands:
  - CmdLine:
    - apt-get update && apt-get install -y     python2.7     python2.7-dev     python-pip     python-psycopg2     libpq-dev     libxml2-dev     libxslt-dev     git     mercurial     ruby     curl
    Name: run
    PrependShell: true
  - CmdLine:
    - 'pip install virtualenv wheel chardet # pycurl'
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sL https://deb.nodesource.com/setup | sudo bash - && sudo apt-get install
      -y nodejs
    Name: run
    PrependShell: true
  - CmdLine:
    - npm update -g npm && npm install -g grunt-cli && npm install -g bower
    Name: run
    PrependShell: true
  From:
    Image: ubuntu:14.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:14.04
