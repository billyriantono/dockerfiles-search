MetaArgs: null
Stages:
- BaseName: buildpack-deps:stable
  Commands:
  - CmdLine:
    - mkdir -p /plan9port  && wget -qO - https://github.com/9fans/plan9port/archive/master.tar.gz
      | tar --strip=1 -C /plan9port -xzf -
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /plan9port && ./INSTALL
    Name: run
    PrependShell: true
  - CmdLine:
    - printf "export PLAN9=/plan9port\nexport PATH=\$PATH:\$PLAN9/bin" > /root/.bashrc
    Name: run
    PrependShell: true
  From:
    Image: buildpack-deps:stable
  Name: ""
  Platform: ""
  SourceCode: FROM buildpack-deps:stable
