MetaArgs: null
Stages:
- BaseName: alpine:3.8
  Commands:
  - Key: TG_VERSION
    Name: arg
    Value: 6547c0b
  - Labels:
    - Key: maintainer
      Value: '"Carlos Remuzzi <carlosremuzzi@gmail.com>"'
    Name: label
  - Labels:
    - Key: version
      Value: 1.3.1
    Name: label
  - Name: workdir
    Path: /usr/src/build
  - CmdLine:
    - apk add --no-cache         jansson-dev         libconfig-dev         libevent-dev         lua-dev         openssl-dev         readline-dev     &&  apk
      add --no-cache --virtual .build-deps         build-base         curl         git         zlib-dev     &&
      git clone https://github.com/vysheng/tg     && cd tg     && git submodule update
      --init --recursive --force     && ./configure     && make     && apk del .build-deps     &&
      mv /usr/src/build/tg/bin/telegram-cli /usr/bin/telegram-cli     && mkdir /etc/telegram-cli     &&
      mv server.pub /etc/telegram-cli/server.pub     && rm -rf /usr/src/build     &&
      addgroup -g 1000 telegram     && adduser -u 1000 -G telegram -s /bin/sh -D telegram
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /
  - CmdLine:
    - telegram-cli
    Name: cmd
    PrependShell: false
  From:
    Image: alpine:3.8
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.8
