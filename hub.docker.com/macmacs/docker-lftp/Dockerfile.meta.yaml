MetaArgs: null
Stages:
- BaseName: alpine:latest
  Commands:
  - CmdLine:
    - "apk --no-cache --update add lftp ca-certificates openssh bash \tphp5-cli     php5-json
      \ttree     python     python-dev     py-pip     php-ctype     build-base     git
      \    && rm -rf /var/cache/apk/*     && git clone https://github.com/Gallore/yaml_cli
      \    && cd yaml_cli     && pip install .     && cd ..     && rm -R yaml_cli"
    Name: run
    PrependShell: true
  - CmdLine: null
    Name: entrypoint
    PrependShell: false
  - CmdLine:
    - --help
    Name: cmd
    PrependShell: false
  From:
    Image: alpine:latest
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:latest
