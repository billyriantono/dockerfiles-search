MetaArgs: null
Stages:
- BaseName: ruby:2.5.1-slim
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"cheimke@loumaris.com"'
    Name: label
  - Name: workdir
    Path: /app
  - Name: expose
    Ports:
    - "3000"
  - CmdLine:
    - apt-get update &&   apt-get install -y build-essential curl git nodejs imagemagick
      libpq-dev sqlite3 libsqlite3-dev &&   gem install bundler
    Name: run
    PrependShell: true
  - CmdLine:
    - curl -sL https://deb.nodesource.com/setup_10.x | bash -   && apt-get install
      -y nodejs
    Name: run
    PrependShell: true
  From:
    Image: ruby:2.5.1-slim
  Name: ""
  Platform: ""
  SourceCode: FROM ruby:2.5.1-slim
