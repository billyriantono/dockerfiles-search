MetaArgs: null
Stages:
- BaseName: land007/cpp-opencv:latest
  Commands:
  - Maintainer: Yiqiu Jia <yiqiujia@hotmail.com>
    Name: maintainer
  - CmdLine:
    - apt-get install -y --no-install-recommends libgflags-dev libboost-all-dev pkg-config
      libcurl4-openssl-dev x11-apps
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /tmp && wget https://github.com/google/glog/archive/v0.3.5.tar.gz && tar
      -zxvf v0.3.5.tar.gz && rm -r /tmp/v0.3.5.tar.gz
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /tmp/glog-0.3.5 && ./configure && make && make install && rm -rf /tmp/glog-0.3.5
    Name: run
    PrependShell: true
  - CmdLine:
    - cd /tmp && git clone https://github.com/FFmpeg/FFmpeg.git && cd FFmpeg && ./configure
      --enable-nonfree --enable-pic --enable-shared --disable-x86asm --disable-doc
      && make && make install && rm -rf /cpp/FFmpeg
    Name: run
    PrependShell: true
  - CmdLine:
    - echo $(date "+%Y-%m-%d_%H:%M:%S") >> /.image_times
    Name: run
    PrependShell: true
  - CmdLine:
    - echo $(date "+%Y-%m-%d_%H:%M:%S") > /.image_time
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "land007/cpp-boost " >> /.image_names
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "land007/cpp-boost " > /.image_name
    Name: run
    PrependShell: true
  From:
    Image: land007/cpp-opencv:latest
  Name: ""
  Platform: ""
  SourceCode: FROM land007/cpp-opencv:latest
