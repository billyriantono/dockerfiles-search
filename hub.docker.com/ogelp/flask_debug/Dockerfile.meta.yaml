MetaArgs: null
Stages:
- BaseName: ogelp/pyenv
  Commands:
  - Name: user
    User: python_user
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - requirements.in
    - requirements.in
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - pycharm-debug.egg
    - pycharm-debug.egg
  - CmdLine:
    - for version in $(pyenv versions | cut -c3- | cut -d' ' -f1); do pyenv global
      $version && pip-compile && pip-sync && pyenv rehash && easy_install pycharm-debug.egg;
      done &&    rm -f requirements.in requirements.txt pycharm-debug.egg
    Name: run
    PrependShell: true
  - CmdLine:
    - bash
    Name: cmd
    PrependShell: false
  From:
    Image: ogelp/pyenv
  Name: ""
  Platform: ""
  SourceCode: FROM ogelp/pyenv
