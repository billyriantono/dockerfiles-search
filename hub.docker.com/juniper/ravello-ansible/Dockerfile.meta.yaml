MetaArgs: null
Stages:
- BaseName: alpine:3.4
  Commands:
  - Maintainer: dgarros@gmail.com
    Name: maintainer
  - Key: ver_ansible
    Name: arg
    Value: 2.2.0.0
  - CmdLine:
    - mkdir /source &&    mkdir /project
    Name: run
    PrependShell: true
  - Name: workdir
    Path: /source
  - Chown: ""
    Name: add
    SourcesAndDest:
    - requirements.txt
    - requirements.txt
  - CmdLine:
    - apk update && apk add ca-certificates &&    apk add build-base make gcc g++
      python-dev py-pip openssl-dev curl libffi-dev &&    pip install --upgrade pip
      &&    pip install -r requirements.txt &&    pip install -q ansible==$ver_ansible
      &&    apk del -r --purge gcc make g++ &&    rm -rf /source/* &&    rm -rf /var/cache/apk/*
      &&    rm -rf /tmp/*
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - roles
    - /etc/ansible/roles
  - Name: workdir
    Path: /project
  From:
    Image: alpine:3.4
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.4
