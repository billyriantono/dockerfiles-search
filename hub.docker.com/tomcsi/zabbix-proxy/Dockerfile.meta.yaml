MetaArgs: null
Stages:
- BaseName: tomcsi/zabbix-proxy:base
  Commands:
  - CmdLine:
    - sed -ie s/\#\ ProxyMode=.*/ProxyMode=1/ /etc/zabbix/zabbix_proxy.conf
    Name: run
    PrependShell: true
  - Env:
    - Key: server
      Value: 127.0.0.1
    Name: env
  - Env:
    - Key: hostname
      Value: zabbix-active-proxy
    Name: env
  From:
    Image: tomcsi/zabbix-proxy:base
  Name: ""
  Platform: ""
  SourceCode: FROM tomcsi/zabbix-proxy:base
