MetaArgs: null
Stages:
- BaseName: alpine:3.8
  Commands:
  - CmdLine:
    - apk --update add docker make bash sudo curl wget
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash -c "(cat /root/cron > /etc/crontabs/root || true) && crond -d 0 -f"
    Name: cmd
    PrependShell: true
  From:
    Image: alpine:3.8
  Name: ""
  Platform: ""
  SourceCode: FROM alpine:3.8
