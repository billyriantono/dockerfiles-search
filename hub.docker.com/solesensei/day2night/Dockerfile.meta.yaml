MetaArgs: null
Stages:
- BaseName: nvidia/cuda:10.1-cudnn7-runtime-ubuntu18.04
  Commands:
  - Env:
    - Key: ANACONDA
      Value: /opt/anaconda
    Name: env
  - Env:
    - Key: PATH
      Value: $ANACONDA/bin:$PATH
    Name: env
  - Env:
    - Key: TZ
      Value: Europe/Moscow
    Name: env
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - Env:
    - Key: ANACONDA_VER
      Value: Anaconda3-5.2.0-Linux-x86_64.sh
    Name: env
  - CmdLine:
    - ln -snf /usr/share/zoneinfo/$TZ /etc/localtime && echo $TZ > /etc/timezone
    Name: run
    PrependShell: true
  - CmdLine:
    - apt-get update && apt-get install -y --no-install-recommends          apt-utils          wget          libopencv-dev          python-opencv          build-essential          cmake          git          curl          ca-certificates          libjpeg-dev          libpng-dev          axel          zip          unzip          vim
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "alias jup=\"bash /mnt/w/prj/jup\"" >> ~/.bashrc
    Name: run
    PrependShell: true
  - CmdLine:
    - wget https://repo.anaconda.com/archive/${ANACONDA_VER} -P /tmp
    Name: run
    PrependShell: true
  - CmdLine:
    - bash /tmp/${ANACONDA_VER} -b -p $ANACONDA
    Name: run
    PrependShell: true
  - CmdLine:
    - rm /tmp/${ANACONDA_VER} -rf
    Name: run
    PrependShell: true
  - CmdLine:
    - conda install -y pytorch torchvision cudatoolkit=10.0 -c pytorch
    Name: run
    PrependShell: true
  - CmdLine:
    - conda install -y pip yaml -c anaconda
    Name: run
    PrependShell: true
  - CmdLine:
    - pip install tensorboard tensorboardX;
    Name: run
    PrependShell: true
  From:
    Image: nvidia/cuda:10.1-cudnn7-runtime-ubuntu18.04
  Name: ""
  Platform: ""
  SourceCode: FROM nvidia/cuda:10.1-cudnn7-runtime-ubuntu18.04
