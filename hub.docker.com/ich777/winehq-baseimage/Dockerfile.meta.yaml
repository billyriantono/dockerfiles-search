MetaArgs: null
Stages:
- BaseName: ich777/debian-baseimage
  Commands:
  - Labels:
    - Key: maintainer
      Value: '"admin@minenet.at"'
    Name: label
  - CmdLine:
    - "dpkg --add-architecture i386 && \tapt-get update && \tapt-get -y install wget
      gnupg software-properties-common && \tapt-get -y install --reinstall ca-certificates
      && \twget -qO - https://dl.winehq.org/wine-builds/winehq.key | apt-key add -
      && \tapt-add-repository https://dl.winehq.org/wine-builds/debian/ && \tapt-get
      update && \tapt-get -y install --install-recommends winehq-stable && \tadd-apt-repository
      --remove 'deb https://dl.winehq.org/wine-builds/ubuntu/ bionic main' && \tapt-get
      -y --purge remove software-properties-common gnupg && \tapt-get -y autoremove
      && \trm -rf /var/lib/apt/lists/*"
    Name: run
    PrependShell: true
  From:
    Image: ich777/debian-baseimage
  Name: ""
  Platform: ""
  SourceCode: FROM ich777/debian-baseimage
