MetaArgs: null
Stages:
- BaseName: php:5.6.30-apache
  Commands:
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - docker-entrypoint.sh
    - /usr/local/bin/docker-entrypoint
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - resize.php
    - /var/www/html/
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - attachments.php
    - /var/www/html/
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - phpinfo.php
    - /var/www/html/
  - CmdLine:
    - docker-php-ext-install mysqli sockets &&   a2enmod rewrite &&   apt-get update
      && apt-get install -y libmemcached11 libmemcachedutil2   libz-dev libmemcached-dev
      build-essential wget zlib1g-dev libfreetype6   libfreetype6-dev libjpeg62-turbo
      libjpeg62-turbo-dev php5-curl &&   docker-php-ext-install zip &&   docker-php-ext-configure
      gd --with-freetype-dir=/usr/include/ --with-jpeg-dir=/usr/include/ &&   docker-php-ext-install
      gd
    Name: run
    PrependShell: true
  - CmdLine:
    - echo display_errors = 0 > /usr/local/etc/php/php.ini &&   apt-get remove -y
      build-essential libmemcached-dev libz-dev &&   apt-get autoremove -y &&   apt-get
      clean &&   rm -rf /tmp/pear &&   chmod ugo+x /usr/local/bin/docker-entrypoint
    Name: run
    PrependShell: true
  - CmdLine:
    - docker-entrypoint
    Name: entrypoint
    PrependShell: false
  From:
    Image: php:5.6.30-apache
  Name: ""
  Platform: ""
  SourceCode: FROM php:5.6.30-apache
