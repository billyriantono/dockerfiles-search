MetaArgs: null
Stages:
- BaseName: ubuntu:18.04
  Commands:
  - Env:
    - Key: DEBIAN_FRONTEND
      Value: noninteractive
    Name: env
  - CmdLine:
    - "apt-get update &&         apt-get install -y \ttzdata         nano         wget
      \        curl         ruby         rubygems-integration         ruby-dev         libmysqlclient-dev
      \        build-essential         git         curl         sudo"
    Name: run
    PrependShell: true
  - CmdLine:
    - "apt-get update && apt-get install -y locales && rm -rf /var/lib/apt/lists/*
      \t&& localedef -i en_US -c -f UTF-8 -A /usr/share/locale/locale.alias en_US.UTF-8"
    Name: run
    PrependShell: true
  - Env:
    - Key: LANG
      Value: en_US.utf8
    Name: env
  - CmdLine:
    - gem install rake bundler foreman mysql2
    Name: run
    PrependShell: true
  - CmdLine:
    - "git clone https://github.com/huginn/huginn.git && \tcd huginn && \tcp .env.example
      .env && \tbundle"
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "mysql-server mysql-server/root_password password " | debconf-set-selections
    Name: run
    PrependShell: true
  - CmdLine:
    - echo "mysql-server mysql-server/root_password_again password " | debconf-set-selections
    Name: run
    PrependShell: true
  - CmdLine:
    - "apt-get update && \tapt-get -y install mysql-server-5.7 && \tmkdir -p /var/lib/mysql
      && \tmkdir -p /var/run/mysqld && \tmkdir -p /var/log/mysql && \tchown -R mysql:mysql
      /var/lib/mysql && \tchown -R mysql:mysql /var/run/mysqld && \tchown -R mysql:mysql
      /var/log/mysql"
    Name: run
    PrependShell: true
  - CmdLine:
    - "sed -i -e \"$ a [client]\\n\\n[mysql]\\n\\n[mysqld]\"  /etc/mysql/my.cnf &&
      \tsed -i -e \"s/\\(\\[client\\]\\)/\\1\\ndefault-character-set = utf8/g\" /etc/mysql/my.cnf
      && \tsed -i -e \"s/\\(\\[mysql\\]\\)/\\1\\ndefault-character-set = utf8/g\"
      /etc/mysql/my.cnf && \tsed -i -e \"s/\\(\\[mysqld\\]\\)/\\1\\ninit_connect='SET
      NAMES utf8'\\ncharacter-set-server = utf8\\ncollation-server=utf8_unicode_ci\\nbind-address
      = 0.0.0.0/g\" /etc/mysql/my.cnf"
    Name: run
    PrependShell: true
  - Name: volume
    Volumes:
    - /var/lib/mysql
  - CmdLine:
    - 'echo "docker ALL=NOPASSWD: ALL" >> /etc/sudoers'
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - init-db.sh
    - /usr/sbin/
  - CmdLine:
    - chmod +x /usr/sbin/init-db.sh
    Name: run
    PrependShell: true
  - Chown: ""
    From: ""
    Name: copy
    SourcesAndDest:
    - start-huginn.sh
    - /usr/sbin/
  - CmdLine:
    - chmod +x /usr/sbin/start-huginn.sh
    Name: run
    PrependShell: true
  - CmdLine:
    - /usr/sbin/start-huginn.sh
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:18.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:18.04
