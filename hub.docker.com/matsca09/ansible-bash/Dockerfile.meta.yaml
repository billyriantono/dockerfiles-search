MetaArgs: null
Stages:
- BaseName: ubuntu:latest
  Commands:
  - CmdLine:
    - "apt update && \tapt install -y software-properties-common && \tapt-add-repository
      ppa:ansible/ansible && \tapt update && \tapt install -y ansible python-shade
      vim openssh-client && \tapt clean"
    Name: run
    PrependShell: true
  - Name: workdir
    Path: '"/ansible"'
  - CmdLine:
    - /bin/bash
    Name: entrypoint
    PrependShell: false
  From:
    Image: ubuntu:latest
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:latest
