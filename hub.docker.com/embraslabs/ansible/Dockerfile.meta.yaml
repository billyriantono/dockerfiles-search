MetaArgs: null
Stages:
- BaseName: ubuntu:16.04
  Commands:
  - Name: workdir
    Path: /app
  - CmdLine:
    - apt-get update -y &&   apt-get install apt-utils -y &&   apt-get install software-properties-common
      -y &&   add-apt-repository ppa:ansible/ansible -y &&   apt-get update -y &&   apt-get
      install ansible -y &&   apt-get install vim -y
    Name: run
    PrependShell: true
  - CmdLine:
    - /bin/bash
    Name: cmd
    PrependShell: false
  From:
    Image: ubuntu:16.04
  Name: ""
  Platform: ""
  SourceCode: FROM ubuntu:16.04
